// Code generated by `make generate-ui-types`. DO NOT EDIT.

/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export type paths = Record<string, never>;
export type webhooks = Record<string, never>;
export type components = {
    schemas: {
        /** @description ArgoCDCommitStatus is the Schema for the argocdcommitstatuses API. */
        ArgoCDCommitStatus: {
            /**
             * @description APIVersion defines the versioned schema of this representation of an object.
             *     Servers should convert recognized schemas to the latest internal value, and
             *     may reject unrecognized values.
             *     More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: string;
            /**
             * @description Kind is a string value representing the REST resource this object represents.
             *     Servers may infer this from the endpoint the client submits requests to.
             *     Cannot be updated.
             *     In CamelCase.
             *     More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: string;
            metadata: Record<string, never>;
            /** @description ArgoCDCommitStatusSpec defines the desired state of ArgoCDCommitStatus. */
            spec?: {
                /** @description ApplicationSelector is a label selector that selects the Argo CD applications to which this commit status applies. */
                applicationSelector: {
                    /** @description matchExpressions is a list of label selector requirements. The requirements are ANDed. */
                    matchExpressions?: {
                        /** @description key is the label key that the selector applies to. */
                        key: string;
                        /**
                         * @description operator represents a key's relationship to a set of values.
                         *     Valid operators are In, NotIn, Exists and DoesNotExist.
                         */
                        operator: string;
                        /**
                         * @description values is an array of string values. If the operator is In or NotIn,
                         *     the values array must be non-empty. If the operator is Exists or DoesNotExist,
                         *     the values array must be empty. This array is replaced during a strategic
                         *     merge patch.
                         */
                        values?: string[];
                    }[];
                    /**
                     * @description matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                     *     map is equivalent to an element of matchExpressions, whose key field is "key", the
                     *     operator is "In", and the values array contains only "value". The requirements are ANDed.
                     */
                    matchLabels?: {
                        [key: string]: string;
                    };
                };
                /**
                 * @description Key is the gate name referenced in the PromotionStrategy's activeCommitStatuses.
                 *     When omitted, the CRD default is argocd-health. Set Key explicitly, even if you use the CRD default.
                 *     Must be lowercase alphanumeric with hyphens, 1–63 characters (pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$).
                 * @default argocd-health
                 */
                key?: string;
                /** @description PromotionStrategyRef is a reference to the promotion strategy that this commit status applies to. */
                promotionStrategyRef: {
                    /** @description Name is the name of the object to refer to. */
                    name: string;
                };
                /** @description URL generates the URL to use in the CommitStatus, for example a link to the Argo CD UI. */
                url?: {
                    /**
                     * @description Options sets options for the template. Options are described by
                     *     strings, either a simple string or "key=value". There can be at
                     *     most one equals sign in an option string. If the option string
                     *     is unrecognized or otherwise invalid, Option panics.
                     *
                     *     Known options:
                     *
                     *     missingkey: Control the behavior during execution if a map is
                     *     indexed with a key that is not present in the map.
                     *
                     *     	"missingkey=default" or "missingkey=invalid"
                     *     		The default behavior: Do nothing and continue execution.
                     *     		If printed, the result of the index operation is the string
                     *     		"<no value>".
                     *     	"missingkey=zero"
                     *     		The operation returns the zero value for the map type's element.
                     *     	"missingkey=error"
                     *     		Execution stops immediately with an error.
                     */
                    options?: string[];
                    /**
                     * @description Template is a go text template and receives .Environment and .ArgoCDCommitStatus variables. A function called urlQueryEscape
                     *     is available to escape url query parameters. The template can be configured with options to control the behavior
                     *     during execution if a variable is not present.
                     *
                     *     Example:
                     *
                     *       {{- $baseURL := "https://dev.argocd.local" -}}
                     *       {{- if eq .Environment "environment/development" -}}
                     *       {{- $baseURL = "https://dev.argocd.local" -}}
                     *       {{- else if eq .Environment "environment/staging" -}}
                     *       {{- $baseURL = "https://staging.argocd.local" -}}
                     *       {{- else if eq .Environment "environment/production" -}}
                     *       {{- $baseURL = "https://prod.argocd.local" -}}
                     *       {{- end -}}
                     *       {{- $labels := "" -}}
                     *       {{- range $key, $value := .ArgoCDCommitStatus.Spec.ApplicationSelector.MatchLabels -}}
                     *       {{- $labels = printf "%s%s=%s," $labels $key $value -}}
                     *       {{- end -}}
                     *       {{ printf "%s/applications?labels=%s" $baseURL (urlQueryEscape $labels) }}
                     */
                    template?: string;
                };
            };
            /** @description ArgoCDCommitStatusStatus defines the observed state of ArgoCDCommitStatus. */
            status?: {
                /**
                 * @description ApplicationsSelected represents the Argo CD applications that are selected by the commit status.
                 *     This field is sorted by environment (same order as the referenced PromotionStrategy), then namespace, then name.
                 */
                applicationsSelected?: {
                    /**
                     * @description ClusterName is the name of the cluster that the application manifest is deployed to. An empty string indicates
                     *     the local cluster. There is no minimum length, since the local cluster is represented by an empty string.
                     */
                    clusterName: string;
                    /** @description Environment is the syncSource.targetBranch of the Argo CD application (in effect, its environment). */
                    environment: string;
                    /**
                     * Format: date-time
                     * @description LastTransitionTime is the last time the phase transitioned.
                     */
                    lastTransitionTime?: string;
                    /** @description Name is the name of the Argo CD application. */
                    name: string;
                    /** @description Namespace is the namespace of the Argo CD application. */
                    namespace: string;
                    /**
                     * @description Phase is the current phase of the commit status.
                     * @enum {string}
                     */
                    phase: "pending" | "success" | "failure";
                    /**
                     * @description Sha is the commit SHA that the application is synced to.
                     *     This field is only populated when the application's sync status is Synced.
                     *     When the sync status is OutOfSync or Unknown, this field will be empty because
                     *     Status.Sync.Revision may contain a branch name instead of a SHA.
                     *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                     */
                    sha: string;
                }[];
                /** @description Conditions Represents the observations of the current state. */
                conditions?: {
                    /**
                     * Format: date-time
                     * @description lastTransitionTime is the last time the condition transitioned from one status to another.
                     *     This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                     */
                    lastTransitionTime: string;
                    /**
                     * @description message is a human readable message indicating details about the transition.
                     *     This may be an empty string.
                     */
                    message: string;
                    /**
                     * Format: int64
                     * @description observedGeneration represents the .metadata.generation that the condition was set based upon.
                     *     For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                     *     with respect to the current state of the instance.
                     */
                    observedGeneration?: number;
                    /**
                     * @description reason contains a programmatic identifier indicating the reason for the condition's last transition.
                     *     Producers of specific condition types may define expected values and meanings for this field,
                     *     and whether the values are considered a guaranteed API.
                     *     The value should be a CamelCase string.
                     *     This field may not be empty.
                     */
                    reason: string;
                    /**
                     * @description status of the condition, one of True, False, Unknown.
                     * @enum {string}
                     */
                    status: "True" | "False" | "Unknown";
                    /** @description type of condition in CamelCase or in foo.example.com/CamelCase. */
                    type: string;
                }[];
                /**
                 * Format: int64
                 * @description ObservedGeneration is the .metadata.generation that this status was reconciled from.
                 *     Because status is written via Server-Side Apply with ForceOwnership (which has no
                 *     optimistic-concurrency check), this field is the canonical way to detect stale
                 *     status writes: compare status.observedGeneration with metadata.generation.
                 */
                observedGeneration?: number;
            };
        };
        /** @description ChangeTransferPolicy is the Schema for the changetransferpolicies API */
        ChangeTransferPolicy: {
            /**
             * @description APIVersion defines the versioned schema of this representation of an object.
             *     Servers should convert recognized schemas to the latest internal value, and
             *     may reject unrecognized values.
             *     More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: string;
            /**
             * @description Kind is a string value representing the REST resource this object represents.
             *     Servers may infer this from the endpoint the client submits requests to.
             *     Cannot be updated.
             *     In CamelCase.
             *     More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: string;
            metadata: Record<string, never>;
            /** @description ChangeTransferPolicySpec defines the desired state of ChangeTransferPolicy */
            spec?: {
                /** @description ActiveBranch staging hydrated branch */
                activeBranch: string;
                /** @description ActiveCommitStatuses lists the statuses to be monitored on the active branch */
                activeCommitStatuses?: {
                    key: string;
                }[];
                /**
                 * @description ActivePath is an optional repository subpath for this policy's active state.
                 *     When set, hydrator metadata is read from <activePath>/hydrator.metadata.
                 */
                activePath?: string;
                /** @default true */
                autoMerge?: boolean;
                /** @description RepositoryReference what repository to open the PR on. */
                gitRepositoryRef: {
                    /** @description Name is the name of the object to refer to. */
                    name: string;
                };
                /** @description ProposedBranch staging hydrated branch */
                proposedBranch: string;
                /** @description ProposedCommitStatuses lists the statuses to be monitored on the proposed branch */
                proposedCommitStatuses?: {
                    key: string;
                }[];
            };
            /** @description ChangeTransferPolicyStatus defines the observed state of ChangeTransferPolicy */
            status?: {
                /** @description Active is the state of the active branch. */
                active?: {
                    /** @description CommitStatuses is a list of commit statuses that are being monitored for this branch. */
                    commitStatuses?: {
                        /** @description Description is the description of the commit status */
                        description?: string;
                        /** @description Key staging hydrated branch */
                        key: string;
                        /**
                         * @description Phase what phase is the status in
                         * @enum {string}
                         */
                        phase: "pending" | "success" | "failure";
                        /** @description Url is the URL of the commit status */
                        url?: string;
                    }[];
                    /** @description Dry is the dry state of the branch, which is the commit that is being proposed. */
                    dry?: {
                        /** @description Author is the author of the commit */
                        author?: string;
                        /** @description Body is the body of the commit message without the subject line */
                        body?: string;
                        /**
                         * Format: date-time
                         * @description CommitTime is the time the commit was made
                         */
                        commitTime?: string;
                        /** @description References are the references to other commits, that went into the hydration of the branch */
                        references?: {
                            /** @description Commit contains metadata about the commit that is related in some way to another commit. */
                            commit?: {
                                /** @description Author is the author of the commit. */
                                author?: string;
                                /** @description Body is the body of the commit message, excluding the subject line, i.e. `git show --format=%b`. */
                                body?: string;
                                /**
                                 * Format: date-time
                                 * @description Date is the date of the commit, formatted as by `git show -s --format=%aI`.
                                 */
                                date?: string;
                                /** @description RepoURL is the URL of the repository where the commit is located. */
                                repoURL?: string;
                                /**
                                 * @description Sha is the commit hash.
                                 *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                                 */
                                sha?: string;
                                /** @description Subject is the subject line of the commit message, i.e. `git show --format=%s`. */
                                subject?: string;
                            };
                        }[];
                        /** @description RepoURL is the URL of the repository where the commit is located */
                        repoURL?: string;
                        /**
                         * @description Sha is the SHA of the commit in the branch
                         *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                         */
                        sha?: string;
                        /** @description Subject is the subject line of the commit message */
                        subject?: string;
                    };
                    /** @description Hydrated is the hydrated state of the branch, which is the commit that is currently being worked on. */
                    hydrated?: {
                        /** @description Author is the author of the commit */
                        author?: string;
                        /** @description Body is the body of the commit message without the subject line */
                        body?: string;
                        /**
                         * Format: date-time
                         * @description CommitTime is the time the commit was made
                         */
                        commitTime?: string;
                        /** @description References are the references to other commits, that went into the hydration of the branch */
                        references?: {
                            /** @description Commit contains metadata about the commit that is related in some way to another commit. */
                            commit?: {
                                /** @description Author is the author of the commit. */
                                author?: string;
                                /** @description Body is the body of the commit message, excluding the subject line, i.e. `git show --format=%b`. */
                                body?: string;
                                /**
                                 * Format: date-time
                                 * @description Date is the date of the commit, formatted as by `git show -s --format=%aI`.
                                 */
                                date?: string;
                                /** @description RepoURL is the URL of the repository where the commit is located. */
                                repoURL?: string;
                                /**
                                 * @description Sha is the commit hash.
                                 *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                                 */
                                sha?: string;
                                /** @description Subject is the subject line of the commit message, i.e. `git show --format=%s`. */
                                subject?: string;
                            };
                        }[];
                        /** @description RepoURL is the URL of the repository where the commit is located */
                        repoURL?: string;
                        /**
                         * @description Sha is the SHA of the commit in the branch
                         *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                         */
                        sha?: string;
                        /** @description Subject is the subject line of the commit message */
                        subject?: string;
                    };
                    /** @description Note is the hydrator metadata from the git note attached to the hydrated commit. */
                    note?: {
                        /** @description Author is the author of the dry commit that was used to hydrate the branch. */
                        author?: string;
                        /** @description Body is the body of the dry commit that was used to hydrate the branch without the subject. */
                        body?: string;
                        /**
                         * Format: date-time
                         * @description Date is the date of the dry commit that was used to hydrate the branch.
                         */
                        date?: string;
                        /**
                         * @description DrySha is the SHA of the commit that was used as the dry source for hydration.
                         *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                         */
                        drySha?: string;
                        /** @description References are the references to other commits, that went into the hydration of the branch. */
                        references?: {
                            /** @description Commit contains metadata about the commit that is related in some way to another commit. */
                            commit?: {
                                /** @description Author is the author of the commit. */
                                author?: string;
                                /** @description Body is the body of the commit message, excluding the subject line, i.e. `git show --format=%b`. */
                                body?: string;
                                /**
                                 * Format: date-time
                                 * @description Date is the date of the commit, formatted as by `git show -s --format=%aI`.
                                 */
                                date?: string;
                                /** @description RepoURL is the URL of the repository where the commit is located. */
                                repoURL?: string;
                                /**
                                 * @description Sha is the commit hash.
                                 *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                                 */
                                sha?: string;
                                /** @description Subject is the subject line of the commit message, i.e. `git show --format=%s`. */
                                subject?: string;
                            };
                        }[];
                        /** @description RepoURL is the URL of the repository where the commit is located. */
                        repoURL?: string;
                        /** @description Subject is the subject line of the dry commit that was used to hydrate the branch. */
                        subject?: string;
                    };
                };
                /** @description Conditions Represents the observations of the current state. */
                conditions?: {
                    /**
                     * Format: date-time
                     * @description lastTransitionTime is the last time the condition transitioned from one status to another.
                     *     This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                     */
                    lastTransitionTime: string;
                    /**
                     * @description message is a human readable message indicating details about the transition.
                     *     This may be an empty string.
                     */
                    message: string;
                    /**
                     * Format: int64
                     * @description observedGeneration represents the .metadata.generation that the condition was set based upon.
                     *     For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                     *     with respect to the current state of the instance.
                     */
                    observedGeneration?: number;
                    /**
                     * @description reason contains a programmatic identifier indicating the reason for the condition's last transition.
                     *     Producers of specific condition types may define expected values and meanings for this field,
                     *     and whether the values are considered a guaranteed API.
                     *     The value should be a CamelCase string.
                     *     This field may not be empty.
                     */
                    reason: string;
                    /**
                     * @description status of the condition, one of True, False, Unknown.
                     * @enum {string}
                     */
                    status: "True" | "False" | "Unknown";
                    /** @description type of condition in CamelCase or in foo.example.com/CamelCase. */
                    type: string;
                }[];
                /**
                 * @description History defines the history of promoted changes done by the ChangeTransferPolicy. You can think of
                 *     it as a list of PRs merged by GitOps Promoter. It will not include changes that were manually merged.
                 *     The history length is hard-coded to be at most 5 entries. This may change in the future.
                 *     History is constructed on a best-effort basis and should be used for informational purposes only.
                 *     History is in reverse chronological order (newest is first).
                 */
                history?: {
                    /** @description Active is the state of the active branch at the time the PR was merged. */
                    active?: {
                        /** @description CommitStatuses is a list of commit statuses that are being monitored for this branch. */
                        commitStatuses?: {
                            /** @description Description is the description of the commit status */
                            description?: string;
                            /** @description Key staging hydrated branch */
                            key: string;
                            /**
                             * @description Phase what phase is the status in
                             * @enum {string}
                             */
                            phase: "pending" | "success" | "failure";
                            /** @description Url is the URL of the commit status */
                            url?: string;
                        }[];
                        /** @description Dry is the dry state of the branch, which is the commit that is being proposed. */
                        dry?: {
                            /** @description Author is the author of the commit */
                            author?: string;
                            /** @description Body is the body of the commit message without the subject line */
                            body?: string;
                            /**
                             * Format: date-time
                             * @description CommitTime is the time the commit was made
                             */
                            commitTime?: string;
                            /** @description References are the references to other commits, that went into the hydration of the branch */
                            references?: {
                                /** @description Commit contains metadata about the commit that is related in some way to another commit. */
                                commit?: {
                                    /** @description Author is the author of the commit. */
                                    author?: string;
                                    /** @description Body is the body of the commit message, excluding the subject line, i.e. `git show --format=%b`. */
                                    body?: string;
                                    /**
                                     * Format: date-time
                                     * @description Date is the date of the commit, formatted as by `git show -s --format=%aI`.
                                     */
                                    date?: string;
                                    /** @description RepoURL is the URL of the repository where the commit is located. */
                                    repoURL?: string;
                                    /**
                                     * @description Sha is the commit hash.
                                     *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                                     */
                                    sha?: string;
                                    /** @description Subject is the subject line of the commit message, i.e. `git show --format=%s`. */
                                    subject?: string;
                                };
                            }[];
                            /** @description RepoURL is the URL of the repository where the commit is located */
                            repoURL?: string;
                            /**
                             * @description Sha is the SHA of the commit in the branch
                             *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                             */
                            sha?: string;
                            /** @description Subject is the subject line of the commit message */
                            subject?: string;
                        };
                        /** @description Hydrated is the hydrated state of the branch, which is the commit that is currently being worked on. */
                        hydrated?: {
                            /** @description Author is the author of the commit */
                            author?: string;
                            /** @description Body is the body of the commit message without the subject line */
                            body?: string;
                            /**
                             * Format: date-time
                             * @description CommitTime is the time the commit was made
                             */
                            commitTime?: string;
                            /** @description References are the references to other commits, that went into the hydration of the branch */
                            references?: {
                                /** @description Commit contains metadata about the commit that is related in some way to another commit. */
                                commit?: {
                                    /** @description Author is the author of the commit. */
                                    author?: string;
                                    /** @description Body is the body of the commit message, excluding the subject line, i.e. `git show --format=%b`. */
                                    body?: string;
                                    /**
                                     * Format: date-time
                                     * @description Date is the date of the commit, formatted as by `git show -s --format=%aI`.
                                     */
                                    date?: string;
                                    /** @description RepoURL is the URL of the repository where the commit is located. */
                                    repoURL?: string;
                                    /**
                                     * @description Sha is the commit hash.
                                     *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                                     */
                                    sha?: string;
                                    /** @description Subject is the subject line of the commit message, i.e. `git show --format=%s`. */
                                    subject?: string;
                                };
                            }[];
                            /** @description RepoURL is the URL of the repository where the commit is located */
                            repoURL?: string;
                            /**
                             * @description Sha is the SHA of the commit in the branch
                             *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                             */
                            sha?: string;
                            /** @description Subject is the subject line of the commit message */
                            subject?: string;
                        };
                        /** @description Note is the hydrator metadata from the git note attached to the hydrated commit. */
                        note?: {
                            /** @description Author is the author of the dry commit that was used to hydrate the branch. */
                            author?: string;
                            /** @description Body is the body of the dry commit that was used to hydrate the branch without the subject. */
                            body?: string;
                            /**
                             * Format: date-time
                             * @description Date is the date of the dry commit that was used to hydrate the branch.
                             */
                            date?: string;
                            /**
                             * @description DrySha is the SHA of the commit that was used as the dry source for hydration.
                             *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                             */
                            drySha?: string;
                            /** @description References are the references to other commits, that went into the hydration of the branch. */
                            references?: {
                                /** @description Commit contains metadata about the commit that is related in some way to another commit. */
                                commit?: {
                                    /** @description Author is the author of the commit. */
                                    author?: string;
                                    /** @description Body is the body of the commit message, excluding the subject line, i.e. `git show --format=%b`. */
                                    body?: string;
                                    /**
                                     * Format: date-time
                                     * @description Date is the date of the commit, formatted as by `git show -s --format=%aI`.
                                     */
                                    date?: string;
                                    /** @description RepoURL is the URL of the repository where the commit is located. */
                                    repoURL?: string;
                                    /**
                                     * @description Sha is the commit hash.
                                     *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                                     */
                                    sha?: string;
                                    /** @description Subject is the subject line of the commit message, i.e. `git show --format=%s`. */
                                    subject?: string;
                                };
                            }[];
                            /** @description RepoURL is the URL of the repository where the commit is located. */
                            repoURL?: string;
                            /** @description Subject is the subject line of the dry commit that was used to hydrate the branch. */
                            subject?: string;
                        };
                    };
                    /** @description Proposed is the state of the proposed branch at the time the PR was merged. */
                    proposed?: {
                        /**
                         * @description CommitStatuses is a list of commit statuses that were being monitored for this branch.
                         *     This contains the state frozen at the moment the PR was merged.
                         */
                        commitStatuses?: {
                            /** @description Description is the description of the commit status */
                            description?: string;
                            /** @description Key staging hydrated branch */
                            key: string;
                            /**
                             * @description Phase what phase is the status in
                             * @enum {string}
                             */
                            phase: "pending" | "success" | "failure";
                            /** @description Url is the URL of the commit status */
                            url?: string;
                        }[];
                        /** @description Hydrated is the hydrated state of the branch, which is the commit that is currently being worked on. */
                        hydrated?: {
                            /** @description Author is the author of the commit */
                            author?: string;
                            /** @description Body is the body of the commit message without the subject line */
                            body?: string;
                            /**
                             * Format: date-time
                             * @description CommitTime is the time the commit was made
                             */
                            commitTime?: string;
                            /** @description References are the references to other commits, that went into the hydration of the branch */
                            references?: {
                                /** @description Commit contains metadata about the commit that is related in some way to another commit. */
                                commit?: {
                                    /** @description Author is the author of the commit. */
                                    author?: string;
                                    /** @description Body is the body of the commit message, excluding the subject line, i.e. `git show --format=%b`. */
                                    body?: string;
                                    /**
                                     * Format: date-time
                                     * @description Date is the date of the commit, formatted as by `git show -s --format=%aI`.
                                     */
                                    date?: string;
                                    /** @description RepoURL is the URL of the repository where the commit is located. */
                                    repoURL?: string;
                                    /**
                                     * @description Sha is the commit hash.
                                     *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                                     */
                                    sha?: string;
                                    /** @description Subject is the subject line of the commit message, i.e. `git show --format=%s`. */
                                    subject?: string;
                                };
                            }[];
                            /** @description RepoURL is the URL of the repository where the commit is located */
                            repoURL?: string;
                            /**
                             * @description Sha is the SHA of the commit in the branch
                             *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                             */
                            sha?: string;
                            /** @description Subject is the subject line of the commit message */
                            subject?: string;
                        };
                    };
                    /** @description PullRequest is the state of the pull request that was created for this ChangeTransferPolicy. */
                    pullRequest?: {
                        /**
                         * @description ExternallyMergedOrClosed indicates that the pull request is no longer open on the SCM while the
                         *     PullRequest still desired it open: merged or closed outside the controller, or closed on the SCM
                         *     because the PullRequest resource was deleted (finalizer) before this status was reconciled.
                         *     When true, the State field will be empty ("") since we cannot tell merge vs. close from the provider.
                         *     This status is preserved even after the PullRequest resource is deleted, maintaining a historical
                         *     record until a new pull request is created for this environment.
                         */
                        externallyMergedOrClosed?: boolean;
                        /** @description ID is the unique identifier of the pull request, set by the SCM. */
                        id?: string;
                        /**
                         * Format: date-time
                         * @description PRCreationTime is the time when the pull request was created.
                         */
                        prCreationTime?: string;
                        /**
                         * Format: date-time
                         * @description PRMergeTime is the time when the pull request was merged. This time can vary slightly from the actual merge time because
                         *     it is the time when the ChangeTransferPolicy controller sets the pull requests spec to merge. In the future we plan on making
                         *     this time more accurate by fetching the actual merge time from the SCM via the webhook this would then be updated in the git note
                         *     for that commit.
                         */
                        prMergeTime?: string;
                        /**
                         * @description State is the state of the pull request.
                         * @enum {string}
                         */
                        state?: "closed" | "merged" | "open";
                        /** @description Url is the URL of the pull request. */
                        url?: string;
                    };
                }[];
                /**
                 * Format: int64
                 * @description ObservedGeneration is the .metadata.generation that this status was reconciled from.
                 *     Because status is written via Server-Side Apply with ForceOwnership (which has no
                 *     optimistic-concurrency check), this field is the canonical way to detect stale
                 *     status writes: compare status.observedGeneration with metadata.generation.
                 */
                observedGeneration?: number;
                /** @description Proposed is the state of the proposed branch. */
                proposed?: {
                    /** @description CommitStatuses is a list of commit statuses that are being monitored for this branch. */
                    commitStatuses?: {
                        /** @description Description is the description of the commit status */
                        description?: string;
                        /** @description Key staging hydrated branch */
                        key: string;
                        /**
                         * @description Phase what phase is the status in
                         * @enum {string}
                         */
                        phase: "pending" | "success" | "failure";
                        /** @description Url is the URL of the commit status */
                        url?: string;
                    }[];
                    /** @description Dry is the dry state of the branch, which is the commit that is being proposed. */
                    dry?: {
                        /** @description Author is the author of the commit */
                        author?: string;
                        /** @description Body is the body of the commit message without the subject line */
                        body?: string;
                        /**
                         * Format: date-time
                         * @description CommitTime is the time the commit was made
                         */
                        commitTime?: string;
                        /** @description References are the references to other commits, that went into the hydration of the branch */
                        references?: {
                            /** @description Commit contains metadata about the commit that is related in some way to another commit. */
                            commit?: {
                                /** @description Author is the author of the commit. */
                                author?: string;
                                /** @description Body is the body of the commit message, excluding the subject line, i.e. `git show --format=%b`. */
                                body?: string;
                                /**
                                 * Format: date-time
                                 * @description Date is the date of the commit, formatted as by `git show -s --format=%aI`.
                                 */
                                date?: string;
                                /** @description RepoURL is the URL of the repository where the commit is located. */
                                repoURL?: string;
                                /**
                                 * @description Sha is the commit hash.
                                 *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                                 */
                                sha?: string;
                                /** @description Subject is the subject line of the commit message, i.e. `git show --format=%s`. */
                                subject?: string;
                            };
                        }[];
                        /** @description RepoURL is the URL of the repository where the commit is located */
                        repoURL?: string;
                        /**
                         * @description Sha is the SHA of the commit in the branch
                         *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                         */
                        sha?: string;
                        /** @description Subject is the subject line of the commit message */
                        subject?: string;
                    };
                    /** @description Hydrated is the hydrated state of the branch, which is the commit that is currently being worked on. */
                    hydrated?: {
                        /** @description Author is the author of the commit */
                        author?: string;
                        /** @description Body is the body of the commit message without the subject line */
                        body?: string;
                        /**
                         * Format: date-time
                         * @description CommitTime is the time the commit was made
                         */
                        commitTime?: string;
                        /** @description References are the references to other commits, that went into the hydration of the branch */
                        references?: {
                            /** @description Commit contains metadata about the commit that is related in some way to another commit. */
                            commit?: {
                                /** @description Author is the author of the commit. */
                                author?: string;
                                /** @description Body is the body of the commit message, excluding the subject line, i.e. `git show --format=%b`. */
                                body?: string;
                                /**
                                 * Format: date-time
                                 * @description Date is the date of the commit, formatted as by `git show -s --format=%aI`.
                                 */
                                date?: string;
                                /** @description RepoURL is the URL of the repository where the commit is located. */
                                repoURL?: string;
                                /**
                                 * @description Sha is the commit hash.
                                 *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                                 */
                                sha?: string;
                                /** @description Subject is the subject line of the commit message, i.e. `git show --format=%s`. */
                                subject?: string;
                            };
                        }[];
                        /** @description RepoURL is the URL of the repository where the commit is located */
                        repoURL?: string;
                        /**
                         * @description Sha is the SHA of the commit in the branch
                         *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                         */
                        sha?: string;
                        /** @description Subject is the subject line of the commit message */
                        subject?: string;
                    };
                    /** @description Note is the hydrator metadata from the git note attached to the hydrated commit. */
                    note?: {
                        /** @description Author is the author of the dry commit that was used to hydrate the branch. */
                        author?: string;
                        /** @description Body is the body of the dry commit that was used to hydrate the branch without the subject. */
                        body?: string;
                        /**
                         * Format: date-time
                         * @description Date is the date of the dry commit that was used to hydrate the branch.
                         */
                        date?: string;
                        /**
                         * @description DrySha is the SHA of the commit that was used as the dry source for hydration.
                         *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                         */
                        drySha?: string;
                        /** @description References are the references to other commits, that went into the hydration of the branch. */
                        references?: {
                            /** @description Commit contains metadata about the commit that is related in some way to another commit. */
                            commit?: {
                                /** @description Author is the author of the commit. */
                                author?: string;
                                /** @description Body is the body of the commit message, excluding the subject line, i.e. `git show --format=%b`. */
                                body?: string;
                                /**
                                 * Format: date-time
                                 * @description Date is the date of the commit, formatted as by `git show -s --format=%aI`.
                                 */
                                date?: string;
                                /** @description RepoURL is the URL of the repository where the commit is located. */
                                repoURL?: string;
                                /**
                                 * @description Sha is the commit hash.
                                 *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                                 */
                                sha?: string;
                                /** @description Subject is the subject line of the commit message, i.e. `git show --format=%s`. */
                                subject?: string;
                            };
                        }[];
                        /** @description RepoURL is the URL of the repository where the commit is located. */
                        repoURL?: string;
                        /** @description Subject is the subject line of the dry commit that was used to hydrate the branch. */
                        subject?: string;
                    };
                };
                /** @description PullRequest is the state of the pull request that was created for this ChangeTransferPolicy. */
                pullRequest?: {
                    /**
                     * @description ExternallyMergedOrClosed indicates that the pull request is no longer open on the SCM while the
                     *     PullRequest still desired it open: merged or closed outside the controller, or closed on the SCM
                     *     because the PullRequest resource was deleted (finalizer) before this status was reconciled.
                     *     When true, the State field will be empty ("") since we cannot tell merge vs. close from the provider.
                     *     This status is preserved even after the PullRequest resource is deleted, maintaining a historical
                     *     record until a new pull request is created for this environment.
                     */
                    externallyMergedOrClosed?: boolean;
                    /** @description ID is the unique identifier of the pull request, set by the SCM. */
                    id?: string;
                    /**
                     * Format: date-time
                     * @description PRCreationTime is the time when the pull request was created.
                     */
                    prCreationTime?: string;
                    /**
                     * Format: date-time
                     * @description PRMergeTime is the time when the pull request was merged. This time can vary slightly from the actual merge time because
                     *     it is the time when the ChangeTransferPolicy controller sets the pull requests spec to merge. In the future we plan on making
                     *     this time more accurate by fetching the actual merge time from the SCM via the webhook this would then be updated in the git note
                     *     for that commit.
                     */
                    prMergeTime?: string;
                    /**
                     * @description State is the state of the pull request.
                     * @enum {string}
                     */
                    state?: "closed" | "merged" | "open";
                    /** @description Url is the URL of the pull request. */
                    url?: string;
                };
            };
        };
        /** @description ClusterScmProvider is the Schema for the clusterscmproviders API. */
        ClusterScmProvider: {
            /**
             * @description APIVersion defines the versioned schema of this representation of an object.
             *     Servers should convert recognized schemas to the latest internal value, and
             *     may reject unrecognized values.
             *     More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: string;
            /**
             * @description Kind is a string value representing the REST resource this object represents.
             *     Servers may infer this from the endpoint the client submits requests to.
             *     Cannot be updated.
             *     In CamelCase.
             *     More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: string;
            metadata: Record<string, never>;
            /** @description ScmProviderSpec defines the desired state of ScmProvider */
            spec?: {
                /** @description AzureDevOps required configuration for Azure DevOps as the SCM provider */
                azureDevOps?: {
                    /** @description Domain is the Azure DevOps domain, such as "dev.azure.com". If using the default Azure DevOps domain, leave this field empty. */
                    domain?: string;
                    /** @description Organization is the Azure DevOps organization name. */
                    organization: string;
                };
                /** @description BitbucketCloud required configuration for Bitbucket Cloud as the SCM provider */
                bitbucketCloud?: Record<string, never>;
                /** @description Fake required configuration for Fake as the SCM provider */
                fake?: {
                    /** @description Domain is the domain of the fake SCM provider. This is used for testing purposes. */
                    domain?: string;
                };
                /** @description Forgejo required configuration for Forgejo as the SCM provider */
                forgejo?: {
                    /**
                     * @description Domain is the Forgejo domain, such as "codeberg.org" or "forgejo.mycompany.com".
                     *     There is no default domain since Forgejo is not a service like Gitlab or Github.
                     */
                    domain: string;
                };
                /** @description Gitea required configuration for Gitea as the SCM provider */
                gitea?: {
                    /**
                     * @description Domain is the Gitea domain, such as "gitea.com" or "gitea.mycompany.com".
                     *     There is no default domain since Gitea is self-hosted.
                     */
                    domain: string;
                };
                /** @description GitHub required configuration for GitHub as the SCM provider */
                github?: {
                    /**
                     * Format: int64
                     * @description AppID is the GitHub App ID.
                     */
                    appID: number;
                    /**
                     * @description Domain is the GitHub domain, such as "github.mycompany.com". If using the default GitHub domain, leave this field
                     *     empty.
                     */
                    domain?: string;
                    /**
                     * Format: int64
                     * @description InstallationID is the GitHub App Installation ID. If you want to use this ScmProvider for multiple
                     *     GitHub orgs, do not specify this field. The installation ID will be inferred from the repo owner
                     *     when needed.
                     */
                    installationID?: number;
                };
                /** @description GitLab required configuration for GitLab as the SCM provider */
                gitlab?: {
                    /**
                     * @description Domain is the GitLab domain, such as "gitlab.mycompany.com". If using the default GitLab domain, leave this field
                     *     empty.
                     */
                    domain?: string;
                };
                /** @description SecretRef contains the credentials required to auth to a specific provider */
                secretRef?: {
                    /**
                     * @description Name of the referent.
                     *     This field is effectively required, but due to backwards compatibility is
                     *     allowed to be empty. Instances of this type with an empty value here are
                     *     almost certainly wrong.
                     *     More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                     * @default
                     */
                    name?: string;
                };
            };
            /** @description ScmProviderStatus defines the observed state of ScmProvider */
            status?: {
                /** @description Conditions Represents the observations of the current state. */
                conditions?: {
                    /**
                     * Format: date-time
                     * @description lastTransitionTime is the last time the condition transitioned from one status to another.
                     *     This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                     */
                    lastTransitionTime: string;
                    /**
                     * @description message is a human readable message indicating details about the transition.
                     *     This may be an empty string.
                     */
                    message: string;
                    /**
                     * Format: int64
                     * @description observedGeneration represents the .metadata.generation that the condition was set based upon.
                     *     For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                     *     with respect to the current state of the instance.
                     */
                    observedGeneration?: number;
                    /**
                     * @description reason contains a programmatic identifier indicating the reason for the condition's last transition.
                     *     Producers of specific condition types may define expected values and meanings for this field,
                     *     and whether the values are considered a guaranteed API.
                     *     The value should be a CamelCase string.
                     *     This field may not be empty.
                     */
                    reason: string;
                    /**
                     * @description status of the condition, one of True, False, Unknown.
                     * @enum {string}
                     */
                    status: "True" | "False" | "Unknown";
                    /** @description type of condition in CamelCase or in foo.example.com/CamelCase. */
                    type: string;
                }[];
                /**
                 * Format: int64
                 * @description ObservedGeneration is the .metadata.generation that this status was reconciled from.
                 *     Because status is written via Server-Side Apply with ForceOwnership (which has no
                 *     optimistic-concurrency check), this field is the canonical way to detect stale
                 *     status writes: compare status.observedGeneration with metadata.generation.
                 */
                observedGeneration?: number;
            };
        };
        /** @description CommitStatus is the Schema for the commitstatuses API */
        CommitStatus: {
            /**
             * @description APIVersion defines the versioned schema of this representation of an object.
             *     Servers should convert recognized schemas to the latest internal value, and
             *     may reject unrecognized values.
             *     More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: string;
            /**
             * @description Kind is a string value representing the REST resource this object represents.
             *     Servers may infer this from the endpoint the client submits requests to.
             *     Cannot be updated.
             *     In CamelCase.
             *     More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: string;
            metadata: Record<string, never>;
            /** @description CommitStatusSpec defines the desired state of CommitStatus */
            spec?: {
                /**
                 * @description Description is a human-readable description of the commit status.
                 *     This is shown in the SCM provider (GitHub, GitLab, etc.) as the commit status description.
                 *     Use an action-oriented message to convey that the system is actively working. For example,
                 *     "Waiting for approval" instead of "Approval pending."
                 */
                description: string;
                /** @description ObjectReference is a reference to an object by name. It is used to refer to objects in the same namespace. */
                gitRepositoryRef: {
                    /** @description Name is the name of the object to refer to. */
                    name: string;
                };
                /** @description Name is the name of the commit status. */
                name: string;
                /**
                 * @description Phase is the state of the commit status. This will be mapped to the appropriate equivalent in the SCM.
                 * @default pending
                 * @enum {string}
                 */
                phase: "pending" | "success" | "failure";
                /**
                 * @description SHA is the commit SHA to set the status on.
                 *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                 */
                sha: string;
                /** @description Url is a URL that the user can follow to see more details about the status */
                url?: string;
            };
            /** @description CommitStatusStatus defines the observed state of CommitStatus */
            status?: {
                /** @description Conditions Represents the observations of the current state. */
                conditions?: {
                    /**
                     * Format: date-time
                     * @description lastTransitionTime is the last time the condition transitioned from one status to another.
                     *     This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                     */
                    lastTransitionTime: string;
                    /**
                     * @description message is a human readable message indicating details about the transition.
                     *     This may be an empty string.
                     */
                    message: string;
                    /**
                     * Format: int64
                     * @description observedGeneration represents the .metadata.generation that the condition was set based upon.
                     *     For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                     *     with respect to the current state of the instance.
                     */
                    observedGeneration?: number;
                    /**
                     * @description reason contains a programmatic identifier indicating the reason for the condition's last transition.
                     *     Producers of specific condition types may define expected values and meanings for this field,
                     *     and whether the values are considered a guaranteed API.
                     *     The value should be a CamelCase string.
                     *     This field may not be empty.
                     */
                    reason: string;
                    /**
                     * @description status of the condition, one of True, False, Unknown.
                     * @enum {string}
                     */
                    status: "True" | "False" | "Unknown";
                    /** @description type of condition in CamelCase or in foo.example.com/CamelCase. */
                    type: string;
                }[];
                /** @description Id is the unique identifier of the commit status, set by the SCM */
                id?: string;
                /**
                 * Format: int64
                 * @description ObservedGeneration is the .metadata.generation that this status was reconciled from.
                 *     Because status is written via Server-Side Apply with ForceOwnership (which has no
                 *     optimistic-concurrency check), this field is the canonical way to detect stale
                 *     status writes: compare status.observedGeneration with metadata.generation.
                 */
                observedGeneration?: number;
                /**
                 * @description Phase is the state of the commit status. This will be mapped to the appropriate equivalent in the SCM.
                 * @default pending
                 * @enum {string}
                 */
                phase?: "pending" | "success" | "failure" | "";
                /**
                 * @description Sha is the commit SHA that the status is set on.
                 *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                 */
                sha?: string;
            };
        };
        /** @description ControllerConfiguration is the Schema for the controllerconfigurations API. */
        ControllerConfiguration: {
            /**
             * @description APIVersion defines the versioned schema of this representation of an object.
             *     Servers should convert recognized schemas to the latest internal value, and
             *     may reject unrecognized values.
             *     More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: string;
            /**
             * @description Kind is a string value representing the REST resource this object represents.
             *     Servers may infer this from the endpoint the client submits requests to.
             *     Cannot be updated.
             *     In CamelCase.
             *     More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: string;
            metadata: Record<string, never>;
            /**
             * @description ControllerConfigurationSpec defines the desired state of ControllerConfiguration.
             *
             *     This spec contains the global configuration for all controllers in the promoter system.
             *     Each controller has its own configuration section that specifies WorkQueue settings,
             *     rate limiters, and other controller-specific parameters. All fields should be required,
             *     with defaults set in manifests rather than in code.
             */
            spec?: {
                /**
                 * @description ArgoCDCommitStatus contains the configuration for the ArgoCDCommitStatus controller,
                 *     including WorkQueue settings that control reconciliation behavior.
                 */
                argocdCommitStatus: {
                    /**
                     * @description WatchLocalApplications controls whether the controller monitors Argo CD Applications
                     *     in the local cluster. When false, the controller will only watch Applications in remote clusters
                     *     configured via kubeconfig secrets. This is useful when the Argo CD Application CRD is not installed
                     *     in the local cluster or when all Applications are deployed to remote clusters.
                     * @default true
                     */
                    watchLocalApplications: boolean;
                    /**
                     * @description WorkQueue contains the work queue configuration for the ArgoCDCommitStatus controller.
                     *     This includes requeue duration, maximum concurrent reconciles, and rate limiter settings.
                     */
                    workQueue: {
                        /**
                         * @description MaxConcurrentReconciles defines the maximum number of concurrent reconcile operations
                         *     that can run for this controller. Higher values increase throughput but consume more
                         *     resources.
                         */
                        maxConcurrentReconciles: number;
                        /**
                         * @description RateLimiter defines the rate limiting strategy for the controller's work queue.
                         *     Rate limiting controls how quickly failed reconciliations are retried and helps
                         *     prevent overwhelming external APIs or systems.
                         */
                        rateLimiter: {
                            /**
                             * @description Bucket rate limiter uses a token bucket algorithm to control request rate.
                             *     Allows bursts while maintaining an average rate limit.
                             */
                            bucket?: {
                                /**
                                 * @description Bucket is the maximum number of tokens that can be accumulated in the bucket.
                                 *     This defines the maximum burst size - how many operations can occur in rapid
                                 *     succession before rate limiting takes effect. Must be non-negative.
                                 */
                                bucket: number;
                                /**
                                 * @description Qps (queries per second) is the rate at which tokens are added to the bucket.
                                 *     This defines the sustained rate limit for operations. Must be non-negative.
                                 */
                                qps: number;
                            };
                            /**
                             * @description ExponentialFailure rate limiter increases delay exponentially with each failure.
                             *     Standard approach for backing off when operations fail repeatedly.
                             */
                            exponentialFailure?: {
                                /**
                                 * @description BaseDelay is the initial delay after the first failure. Subsequent failures will exponentially
                                 *     increase this delay (2x, 4x, 8x, etc.) until MaxDelay is reached.
                                 *     Format follows Go's time.Duration syntax (e.g., "1s" for 1 second).
                                 */
                                baseDelay: string;
                                /**
                                 * @description MaxDelay is the maximum delay between retry attempts. Once the exponential backoff reaches
                                 *     this value, all subsequent retries will use this delay.
                                 *     Format follows Go's time.Duration syntax (e.g., "1m" for 1 minute).
                                 */
                                maxDelay: string;
                            };
                            /**
                             * @description FastSlow rate limiter provides fast retries initially, then switches to slow retries.
                             *     Useful for quickly retrying transient errors while backing off for persistent failures.
                             */
                            fastSlow?: {
                                /**
                                 * @description FastDelay is the delay used for the first MaxFastAttempts retry attempts.
                                 *     Format follows Go's time.Duration syntax (e.g., "100ms" for 100 milliseconds).
                                 */
                                fastDelay: string;
                                /**
                                 * @description MaxFastAttempts is the number of retry attempts that use FastDelay before switching to SlowDelay.
                                 *     Must be at least 1.
                                 */
                                maxFastAttempts: number;
                                /**
                                 * @description SlowDelay is the delay used for retry attempts after MaxFastAttempts have been exhausted.
                                 *     Format follows Go's time.Duration syntax (e.g., "10s" for 10 seconds).
                                 */
                                slowDelay: string;
                            };
                            /**
                             * @description MaxOf allows combining multiple rate limiters, where the maximum delay from all
                             *     limiters is used. This enables sophisticated rate limiting that respects multiple
                             *     constraints simultaneously (e.g., both per-item exponential backoff and global rate limits).
                             */
                            maxOf?: {
                                /**
                                 * @description Bucket rate limiter uses a token bucket algorithm to control request rate.
                                 *     Allows bursts while maintaining an average rate limit.
                                 */
                                bucket?: {
                                    /**
                                     * @description Bucket is the maximum number of tokens that can be accumulated in the bucket.
                                     *     This defines the maximum burst size - how many operations can occur in rapid
                                     *     succession before rate limiting takes effect. Must be non-negative.
                                     */
                                    bucket: number;
                                    /**
                                     * @description Qps (queries per second) is the rate at which tokens are added to the bucket.
                                     *     This defines the sustained rate limit for operations. Must be non-negative.
                                     */
                                    qps: number;
                                };
                                /**
                                 * @description ExponentialFailure rate limiter increases delay exponentially with each failure.
                                 *     Standard approach for backing off when operations fail repeatedly.
                                 */
                                exponentialFailure?: {
                                    /**
                                     * @description BaseDelay is the initial delay after the first failure. Subsequent failures will exponentially
                                     *     increase this delay (2x, 4x, 8x, etc.) until MaxDelay is reached.
                                     *     Format follows Go's time.Duration syntax (e.g., "1s" for 1 second).
                                     */
                                    baseDelay: string;
                                    /**
                                     * @description MaxDelay is the maximum delay between retry attempts. Once the exponential backoff reaches
                                     *     this value, all subsequent retries will use this delay.
                                     *     Format follows Go's time.Duration syntax (e.g., "1m" for 1 minute).
                                     */
                                    maxDelay: string;
                                };
                                /**
                                 * @description FastSlow rate limiter provides fast retries initially, then switches to slow retries.
                                 *     Useful for quickly retrying transient errors while backing off for persistent failures.
                                 */
                                fastSlow?: {
                                    /**
                                     * @description FastDelay is the delay used for the first MaxFastAttempts retry attempts.
                                     *     Format follows Go's time.Duration syntax (e.g., "100ms" for 100 milliseconds).
                                     */
                                    fastDelay: string;
                                    /**
                                     * @description MaxFastAttempts is the number of retry attempts that use FastDelay before switching to SlowDelay.
                                     *     Must be at least 1.
                                     */
                                    maxFastAttempts: number;
                                    /**
                                     * @description SlowDelay is the delay used for retry attempts after MaxFastAttempts have been exhausted.
                                     *     Format follows Go's time.Duration syntax (e.g., "10s" for 10 seconds).
                                     */
                                    slowDelay: string;
                                };
                            }[];
                        };
                        /**
                         * @description RequeueDuration specifies how frequently resources should be requeued for automatic reconciliation.
                         *     This creates a periodic reconciliation loop that ensures the desired state is maintained even
                         *     without external triggers. Format follows Go's time.Duration syntax (e.g., "5m" for 5 minutes).
                         */
                        requeueDuration: string;
                    };
                };
                /**
                 * @description ChangeTransferPolicy contains the configuration for the ChangeTransferPolicy controller,
                 *     including WorkQueue settings that control reconciliation behavior.
                 */
                changeTransferPolicy: {
                    /**
                     * @description WorkQueue contains the work queue configuration for the ChangeTransferPolicy controller.
                     *     This includes requeue duration, maximum concurrent reconciles, and rate limiter settings.
                     */
                    workQueue: {
                        /**
                         * @description MaxConcurrentReconciles defines the maximum number of concurrent reconcile operations
                         *     that can run for this controller. Higher values increase throughput but consume more
                         *     resources.
                         */
                        maxConcurrentReconciles: number;
                        /**
                         * @description RateLimiter defines the rate limiting strategy for the controller's work queue.
                         *     Rate limiting controls how quickly failed reconciliations are retried and helps
                         *     prevent overwhelming external APIs or systems.
                         */
                        rateLimiter: {
                            /**
                             * @description Bucket rate limiter uses a token bucket algorithm to control request rate.
                             *     Allows bursts while maintaining an average rate limit.
                             */
                            bucket?: {
                                /**
                                 * @description Bucket is the maximum number of tokens that can be accumulated in the bucket.
                                 *     This defines the maximum burst size - how many operations can occur in rapid
                                 *     succession before rate limiting takes effect. Must be non-negative.
                                 */
                                bucket: number;
                                /**
                                 * @description Qps (queries per second) is the rate at which tokens are added to the bucket.
                                 *     This defines the sustained rate limit for operations. Must be non-negative.
                                 */
                                qps: number;
                            };
                            /**
                             * @description ExponentialFailure rate limiter increases delay exponentially with each failure.
                             *     Standard approach for backing off when operations fail repeatedly.
                             */
                            exponentialFailure?: {
                                /**
                                 * @description BaseDelay is the initial delay after the first failure. Subsequent failures will exponentially
                                 *     increase this delay (2x, 4x, 8x, etc.) until MaxDelay is reached.
                                 *     Format follows Go's time.Duration syntax (e.g., "1s" for 1 second).
                                 */
                                baseDelay: string;
                                /**
                                 * @description MaxDelay is the maximum delay between retry attempts. Once the exponential backoff reaches
                                 *     this value, all subsequent retries will use this delay.
                                 *     Format follows Go's time.Duration syntax (e.g., "1m" for 1 minute).
                                 */
                                maxDelay: string;
                            };
                            /**
                             * @description FastSlow rate limiter provides fast retries initially, then switches to slow retries.
                             *     Useful for quickly retrying transient errors while backing off for persistent failures.
                             */
                            fastSlow?: {
                                /**
                                 * @description FastDelay is the delay used for the first MaxFastAttempts retry attempts.
                                 *     Format follows Go's time.Duration syntax (e.g., "100ms" for 100 milliseconds).
                                 */
                                fastDelay: string;
                                /**
                                 * @description MaxFastAttempts is the number of retry attempts that use FastDelay before switching to SlowDelay.
                                 *     Must be at least 1.
                                 */
                                maxFastAttempts: number;
                                /**
                                 * @description SlowDelay is the delay used for retry attempts after MaxFastAttempts have been exhausted.
                                 *     Format follows Go's time.Duration syntax (e.g., "10s" for 10 seconds).
                                 */
                                slowDelay: string;
                            };
                            /**
                             * @description MaxOf allows combining multiple rate limiters, where the maximum delay from all
                             *     limiters is used. This enables sophisticated rate limiting that respects multiple
                             *     constraints simultaneously (e.g., both per-item exponential backoff and global rate limits).
                             */
                            maxOf?: {
                                /**
                                 * @description Bucket rate limiter uses a token bucket algorithm to control request rate.
                                 *     Allows bursts while maintaining an average rate limit.
                                 */
                                bucket?: {
                                    /**
                                     * @description Bucket is the maximum number of tokens that can be accumulated in the bucket.
                                     *     This defines the maximum burst size - how many operations can occur in rapid
                                     *     succession before rate limiting takes effect. Must be non-negative.
                                     */
                                    bucket: number;
                                    /**
                                     * @description Qps (queries per second) is the rate at which tokens are added to the bucket.
                                     *     This defines the sustained rate limit for operations. Must be non-negative.
                                     */
                                    qps: number;
                                };
                                /**
                                 * @description ExponentialFailure rate limiter increases delay exponentially with each failure.
                                 *     Standard approach for backing off when operations fail repeatedly.
                                 */
                                exponentialFailure?: {
                                    /**
                                     * @description BaseDelay is the initial delay after the first failure. Subsequent failures will exponentially
                                     *     increase this delay (2x, 4x, 8x, etc.) until MaxDelay is reached.
                                     *     Format follows Go's time.Duration syntax (e.g., "1s" for 1 second).
                                     */
                                    baseDelay: string;
                                    /**
                                     * @description MaxDelay is the maximum delay between retry attempts. Once the exponential backoff reaches
                                     *     this value, all subsequent retries will use this delay.
                                     *     Format follows Go's time.Duration syntax (e.g., "1m" for 1 minute).
                                     */
                                    maxDelay: string;
                                };
                                /**
                                 * @description FastSlow rate limiter provides fast retries initially, then switches to slow retries.
                                 *     Useful for quickly retrying transient errors while backing off for persistent failures.
                                 */
                                fastSlow?: {
                                    /**
                                     * @description FastDelay is the delay used for the first MaxFastAttempts retry attempts.
                                     *     Format follows Go's time.Duration syntax (e.g., "100ms" for 100 milliseconds).
                                     */
                                    fastDelay: string;
                                    /**
                                     * @description MaxFastAttempts is the number of retry attempts that use FastDelay before switching to SlowDelay.
                                     *     Must be at least 1.
                                     */
                                    maxFastAttempts: number;
                                    /**
                                     * @description SlowDelay is the delay used for retry attempts after MaxFastAttempts have been exhausted.
                                     *     Format follows Go's time.Duration syntax (e.g., "10s" for 10 seconds).
                                     */
                                    slowDelay: string;
                                };
                            }[];
                        };
                        /**
                         * @description RequeueDuration specifies how frequently resources should be requeued for automatic reconciliation.
                         *     This creates a periodic reconciliation loop that ensures the desired state is maintained even
                         *     without external triggers. Format follows Go's time.Duration syntax (e.g., "5m" for 5 minutes).
                         */
                        requeueDuration: string;
                    };
                };
                /**
                 * @description CommitStatus contains the configuration for the CommitStatus controller,
                 *     including WorkQueue settings that control reconciliation behavior.
                 */
                commitStatus: {
                    /**
                     * @description WorkQueue contains the work queue configuration for the CommitStatus controller.
                     *     This includes requeue duration, maximum concurrent reconciles, and rate limiter settings.
                     */
                    workQueue: {
                        /**
                         * @description MaxConcurrentReconciles defines the maximum number of concurrent reconcile operations
                         *     that can run for this controller. Higher values increase throughput but consume more
                         *     resources.
                         */
                        maxConcurrentReconciles: number;
                        /**
                         * @description RateLimiter defines the rate limiting strategy for the controller's work queue.
                         *     Rate limiting controls how quickly failed reconciliations are retried and helps
                         *     prevent overwhelming external APIs or systems.
                         */
                        rateLimiter: {
                            /**
                             * @description Bucket rate limiter uses a token bucket algorithm to control request rate.
                             *     Allows bursts while maintaining an average rate limit.
                             */
                            bucket?: {
                                /**
                                 * @description Bucket is the maximum number of tokens that can be accumulated in the bucket.
                                 *     This defines the maximum burst size - how many operations can occur in rapid
                                 *     succession before rate limiting takes effect. Must be non-negative.
                                 */
                                bucket: number;
                                /**
                                 * @description Qps (queries per second) is the rate at which tokens are added to the bucket.
                                 *     This defines the sustained rate limit for operations. Must be non-negative.
                                 */
                                qps: number;
                            };
                            /**
                             * @description ExponentialFailure rate limiter increases delay exponentially with each failure.
                             *     Standard approach for backing off when operations fail repeatedly.
                             */
                            exponentialFailure?: {
                                /**
                                 * @description BaseDelay is the initial delay after the first failure. Subsequent failures will exponentially
                                 *     increase this delay (2x, 4x, 8x, etc.) until MaxDelay is reached.
                                 *     Format follows Go's time.Duration syntax (e.g., "1s" for 1 second).
                                 */
                                baseDelay: string;
                                /**
                                 * @description MaxDelay is the maximum delay between retry attempts. Once the exponential backoff reaches
                                 *     this value, all subsequent retries will use this delay.
                                 *     Format follows Go's time.Duration syntax (e.g., "1m" for 1 minute).
                                 */
                                maxDelay: string;
                            };
                            /**
                             * @description FastSlow rate limiter provides fast retries initially, then switches to slow retries.
                             *     Useful for quickly retrying transient errors while backing off for persistent failures.
                             */
                            fastSlow?: {
                                /**
                                 * @description FastDelay is the delay used for the first MaxFastAttempts retry attempts.
                                 *     Format follows Go's time.Duration syntax (e.g., "100ms" for 100 milliseconds).
                                 */
                                fastDelay: string;
                                /**
                                 * @description MaxFastAttempts is the number of retry attempts that use FastDelay before switching to SlowDelay.
                                 *     Must be at least 1.
                                 */
                                maxFastAttempts: number;
                                /**
                                 * @description SlowDelay is the delay used for retry attempts after MaxFastAttempts have been exhausted.
                                 *     Format follows Go's time.Duration syntax (e.g., "10s" for 10 seconds).
                                 */
                                slowDelay: string;
                            };
                            /**
                             * @description MaxOf allows combining multiple rate limiters, where the maximum delay from all
                             *     limiters is used. This enables sophisticated rate limiting that respects multiple
                             *     constraints simultaneously (e.g., both per-item exponential backoff and global rate limits).
                             */
                            maxOf?: {
                                /**
                                 * @description Bucket rate limiter uses a token bucket algorithm to control request rate.
                                 *     Allows bursts while maintaining an average rate limit.
                                 */
                                bucket?: {
                                    /**
                                     * @description Bucket is the maximum number of tokens that can be accumulated in the bucket.
                                     *     This defines the maximum burst size - how many operations can occur in rapid
                                     *     succession before rate limiting takes effect. Must be non-negative.
                                     */
                                    bucket: number;
                                    /**
                                     * @description Qps (queries per second) is the rate at which tokens are added to the bucket.
                                     *     This defines the sustained rate limit for operations. Must be non-negative.
                                     */
                                    qps: number;
                                };
                                /**
                                 * @description ExponentialFailure rate limiter increases delay exponentially with each failure.
                                 *     Standard approach for backing off when operations fail repeatedly.
                                 */
                                exponentialFailure?: {
                                    /**
                                     * @description BaseDelay is the initial delay after the first failure. Subsequent failures will exponentially
                                     *     increase this delay (2x, 4x, 8x, etc.) until MaxDelay is reached.
                                     *     Format follows Go's time.Duration syntax (e.g., "1s" for 1 second).
                                     */
                                    baseDelay: string;
                                    /**
                                     * @description MaxDelay is the maximum delay between retry attempts. Once the exponential backoff reaches
                                     *     this value, all subsequent retries will use this delay.
                                     *     Format follows Go's time.Duration syntax (e.g., "1m" for 1 minute).
                                     */
                                    maxDelay: string;
                                };
                                /**
                                 * @description FastSlow rate limiter provides fast retries initially, then switches to slow retries.
                                 *     Useful for quickly retrying transient errors while backing off for persistent failures.
                                 */
                                fastSlow?: {
                                    /**
                                     * @description FastDelay is the delay used for the first MaxFastAttempts retry attempts.
                                     *     Format follows Go's time.Duration syntax (e.g., "100ms" for 100 milliseconds).
                                     */
                                    fastDelay: string;
                                    /**
                                     * @description MaxFastAttempts is the number of retry attempts that use FastDelay before switching to SlowDelay.
                                     *     Must be at least 1.
                                     */
                                    maxFastAttempts: number;
                                    /**
                                     * @description SlowDelay is the delay used for retry attempts after MaxFastAttempts have been exhausted.
                                     *     Format follows Go's time.Duration syntax (e.g., "10s" for 10 seconds).
                                     */
                                    slowDelay: string;
                                };
                            }[];
                        };
                        /**
                         * @description RequeueDuration specifies how frequently resources should be requeued for automatic reconciliation.
                         *     This creates a periodic reconciliation loop that ensures the desired state is maintained even
                         *     without external triggers. Format follows Go's time.Duration syntax (e.g., "5m" for 5 minutes).
                         */
                        requeueDuration: string;
                    };
                };
                /**
                 * @description GitCommitStatus contains the configuration for the GitCommitStatus controller,
                 *     including WorkQueue settings that control reconciliation behavior.
                 */
                gitCommitStatus: {
                    /**
                     * @description WorkQueue contains the work queue configuration for the GitCommitStatus controller.
                     *     This includes requeue duration, maximum concurrent reconciles, and rate limiter settings.
                     */
                    workQueue: {
                        /**
                         * @description MaxConcurrentReconciles defines the maximum number of concurrent reconcile operations
                         *     that can run for this controller. Higher values increase throughput but consume more
                         *     resources.
                         */
                        maxConcurrentReconciles: number;
                        /**
                         * @description RateLimiter defines the rate limiting strategy for the controller's work queue.
                         *     Rate limiting controls how quickly failed reconciliations are retried and helps
                         *     prevent overwhelming external APIs or systems.
                         */
                        rateLimiter: {
                            /**
                             * @description Bucket rate limiter uses a token bucket algorithm to control request rate.
                             *     Allows bursts while maintaining an average rate limit.
                             */
                            bucket?: {
                                /**
                                 * @description Bucket is the maximum number of tokens that can be accumulated in the bucket.
                                 *     This defines the maximum burst size - how many operations can occur in rapid
                                 *     succession before rate limiting takes effect. Must be non-negative.
                                 */
                                bucket: number;
                                /**
                                 * @description Qps (queries per second) is the rate at which tokens are added to the bucket.
                                 *     This defines the sustained rate limit for operations. Must be non-negative.
                                 */
                                qps: number;
                            };
                            /**
                             * @description ExponentialFailure rate limiter increases delay exponentially with each failure.
                             *     Standard approach for backing off when operations fail repeatedly.
                             */
                            exponentialFailure?: {
                                /**
                                 * @description BaseDelay is the initial delay after the first failure. Subsequent failures will exponentially
                                 *     increase this delay (2x, 4x, 8x, etc.) until MaxDelay is reached.
                                 *     Format follows Go's time.Duration syntax (e.g., "1s" for 1 second).
                                 */
                                baseDelay: string;
                                /**
                                 * @description MaxDelay is the maximum delay between retry attempts. Once the exponential backoff reaches
                                 *     this value, all subsequent retries will use this delay.
                                 *     Format follows Go's time.Duration syntax (e.g., "1m" for 1 minute).
                                 */
                                maxDelay: string;
                            };
                            /**
                             * @description FastSlow rate limiter provides fast retries initially, then switches to slow retries.
                             *     Useful for quickly retrying transient errors while backing off for persistent failures.
                             */
                            fastSlow?: {
                                /**
                                 * @description FastDelay is the delay used for the first MaxFastAttempts retry attempts.
                                 *     Format follows Go's time.Duration syntax (e.g., "100ms" for 100 milliseconds).
                                 */
                                fastDelay: string;
                                /**
                                 * @description MaxFastAttempts is the number of retry attempts that use FastDelay before switching to SlowDelay.
                                 *     Must be at least 1.
                                 */
                                maxFastAttempts: number;
                                /**
                                 * @description SlowDelay is the delay used for retry attempts after MaxFastAttempts have been exhausted.
                                 *     Format follows Go's time.Duration syntax (e.g., "10s" for 10 seconds).
                                 */
                                slowDelay: string;
                            };
                            /**
                             * @description MaxOf allows combining multiple rate limiters, where the maximum delay from all
                             *     limiters is used. This enables sophisticated rate limiting that respects multiple
                             *     constraints simultaneously (e.g., both per-item exponential backoff and global rate limits).
                             */
                            maxOf?: {
                                /**
                                 * @description Bucket rate limiter uses a token bucket algorithm to control request rate.
                                 *     Allows bursts while maintaining an average rate limit.
                                 */
                                bucket?: {
                                    /**
                                     * @description Bucket is the maximum number of tokens that can be accumulated in the bucket.
                                     *     This defines the maximum burst size - how many operations can occur in rapid
                                     *     succession before rate limiting takes effect. Must be non-negative.
                                     */
                                    bucket: number;
                                    /**
                                     * @description Qps (queries per second) is the rate at which tokens are added to the bucket.
                                     *     This defines the sustained rate limit for operations. Must be non-negative.
                                     */
                                    qps: number;
                                };
                                /**
                                 * @description ExponentialFailure rate limiter increases delay exponentially with each failure.
                                 *     Standard approach for backing off when operations fail repeatedly.
                                 */
                                exponentialFailure?: {
                                    /**
                                     * @description BaseDelay is the initial delay after the first failure. Subsequent failures will exponentially
                                     *     increase this delay (2x, 4x, 8x, etc.) until MaxDelay is reached.
                                     *     Format follows Go's time.Duration syntax (e.g., "1s" for 1 second).
                                     */
                                    baseDelay: string;
                                    /**
                                     * @description MaxDelay is the maximum delay between retry attempts. Once the exponential backoff reaches
                                     *     this value, all subsequent retries will use this delay.
                                     *     Format follows Go's time.Duration syntax (e.g., "1m" for 1 minute).
                                     */
                                    maxDelay: string;
                                };
                                /**
                                 * @description FastSlow rate limiter provides fast retries initially, then switches to slow retries.
                                 *     Useful for quickly retrying transient errors while backing off for persistent failures.
                                 */
                                fastSlow?: {
                                    /**
                                     * @description FastDelay is the delay used for the first MaxFastAttempts retry attempts.
                                     *     Format follows Go's time.Duration syntax (e.g., "100ms" for 100 milliseconds).
                                     */
                                    fastDelay: string;
                                    /**
                                     * @description MaxFastAttempts is the number of retry attempts that use FastDelay before switching to SlowDelay.
                                     *     Must be at least 1.
                                     */
                                    maxFastAttempts: number;
                                    /**
                                     * @description SlowDelay is the delay used for retry attempts after MaxFastAttempts have been exhausted.
                                     *     Format follows Go's time.Duration syntax (e.g., "10s" for 10 seconds).
                                     */
                                    slowDelay: string;
                                };
                            }[];
                        };
                        /**
                         * @description RequeueDuration specifies how frequently resources should be requeued for automatic reconciliation.
                         *     This creates a periodic reconciliation loop that ensures the desired state is maintained even
                         *     without external triggers. Format follows Go's time.Duration syntax (e.g., "5m" for 5 minutes).
                         */
                        requeueDuration: string;
                    };
                };
                /**
                 * @description PromotionStrategy contains the configuration for the PromotionStrategy controller,
                 *     including WorkQueue settings that control reconciliation behavior.
                 */
                promotionStrategy: {
                    /**
                     * @description WorkQueue contains the work queue configuration for the PromotionStrategy controller.
                     *     This includes requeue duration, maximum concurrent reconciles, and rate limiter settings.
                     */
                    workQueue: {
                        /**
                         * @description MaxConcurrentReconciles defines the maximum number of concurrent reconcile operations
                         *     that can run for this controller. Higher values increase throughput but consume more
                         *     resources.
                         */
                        maxConcurrentReconciles: number;
                        /**
                         * @description RateLimiter defines the rate limiting strategy for the controller's work queue.
                         *     Rate limiting controls how quickly failed reconciliations are retried and helps
                         *     prevent overwhelming external APIs or systems.
                         */
                        rateLimiter: {
                            /**
                             * @description Bucket rate limiter uses a token bucket algorithm to control request rate.
                             *     Allows bursts while maintaining an average rate limit.
                             */
                            bucket?: {
                                /**
                                 * @description Bucket is the maximum number of tokens that can be accumulated in the bucket.
                                 *     This defines the maximum burst size - how many operations can occur in rapid
                                 *     succession before rate limiting takes effect. Must be non-negative.
                                 */
                                bucket: number;
                                /**
                                 * @description Qps (queries per second) is the rate at which tokens are added to the bucket.
                                 *     This defines the sustained rate limit for operations. Must be non-negative.
                                 */
                                qps: number;
                            };
                            /**
                             * @description ExponentialFailure rate limiter increases delay exponentially with each failure.
                             *     Standard approach for backing off when operations fail repeatedly.
                             */
                            exponentialFailure?: {
                                /**
                                 * @description BaseDelay is the initial delay after the first failure. Subsequent failures will exponentially
                                 *     increase this delay (2x, 4x, 8x, etc.) until MaxDelay is reached.
                                 *     Format follows Go's time.Duration syntax (e.g., "1s" for 1 second).
                                 */
                                baseDelay: string;
                                /**
                                 * @description MaxDelay is the maximum delay between retry attempts. Once the exponential backoff reaches
                                 *     this value, all subsequent retries will use this delay.
                                 *     Format follows Go's time.Duration syntax (e.g., "1m" for 1 minute).
                                 */
                                maxDelay: string;
                            };
                            /**
                             * @description FastSlow rate limiter provides fast retries initially, then switches to slow retries.
                             *     Useful for quickly retrying transient errors while backing off for persistent failures.
                             */
                            fastSlow?: {
                                /**
                                 * @description FastDelay is the delay used for the first MaxFastAttempts retry attempts.
                                 *     Format follows Go's time.Duration syntax (e.g., "100ms" for 100 milliseconds).
                                 */
                                fastDelay: string;
                                /**
                                 * @description MaxFastAttempts is the number of retry attempts that use FastDelay before switching to SlowDelay.
                                 *     Must be at least 1.
                                 */
                                maxFastAttempts: number;
                                /**
                                 * @description SlowDelay is the delay used for retry attempts after MaxFastAttempts have been exhausted.
                                 *     Format follows Go's time.Duration syntax (e.g., "10s" for 10 seconds).
                                 */
                                slowDelay: string;
                            };
                            /**
                             * @description MaxOf allows combining multiple rate limiters, where the maximum delay from all
                             *     limiters is used. This enables sophisticated rate limiting that respects multiple
                             *     constraints simultaneously (e.g., both per-item exponential backoff and global rate limits).
                             */
                            maxOf?: {
                                /**
                                 * @description Bucket rate limiter uses a token bucket algorithm to control request rate.
                                 *     Allows bursts while maintaining an average rate limit.
                                 */
                                bucket?: {
                                    /**
                                     * @description Bucket is the maximum number of tokens that can be accumulated in the bucket.
                                     *     This defines the maximum burst size - how many operations can occur in rapid
                                     *     succession before rate limiting takes effect. Must be non-negative.
                                     */
                                    bucket: number;
                                    /**
                                     * @description Qps (queries per second) is the rate at which tokens are added to the bucket.
                                     *     This defines the sustained rate limit for operations. Must be non-negative.
                                     */
                                    qps: number;
                                };
                                /**
                                 * @description ExponentialFailure rate limiter increases delay exponentially with each failure.
                                 *     Standard approach for backing off when operations fail repeatedly.
                                 */
                                exponentialFailure?: {
                                    /**
                                     * @description BaseDelay is the initial delay after the first failure. Subsequent failures will exponentially
                                     *     increase this delay (2x, 4x, 8x, etc.) until MaxDelay is reached.
                                     *     Format follows Go's time.Duration syntax (e.g., "1s" for 1 second).
                                     */
                                    baseDelay: string;
                                    /**
                                     * @description MaxDelay is the maximum delay between retry attempts. Once the exponential backoff reaches
                                     *     this value, all subsequent retries will use this delay.
                                     *     Format follows Go's time.Duration syntax (e.g., "1m" for 1 minute).
                                     */
                                    maxDelay: string;
                                };
                                /**
                                 * @description FastSlow rate limiter provides fast retries initially, then switches to slow retries.
                                 *     Useful for quickly retrying transient errors while backing off for persistent failures.
                                 */
                                fastSlow?: {
                                    /**
                                     * @description FastDelay is the delay used for the first MaxFastAttempts retry attempts.
                                     *     Format follows Go's time.Duration syntax (e.g., "100ms" for 100 milliseconds).
                                     */
                                    fastDelay: string;
                                    /**
                                     * @description MaxFastAttempts is the number of retry attempts that use FastDelay before switching to SlowDelay.
                                     *     Must be at least 1.
                                     */
                                    maxFastAttempts: number;
                                    /**
                                     * @description SlowDelay is the delay used for retry attempts after MaxFastAttempts have been exhausted.
                                     *     Format follows Go's time.Duration syntax (e.g., "10s" for 10 seconds).
                                     */
                                    slowDelay: string;
                                };
                            }[];
                        };
                        /**
                         * @description RequeueDuration specifies how frequently resources should be requeued for automatic reconciliation.
                         *     This creates a periodic reconciliation loop that ensures the desired state is maintained even
                         *     without external triggers. Format follows Go's time.Duration syntax (e.g., "5m" for 5 minutes).
                         */
                        requeueDuration: string;
                    };
                };
                /**
                 * @description PullRequest contains the configuration for the PullRequest controller,
                 *     including WorkQueue settings and pull request template configuration.
                 */
                pullRequest: {
                    /**
                     * @description Template is the template configuration used to generate pull request titles and descriptions.
                     *     Uses Go template syntax with Sprig functions available.
                     */
                    template: {
                        /**
                         * @description Description is the template used to generate the body/description of the pull request.
                         *     Uses Go template syntax with Sprig functions available for string manipulation.
                         */
                        description: string;
                        /**
                         * @description Title is the template used to generate the title of the pull request.
                         *     Uses Go template syntax with Sprig functions available for string manipulation.
                         */
                        title: string;
                    };
                    /**
                     * @description WorkQueue contains the work queue configuration for the PullRequest controller.
                     *     This includes requeue duration, maximum concurrent reconciles, and rate limiter settings.
                     */
                    workQueue: {
                        /**
                         * @description MaxConcurrentReconciles defines the maximum number of concurrent reconcile operations
                         *     that can run for this controller. Higher values increase throughput but consume more
                         *     resources.
                         */
                        maxConcurrentReconciles: number;
                        /**
                         * @description RateLimiter defines the rate limiting strategy for the controller's work queue.
                         *     Rate limiting controls how quickly failed reconciliations are retried and helps
                         *     prevent overwhelming external APIs or systems.
                         */
                        rateLimiter: {
                            /**
                             * @description Bucket rate limiter uses a token bucket algorithm to control request rate.
                             *     Allows bursts while maintaining an average rate limit.
                             */
                            bucket?: {
                                /**
                                 * @description Bucket is the maximum number of tokens that can be accumulated in the bucket.
                                 *     This defines the maximum burst size - how many operations can occur in rapid
                                 *     succession before rate limiting takes effect. Must be non-negative.
                                 */
                                bucket: number;
                                /**
                                 * @description Qps (queries per second) is the rate at which tokens are added to the bucket.
                                 *     This defines the sustained rate limit for operations. Must be non-negative.
                                 */
                                qps: number;
                            };
                            /**
                             * @description ExponentialFailure rate limiter increases delay exponentially with each failure.
                             *     Standard approach for backing off when operations fail repeatedly.
                             */
                            exponentialFailure?: {
                                /**
                                 * @description BaseDelay is the initial delay after the first failure. Subsequent failures will exponentially
                                 *     increase this delay (2x, 4x, 8x, etc.) until MaxDelay is reached.
                                 *     Format follows Go's time.Duration syntax (e.g., "1s" for 1 second).
                                 */
                                baseDelay: string;
                                /**
                                 * @description MaxDelay is the maximum delay between retry attempts. Once the exponential backoff reaches
                                 *     this value, all subsequent retries will use this delay.
                                 *     Format follows Go's time.Duration syntax (e.g., "1m" for 1 minute).
                                 */
                                maxDelay: string;
                            };
                            /**
                             * @description FastSlow rate limiter provides fast retries initially, then switches to slow retries.
                             *     Useful for quickly retrying transient errors while backing off for persistent failures.
                             */
                            fastSlow?: {
                                /**
                                 * @description FastDelay is the delay used for the first MaxFastAttempts retry attempts.
                                 *     Format follows Go's time.Duration syntax (e.g., "100ms" for 100 milliseconds).
                                 */
                                fastDelay: string;
                                /**
                                 * @description MaxFastAttempts is the number of retry attempts that use FastDelay before switching to SlowDelay.
                                 *     Must be at least 1.
                                 */
                                maxFastAttempts: number;
                                /**
                                 * @description SlowDelay is the delay used for retry attempts after MaxFastAttempts have been exhausted.
                                 *     Format follows Go's time.Duration syntax (e.g., "10s" for 10 seconds).
                                 */
                                slowDelay: string;
                            };
                            /**
                             * @description MaxOf allows combining multiple rate limiters, where the maximum delay from all
                             *     limiters is used. This enables sophisticated rate limiting that respects multiple
                             *     constraints simultaneously (e.g., both per-item exponential backoff and global rate limits).
                             */
                            maxOf?: {
                                /**
                                 * @description Bucket rate limiter uses a token bucket algorithm to control request rate.
                                 *     Allows bursts while maintaining an average rate limit.
                                 */
                                bucket?: {
                                    /**
                                     * @description Bucket is the maximum number of tokens that can be accumulated in the bucket.
                                     *     This defines the maximum burst size - how many operations can occur in rapid
                                     *     succession before rate limiting takes effect. Must be non-negative.
                                     */
                                    bucket: number;
                                    /**
                                     * @description Qps (queries per second) is the rate at which tokens are added to the bucket.
                                     *     This defines the sustained rate limit for operations. Must be non-negative.
                                     */
                                    qps: number;
                                };
                                /**
                                 * @description ExponentialFailure rate limiter increases delay exponentially with each failure.
                                 *     Standard approach for backing off when operations fail repeatedly.
                                 */
                                exponentialFailure?: {
                                    /**
                                     * @description BaseDelay is the initial delay after the first failure. Subsequent failures will exponentially
                                     *     increase this delay (2x, 4x, 8x, etc.) until MaxDelay is reached.
                                     *     Format follows Go's time.Duration syntax (e.g., "1s" for 1 second).
                                     */
                                    baseDelay: string;
                                    /**
                                     * @description MaxDelay is the maximum delay between retry attempts. Once the exponential backoff reaches
                                     *     this value, all subsequent retries will use this delay.
                                     *     Format follows Go's time.Duration syntax (e.g., "1m" for 1 minute).
                                     */
                                    maxDelay: string;
                                };
                                /**
                                 * @description FastSlow rate limiter provides fast retries initially, then switches to slow retries.
                                 *     Useful for quickly retrying transient errors while backing off for persistent failures.
                                 */
                                fastSlow?: {
                                    /**
                                     * @description FastDelay is the delay used for the first MaxFastAttempts retry attempts.
                                     *     Format follows Go's time.Duration syntax (e.g., "100ms" for 100 milliseconds).
                                     */
                                    fastDelay: string;
                                    /**
                                     * @description MaxFastAttempts is the number of retry attempts that use FastDelay before switching to SlowDelay.
                                     *     Must be at least 1.
                                     */
                                    maxFastAttempts: number;
                                    /**
                                     * @description SlowDelay is the delay used for retry attempts after MaxFastAttempts have been exhausted.
                                     *     Format follows Go's time.Duration syntax (e.g., "10s" for 10 seconds).
                                     */
                                    slowDelay: string;
                                };
                            }[];
                        };
                        /**
                         * @description RequeueDuration specifies how frequently resources should be requeued for automatic reconciliation.
                         *     This creates a periodic reconciliation loop that ensures the desired state is maintained even
                         *     without external triggers. Format follows Go's time.Duration syntax (e.g., "5m" for 5 minutes).
                         */
                        requeueDuration: string;
                    };
                };
                /**
                 * @description TimedCommitStatus contains the configuration for the TimedCommitStatus controller,
                 *     including WorkQueue settings that control reconciliation behavior.
                 */
                timedCommitStatus: {
                    /**
                     * @description WorkQueue contains the work queue configuration for the TimedCommitStatus controller.
                     *     This includes requeue duration, maximum concurrent reconciles, and rate limiter settings.
                     */
                    workQueue: {
                        /**
                         * @description MaxConcurrentReconciles defines the maximum number of concurrent reconcile operations
                         *     that can run for this controller. Higher values increase throughput but consume more
                         *     resources.
                         */
                        maxConcurrentReconciles: number;
                        /**
                         * @description RateLimiter defines the rate limiting strategy for the controller's work queue.
                         *     Rate limiting controls how quickly failed reconciliations are retried and helps
                         *     prevent overwhelming external APIs or systems.
                         */
                        rateLimiter: {
                            /**
                             * @description Bucket rate limiter uses a token bucket algorithm to control request rate.
                             *     Allows bursts while maintaining an average rate limit.
                             */
                            bucket?: {
                                /**
                                 * @description Bucket is the maximum number of tokens that can be accumulated in the bucket.
                                 *     This defines the maximum burst size - how many operations can occur in rapid
                                 *     succession before rate limiting takes effect. Must be non-negative.
                                 */
                                bucket: number;
                                /**
                                 * @description Qps (queries per second) is the rate at which tokens are added to the bucket.
                                 *     This defines the sustained rate limit for operations. Must be non-negative.
                                 */
                                qps: number;
                            };
                            /**
                             * @description ExponentialFailure rate limiter increases delay exponentially with each failure.
                             *     Standard approach for backing off when operations fail repeatedly.
                             */
                            exponentialFailure?: {
                                /**
                                 * @description BaseDelay is the initial delay after the first failure. Subsequent failures will exponentially
                                 *     increase this delay (2x, 4x, 8x, etc.) until MaxDelay is reached.
                                 *     Format follows Go's time.Duration syntax (e.g., "1s" for 1 second).
                                 */
                                baseDelay: string;
                                /**
                                 * @description MaxDelay is the maximum delay between retry attempts. Once the exponential backoff reaches
                                 *     this value, all subsequent retries will use this delay.
                                 *     Format follows Go's time.Duration syntax (e.g., "1m" for 1 minute).
                                 */
                                maxDelay: string;
                            };
                            /**
                             * @description FastSlow rate limiter provides fast retries initially, then switches to slow retries.
                             *     Useful for quickly retrying transient errors while backing off for persistent failures.
                             */
                            fastSlow?: {
                                /**
                                 * @description FastDelay is the delay used for the first MaxFastAttempts retry attempts.
                                 *     Format follows Go's time.Duration syntax (e.g., "100ms" for 100 milliseconds).
                                 */
                                fastDelay: string;
                                /**
                                 * @description MaxFastAttempts is the number of retry attempts that use FastDelay before switching to SlowDelay.
                                 *     Must be at least 1.
                                 */
                                maxFastAttempts: number;
                                /**
                                 * @description SlowDelay is the delay used for retry attempts after MaxFastAttempts have been exhausted.
                                 *     Format follows Go's time.Duration syntax (e.g., "10s" for 10 seconds).
                                 */
                                slowDelay: string;
                            };
                            /**
                             * @description MaxOf allows combining multiple rate limiters, where the maximum delay from all
                             *     limiters is used. This enables sophisticated rate limiting that respects multiple
                             *     constraints simultaneously (e.g., both per-item exponential backoff and global rate limits).
                             */
                            maxOf?: {
                                /**
                                 * @description Bucket rate limiter uses a token bucket algorithm to control request rate.
                                 *     Allows bursts while maintaining an average rate limit.
                                 */
                                bucket?: {
                                    /**
                                     * @description Bucket is the maximum number of tokens that can be accumulated in the bucket.
                                     *     This defines the maximum burst size - how many operations can occur in rapid
                                     *     succession before rate limiting takes effect. Must be non-negative.
                                     */
                                    bucket: number;
                                    /**
                                     * @description Qps (queries per second) is the rate at which tokens are added to the bucket.
                                     *     This defines the sustained rate limit for operations. Must be non-negative.
                                     */
                                    qps: number;
                                };
                                /**
                                 * @description ExponentialFailure rate limiter increases delay exponentially with each failure.
                                 *     Standard approach for backing off when operations fail repeatedly.
                                 */
                                exponentialFailure?: {
                                    /**
                                     * @description BaseDelay is the initial delay after the first failure. Subsequent failures will exponentially
                                     *     increase this delay (2x, 4x, 8x, etc.) until MaxDelay is reached.
                                     *     Format follows Go's time.Duration syntax (e.g., "1s" for 1 second).
                                     */
                                    baseDelay: string;
                                    /**
                                     * @description MaxDelay is the maximum delay between retry attempts. Once the exponential backoff reaches
                                     *     this value, all subsequent retries will use this delay.
                                     *     Format follows Go's time.Duration syntax (e.g., "1m" for 1 minute).
                                     */
                                    maxDelay: string;
                                };
                                /**
                                 * @description FastSlow rate limiter provides fast retries initially, then switches to slow retries.
                                 *     Useful for quickly retrying transient errors while backing off for persistent failures.
                                 */
                                fastSlow?: {
                                    /**
                                     * @description FastDelay is the delay used for the first MaxFastAttempts retry attempts.
                                     *     Format follows Go's time.Duration syntax (e.g., "100ms" for 100 milliseconds).
                                     */
                                    fastDelay: string;
                                    /**
                                     * @description MaxFastAttempts is the number of retry attempts that use FastDelay before switching to SlowDelay.
                                     *     Must be at least 1.
                                     */
                                    maxFastAttempts: number;
                                    /**
                                     * @description SlowDelay is the delay used for retry attempts after MaxFastAttempts have been exhausted.
                                     *     Format follows Go's time.Duration syntax (e.g., "10s" for 10 seconds).
                                     */
                                    slowDelay: string;
                                };
                            }[];
                        };
                        /**
                         * @description RequeueDuration specifies how frequently resources should be requeued for automatic reconciliation.
                         *     This creates a periodic reconciliation loop that ensures the desired state is maintained even
                         *     without external triggers. Format follows Go's time.Duration syntax (e.g., "5m" for 5 minutes).
                         */
                        requeueDuration: string;
                    };
                };
                /**
                 * @description WebRequestCommitStatus contains the configuration for the WebRequestCommitStatus controller,
                 *     including WorkQueue settings that control reconciliation behavior.
                 */
                webRequestCommitStatus: {
                    /**
                     * @description WorkQueue contains the work queue configuration for the WebRequestCommitStatus controller.
                     *     This includes requeue duration, maximum concurrent reconciles, and rate limiter settings.
                     */
                    workQueue: {
                        /**
                         * @description MaxConcurrentReconciles defines the maximum number of concurrent reconcile operations
                         *     that can run for this controller. Higher values increase throughput but consume more
                         *     resources.
                         */
                        maxConcurrentReconciles: number;
                        /**
                         * @description RateLimiter defines the rate limiting strategy for the controller's work queue.
                         *     Rate limiting controls how quickly failed reconciliations are retried and helps
                         *     prevent overwhelming external APIs or systems.
                         */
                        rateLimiter: {
                            /**
                             * @description Bucket rate limiter uses a token bucket algorithm to control request rate.
                             *     Allows bursts while maintaining an average rate limit.
                             */
                            bucket?: {
                                /**
                                 * @description Bucket is the maximum number of tokens that can be accumulated in the bucket.
                                 *     This defines the maximum burst size - how many operations can occur in rapid
                                 *     succession before rate limiting takes effect. Must be non-negative.
                                 */
                                bucket: number;
                                /**
                                 * @description Qps (queries per second) is the rate at which tokens are added to the bucket.
                                 *     This defines the sustained rate limit for operations. Must be non-negative.
                                 */
                                qps: number;
                            };
                            /**
                             * @description ExponentialFailure rate limiter increases delay exponentially with each failure.
                             *     Standard approach for backing off when operations fail repeatedly.
                             */
                            exponentialFailure?: {
                                /**
                                 * @description BaseDelay is the initial delay after the first failure. Subsequent failures will exponentially
                                 *     increase this delay (2x, 4x, 8x, etc.) until MaxDelay is reached.
                                 *     Format follows Go's time.Duration syntax (e.g., "1s" for 1 second).
                                 */
                                baseDelay: string;
                                /**
                                 * @description MaxDelay is the maximum delay between retry attempts. Once the exponential backoff reaches
                                 *     this value, all subsequent retries will use this delay.
                                 *     Format follows Go's time.Duration syntax (e.g., "1m" for 1 minute).
                                 */
                                maxDelay: string;
                            };
                            /**
                             * @description FastSlow rate limiter provides fast retries initially, then switches to slow retries.
                             *     Useful for quickly retrying transient errors while backing off for persistent failures.
                             */
                            fastSlow?: {
                                /**
                                 * @description FastDelay is the delay used for the first MaxFastAttempts retry attempts.
                                 *     Format follows Go's time.Duration syntax (e.g., "100ms" for 100 milliseconds).
                                 */
                                fastDelay: string;
                                /**
                                 * @description MaxFastAttempts is the number of retry attempts that use FastDelay before switching to SlowDelay.
                                 *     Must be at least 1.
                                 */
                                maxFastAttempts: number;
                                /**
                                 * @description SlowDelay is the delay used for retry attempts after MaxFastAttempts have been exhausted.
                                 *     Format follows Go's time.Duration syntax (e.g., "10s" for 10 seconds).
                                 */
                                slowDelay: string;
                            };
                            /**
                             * @description MaxOf allows combining multiple rate limiters, where the maximum delay from all
                             *     limiters is used. This enables sophisticated rate limiting that respects multiple
                             *     constraints simultaneously (e.g., both per-item exponential backoff and global rate limits).
                             */
                            maxOf?: {
                                /**
                                 * @description Bucket rate limiter uses a token bucket algorithm to control request rate.
                                 *     Allows bursts while maintaining an average rate limit.
                                 */
                                bucket?: {
                                    /**
                                     * @description Bucket is the maximum number of tokens that can be accumulated in the bucket.
                                     *     This defines the maximum burst size - how many operations can occur in rapid
                                     *     succession before rate limiting takes effect. Must be non-negative.
                                     */
                                    bucket: number;
                                    /**
                                     * @description Qps (queries per second) is the rate at which tokens are added to the bucket.
                                     *     This defines the sustained rate limit for operations. Must be non-negative.
                                     */
                                    qps: number;
                                };
                                /**
                                 * @description ExponentialFailure rate limiter increases delay exponentially with each failure.
                                 *     Standard approach for backing off when operations fail repeatedly.
                                 */
                                exponentialFailure?: {
                                    /**
                                     * @description BaseDelay is the initial delay after the first failure. Subsequent failures will exponentially
                                     *     increase this delay (2x, 4x, 8x, etc.) until MaxDelay is reached.
                                     *     Format follows Go's time.Duration syntax (e.g., "1s" for 1 second).
                                     */
                                    baseDelay: string;
                                    /**
                                     * @description MaxDelay is the maximum delay between retry attempts. Once the exponential backoff reaches
                                     *     this value, all subsequent retries will use this delay.
                                     *     Format follows Go's time.Duration syntax (e.g., "1m" for 1 minute).
                                     */
                                    maxDelay: string;
                                };
                                /**
                                 * @description FastSlow rate limiter provides fast retries initially, then switches to slow retries.
                                 *     Useful for quickly retrying transient errors while backing off for persistent failures.
                                 */
                                fastSlow?: {
                                    /**
                                     * @description FastDelay is the delay used for the first MaxFastAttempts retry attempts.
                                     *     Format follows Go's time.Duration syntax (e.g., "100ms" for 100 milliseconds).
                                     */
                                    fastDelay: string;
                                    /**
                                     * @description MaxFastAttempts is the number of retry attempts that use FastDelay before switching to SlowDelay.
                                     *     Must be at least 1.
                                     */
                                    maxFastAttempts: number;
                                    /**
                                     * @description SlowDelay is the delay used for retry attempts after MaxFastAttempts have been exhausted.
                                     *     Format follows Go's time.Duration syntax (e.g., "10s" for 10 seconds).
                                     */
                                    slowDelay: string;
                                };
                            }[];
                        };
                        /**
                         * @description RequeueDuration specifies how frequently resources should be requeued for automatic reconciliation.
                         *     This creates a periodic reconciliation loop that ensures the desired state is maintained even
                         *     without external triggers. Format follows Go's time.Duration syntax (e.g., "5m" for 5 minutes).
                         */
                        requeueDuration: string;
                    };
                };
            };
            /**
             * @description ControllerConfigurationStatus defines the observed state of ControllerConfiguration.
             *
             *     Currently, this resource does not maintain any status information as it is a configuration-only
             *     resource. Status fields may be added in the future to track configuration validation or
             *     controller health metrics.
             */
            status?: Record<string, never>;
        };
        /**
         * @description GitCommitStatus is the Schema for the gitcommitstatuses API.
         *
         *     It validates commits from PromotionStrategy environments using configurable expressions
         *     and creates CommitStatus resources with the validation results.
         *
         *     Use the Target field to control which commit is validated:
         *     - "active" (default): Validates the currently deployed commit
         *     - "proposed": Validates the incoming commit
         *
         *     The validation result is always reported on the PROPOSED commit to enable promotion gating,
         *     regardless of which commit was validated.
         *
         *     Workflow:
         *      1. Controller reads PromotionStrategy to get ProposedHydratedSha and ActiveHydratedSha
         *      2. Controller selects SHA to validate based on Target field
         *      3. Controller fetches commit data (subject, body, author, trailers) for selected SHA
         *      4. Controller evaluates expression against selected commit data
         *      5. Controller creates/updates CommitStatus with result attached to PROPOSED SHA
         *      6. PromotionStrategy checks CommitStatus on PROPOSED SHA before allowing promotion
         *
         *     Common use cases:
         *       - "Ensure active commit is not a revert before promoting"
         */
        GitCommitStatus: {
            /**
             * @description APIVersion defines the versioned schema of this representation of an object.
             *     Servers should convert recognized schemas to the latest internal value, and
             *     may reject unrecognized values.
             *     More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: string;
            /**
             * @description Kind is a string value representing the REST resource this object represents.
             *     Servers may infer this from the endpoint the client submits requests to.
             *     Cannot be updated.
             *     In CamelCase.
             *     More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: string;
            metadata: Record<string, never>;
            /** @description spec defines the desired state of GitCommitStatus */
            spec: {
                /**
                 * @description Description is a human-readable description of this validation that will be shown in the SCM provider
                 *     (GitHub, GitLab, etc.) as the commit status description.
                 *     If not specified, defaults to empty string.
                 */
                description?: string;
                /**
                 * @description Expression is evaluated using the expr library (github.com/expr-lang/expr) against commit data
                 *     for environments in the referenced PromotionStrategy.
                 *     The expression must return a boolean value where true indicates the validation passed.
                 *
                 *     The commit validated is determined by the Target field:
                 *     - "active" (default): Validates the ACTIVE (currently deployed) commit
                 *     - "proposed": Validates the PROPOSED commit (what will be promoted)
                 *
                 *     The validation result is always reported on the PROPOSED commit to enable promotion gating.
                 *
                 *     Use Cases by Mode:
                 *       Active mode: State-based gating - validate current environment before allowing promotion
                 *         - "Don't promote until active commit has required sign-offs"
                 *         - "Verify current deployment meets compliance before next promotion"
                 *         - "Ensure active commit is not a revert before promoting"
                 *
                 *       Proposed mode: Change-based gating - validate the incoming change itself
                 *         - "Don't promote unless new commit follows naming convention"
                 *         - "Ensure proposed commit has proper JIRA ticket reference"
                 *         - "Require specific author for proposed changes"
                 *
                 *     Available variables in the expression context:
                 *       - Commit.SHA (string): the commit SHA being validated (active or proposed based on Target)
                 *       - Commit.Subject (string): the first line of the commit message
                 *       - Commit.Body (string): the commit message body (everything after the subject line)
                 *       - Commit.Author (string): commit author email address
                 *       - Commit.Trailers (map[string][]string): git trailers parsed from commit message
                 */
                expression: string;
                /**
                 * @description Key is the unique identifier for this validation rule.
                 *     It is used as the commit status key and in status messages.
                 *     This key is matched against PromotionStrategy's proposedCommitStatuses or activeCommitStatuses
                 *     to determine which environments this validation applies to.
                 */
                key: string;
                /**
                 * @description PromotionStrategyRef is a reference to the promotion strategy that this commit status applies to.
                 *     The controller will validate commits from ALL environments in the referenced PromotionStrategy
                 *     where this GitCommitStatus.Spec.Key matches an entry in either:
                 *       - PromotionStrategy.Spec.ProposedCommitStatuses (applies to all environments), OR
                 *       - Environment.ProposedCommitStatuses (applies to specific environment)
                 */
                promotionStrategyRef: {
                    /** @description Name is the name of the object to refer to. */
                    name: string;
                };
                /**
                 * @description Target specifies which commit SHA to validate with the expression.
                 *     - "active": Validates the currently active/deployed commit (default behavior)
                 *     - "proposed": Validates the proposed commit that will be promoted
                 *
                 *     The validation result is always reported on the PROPOSED commit (for gating), but this field
                 *     controls which commit's data is used in the expression evaluation.
                 *
                 *     Examples:
                 *       target: "active" - "Don't promote if a revert commit is detected"
                 *       target: "proposed" - "Don't promote unless new commit follows naming convention"
                 * @default active
                 * @enum {string}
                 */
                target?: "active" | "proposed";
            };
            /** @description status defines the observed state of GitCommitStatus */
            status?: {
                /**
                 * @description Conditions represent the latest available observations of the GitCommitStatus's state.
                 *     Standard condition types include "Ready" which aggregates the status of all environments.
                 */
                conditions?: {
                    /**
                     * Format: date-time
                     * @description lastTransitionTime is the last time the condition transitioned from one status to another.
                     *     This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                     */
                    lastTransitionTime: string;
                    /**
                     * @description message is a human readable message indicating details about the transition.
                     *     This may be an empty string.
                     */
                    message: string;
                    /**
                     * Format: int64
                     * @description observedGeneration represents the .metadata.generation that the condition was set based upon.
                     *     For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                     *     with respect to the current state of the instance.
                     */
                    observedGeneration?: number;
                    /**
                     * @description reason contains a programmatic identifier indicating the reason for the condition's last transition.
                     *     Producers of specific condition types may define expected values and meanings for this field,
                     *     and whether the values are considered a guaranteed API.
                     *     The value should be a CamelCase string.
                     *     This field may not be empty.
                     */
                    reason: string;
                    /**
                     * @description status of the condition, one of True, False, Unknown.
                     * @enum {string}
                     */
                    status: "True" | "False" | "Unknown";
                    /** @description type of condition in CamelCase or in foo.example.com/CamelCase. */
                    type: string;
                }[];
                /**
                 * @description Environments holds the validation results for each environment where this validation applies.
                 *     Each entry corresponds to an environment from the PromotionStrategy where the Key matches
                 *     either global or environment-specific proposedCommitStatuses.
                 *
                 *     The controller validates the commit specified by Target ("active" or "proposed")
                 *     but the CommitStatus is always reported on the PROPOSED commit for promotion gating.
                 *     Each environment entry tracks both the ProposedHydratedSha (where status is reported) and the
                 *     ActiveHydratedSha, with TargetedSha indicating which one was actually evaluated.
                 */
                environments?: {
                    /**
                     * @description ActiveHydratedSha is the currently active (deployed) hydrated commit SHA that was validated.
                     *     This comes from the PromotionStrategy's environment status.
                     *     The expression is evaluated against THIS commit's data, not the proposed commit.
                     *     May be empty if the PromotionStrategy hasn't reconciled yet.
                     *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                     */
                    activeHydratedSha?: string;
                    /** @description Branch is the environment branch name being validated. */
                    branch: string;
                    /**
                     * @description ExpressionResult contains the boolean result of the expression evaluation.
                     *     Only set when the expression successfully evaluates to a boolean.
                     *     nil indicates the expression has not yet been evaluated, failed to compile, or failed to evaluate.
                     */
                    expressionResult?: boolean;
                    /**
                     * @description Phase represents the current validation state of the commit.
                     *     - "pending": validation has not completed, commit data is not yet available, or SHAs are empty
                     *     - "success": expression evaluated to true, validation passed
                     *     - "failure": expression evaluated to false, validation failed, or expression compilation failed
                     * @enum {string}
                     */
                    phase: "pending" | "success" | "failure";
                    /**
                     * @description ProposedHydratedSha is the proposed hydrated commit SHA where the validation result is reported.
                     *     This comes from the PromotionStrategy's environment status.
                     *     The CommitStatus resource is created with this SHA, allowing the PromotionStrategy to gate
                     *     promotions based on the validation of the ACTIVE commit.
                     *     May be empty if the PromotionStrategy hasn't reconciled yet.
                     *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                     */
                    proposedHydratedSha: string;
                    /**
                     * @description TargetedSha is the commit SHA that was actually validated by the expression.
                     *     This will match either ProposedHydratedSha or ActiveHydratedSha depending on
                     *     the Target setting ("proposed" or "active").
                     *     This field clarifies which commit's data was used in the expression evaluation.
                     *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                     */
                    targetedSha?: string;
                }[];
                /**
                 * Format: int64
                 * @description ObservedGeneration is the .metadata.generation that this status was reconciled from.
                 *     Because status is written via Server-Side Apply with ForceOwnership (which has no
                 *     optimistic-concurrency check), this field is the canonical way to detect stale
                 *     status writes: compare status.observedGeneration with metadata.generation.
                 */
                observedGeneration?: number;
            };
        };
        /** @description GitRepository is the Schema for the gitrepositories API */
        GitRepository: {
            /**
             * @description APIVersion defines the versioned schema of this representation of an object.
             *     Servers should convert recognized schemas to the latest internal value, and
             *     may reject unrecognized values.
             *     More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: string;
            /**
             * @description Kind is a string value representing the REST resource this object represents.
             *     Servers may infer this from the endpoint the client submits requests to.
             *     Cannot be updated.
             *     In CamelCase.
             *     More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: string;
            metadata: Record<string, never>;
            /** @description GitRepositorySpec defines the desired state of GitRepository */
            spec?: {
                /** @description AzureDevOpsRepo is a repository in Azure DevOps, identified by its project and name. */
                azureDevOps?: {
                    /** @description Name is the name of the repository. */
                    name: string;
                    /** @description Project is the project name in Azure DevOps. */
                    project: string;
                };
                /** @description BitbucketCloudRepo is a repository in Bitbucket Cloud, identified by its owner and name. */
                bitbucketCloud?: {
                    /** @description Name is the name of the repository. */
                    name: string;
                    /** @description Owner is the owner of the repository (can be a user or workspace). */
                    owner: string;
                };
                /** @description FakeRepo is a placeholder for a repository in the fake SCM provider, used for testing purposes. */
                fake?: {
                    /** @description Name is the name of the repository. */
                    name: string;
                    /** @description Owner is the owner of the repository. */
                    owner: string;
                };
                /** @description ForgejoRepo is a repository in Forgejo, identified by its owner and name. */
                forgejo?: {
                    /** @description Name is the name of the repository. */
                    name: string;
                    /** @description Owner is the owner of the repository. */
                    owner: string;
                };
                /** @description GiteaRepo is a repository in Gitea, identified by its owner and name. */
                gitea?: {
                    /** @description Name is the name of the repository. */
                    name: string;
                    /** @description Owner is the owner of the repository. */
                    owner: string;
                };
                /** @description GitHubRepo is a repository in GitHub, identified by its owner and name. */
                github?: {
                    /** @description Name is the name of the repository. */
                    name: string;
                    /** @description Owner is the owner of the repository, which can be a user or an organization. */
                    owner: string;
                };
                /** @description GitLabRepo is a repository in GitLab, identified by its namespace, name, and project ID. */
                gitlab?: {
                    /** @description Name is the project slug of the repository. */
                    name: string;
                    /** @description Namespace is the user, group or group with subgroup (e.g. group/subgroup). */
                    namespace: string;
                    /** @description ProjectID is the ID of the project in GitLab. */
                    projectId: number;
                };
                /** @description ScmProviderObjectReference is a reference to a SCM provider object. */
                scmProviderRef: {
                    /**
                     * @description Kind is the type of resource being referenced
                     * @default ScmProvider
                     * @enum {string}
                     */
                    kind: "ScmProvider" | "ClusterScmProvider";
                    /** @description Name is the name of the resource being referenced */
                    name: string;
                };
            };
            /** @description GitRepositoryStatus defines the observed state of GitRepository */
            status?: {
                /** @description Conditions Represents the observations of the current state. */
                conditions?: {
                    /**
                     * Format: date-time
                     * @description lastTransitionTime is the last time the condition transitioned from one status to another.
                     *     This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                     */
                    lastTransitionTime: string;
                    /**
                     * @description message is a human readable message indicating details about the transition.
                     *     This may be an empty string.
                     */
                    message: string;
                    /**
                     * Format: int64
                     * @description observedGeneration represents the .metadata.generation that the condition was set based upon.
                     *     For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                     *     with respect to the current state of the instance.
                     */
                    observedGeneration?: number;
                    /**
                     * @description reason contains a programmatic identifier indicating the reason for the condition's last transition.
                     *     Producers of specific condition types may define expected values and meanings for this field,
                     *     and whether the values are considered a guaranteed API.
                     *     The value should be a CamelCase string.
                     *     This field may not be empty.
                     */
                    reason: string;
                    /**
                     * @description status of the condition, one of True, False, Unknown.
                     * @enum {string}
                     */
                    status: "True" | "False" | "Unknown";
                    /** @description type of condition in CamelCase or in foo.example.com/CamelCase. */
                    type: string;
                }[];
                /**
                 * Format: int64
                 * @description ObservedGeneration is the .metadata.generation that this status was reconciled from.
                 *     Because status is written via Server-Side Apply with ForceOwnership (which has no
                 *     optimistic-concurrency check), this field is the canonical way to detect stale
                 *     status writes: compare status.observedGeneration with metadata.generation.
                 */
                observedGeneration?: number;
            };
        };
        /** @description PromotionStrategy is the Schema for the promotionstrategies API */
        PromotionStrategy: {
            /**
             * @description APIVersion defines the versioned schema of this representation of an object.
             *     Servers should convert recognized schemas to the latest internal value, and
             *     may reject unrecognized values.
             *     More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: string;
            /**
             * @description Kind is a string value representing the REST resource this object represents.
             *     Servers may infer this from the endpoint the client submits requests to.
             *     Cannot be updated.
             *     In CamelCase.
             *     More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: string;
            metadata: Record<string, never>;
            /** @description PromotionStrategySpec defines the desired state of PromotionStrategy */
            spec: {
                /**
                 * @description ActiveCommitStatuses are commit statuses describing an actively running dry commit. If an active commit status
                 *     is failing for an environment, subsequent environments will not deploy the failing commit.
                 *
                 *     The commit statuses specified in this field apply to all environments in the promotion sequence. You can also
                 *     specify commit statuses for individual environments in the `environments` field.
                 */
                activeCommitStatuses?: {
                    key: string;
                }[];
                /**
                 * @description ActivePath is the default repository subpath for this strategy's active state.
                 *     When set, proposed branches are created as <environment-branch>-next/<activePath>.
                 *     Individual environments can override this value via their own activePath field.
                 */
                activePath?: string;
                /** @description Environments is the sequence of environments that a dry commit will be promoted through. */
                environments: {
                    /**
                     * @description ActiveCommitStatuses are commit statuses describing an actively running dry commit. If an active commit status
                     *     is failing for an environment, subsequent environments will not deploy the failing commit.
                     *
                     *     The commit statuses specified in this field apply to this environment only. You can also specify commit statuses
                     *     for all environments in the `spec.activeCommitStatuses` field.
                     */
                    activeCommitStatuses?: {
                        key: string;
                    }[];
                    /**
                     * @description ActivePath optionally overrides the strategy-level activePath for this environment.
                     *     When set, this environment's CTP uses this path instead of spec.activePath.
                     */
                    activePath?: string;
                    /**
                     * @description AutoMerge determines whether the dry commit should be automatically merged into the next branch in the sequence.
                     *     If false, the dry commit will be proposed but not merged.
                     * @default true
                     */
                    autoMerge?: boolean;
                    /** @description Branch is the name of the active branch for the environment. */
                    branch: string;
                    /**
                     * @description ProposedCommitStatuses are commit statuses describing a proposed dry commit, i.e. one that is not yet running
                     *     in a live environment. If a proposed commit status is failing for a given environment, the dry commit will not
                     *     be promoted to that environment.
                     *
                     *     The commit statuses specified in this field apply to this environment only. You can also specify commit statuses
                     *     for all environments in the `spec.proposedCommitStatuses` field.
                     */
                    proposedCommitStatuses?: {
                        key: string;
                    }[];
                }[];
                /** @description RepositoryReference indicates what repository to promote commits in. */
                gitRepositoryRef: {
                    /** @description Name is the name of the object to refer to. */
                    name: string;
                };
                /**
                 * @description ProposedCommitStatuses are commit statuses describing a proposed dry commit, i.e. one that is not yet running
                 *     in a live environment. If a proposed commit status is failing for a given environment, the dry commit will not
                 *     be promoted to that environment.
                 *
                 *     The commit statuses specified in this field apply to all environments in the promotion sequence. You can also
                 *     specify commit statuses for individual environments in the `environments` field.
                 */
                proposedCommitStatuses?: {
                    key: string;
                }[];
            };
            /** @description PromotionStrategyStatus defines the observed state of PromotionStrategy */
            status?: {
                /** @description Conditions Represents the observations of the current state. */
                conditions?: {
                    /**
                     * Format: date-time
                     * @description lastTransitionTime is the last time the condition transitioned from one status to another.
                     *     This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                     */
                    lastTransitionTime: string;
                    /**
                     * @description message is a human readable message indicating details about the transition.
                     *     This may be an empty string.
                     */
                    message: string;
                    /**
                     * Format: int64
                     * @description observedGeneration represents the .metadata.generation that the condition was set based upon.
                     *     For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                     *     with respect to the current state of the instance.
                     */
                    observedGeneration?: number;
                    /**
                     * @description reason contains a programmatic identifier indicating the reason for the condition's last transition.
                     *     Producers of specific condition types may define expected values and meanings for this field,
                     *     and whether the values are considered a guaranteed API.
                     *     The value should be a CamelCase string.
                     *     This field may not be empty.
                     */
                    reason: string;
                    /**
                     * @description status of the condition, one of True, False, Unknown.
                     * @enum {string}
                     */
                    status: "True" | "False" | "Unknown";
                    /** @description type of condition in CamelCase or in foo.example.com/CamelCase. */
                    type: string;
                }[];
                /** @description Environments holds the status of each environment in the promotion sequence. */
                environments: {
                    /** @description Active is the state of the active branch for the environment. */
                    active: {
                        /** @description CommitStatuses is a list of commit statuses that are being monitored for this branch. */
                        commitStatuses?: {
                            /** @description Description is the description of the commit status */
                            description?: string;
                            /** @description Key staging hydrated branch */
                            key: string;
                            /**
                             * @description Phase what phase is the status in
                             * @enum {string}
                             */
                            phase: "pending" | "success" | "failure";
                            /** @description Url is the URL of the commit status */
                            url?: string;
                        }[];
                        /** @description Dry is the dry state of the branch, which is the commit that is being proposed. */
                        dry?: {
                            /** @description Author is the author of the commit */
                            author?: string;
                            /** @description Body is the body of the commit message without the subject line */
                            body?: string;
                            /**
                             * Format: date-time
                             * @description CommitTime is the time the commit was made
                             */
                            commitTime?: string;
                            /** @description References are the references to other commits, that went into the hydration of the branch */
                            references?: {
                                /** @description Commit contains metadata about the commit that is related in some way to another commit. */
                                commit?: {
                                    /** @description Author is the author of the commit. */
                                    author?: string;
                                    /** @description Body is the body of the commit message, excluding the subject line, i.e. `git show --format=%b`. */
                                    body?: string;
                                    /**
                                     * Format: date-time
                                     * @description Date is the date of the commit, formatted as by `git show -s --format=%aI`.
                                     */
                                    date?: string;
                                    /** @description RepoURL is the URL of the repository where the commit is located. */
                                    repoURL?: string;
                                    /**
                                     * @description Sha is the commit hash.
                                     *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                                     */
                                    sha?: string;
                                    /** @description Subject is the subject line of the commit message, i.e. `git show --format=%s`. */
                                    subject?: string;
                                };
                            }[];
                            /** @description RepoURL is the URL of the repository where the commit is located */
                            repoURL?: string;
                            /**
                             * @description Sha is the SHA of the commit in the branch
                             *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                             */
                            sha?: string;
                            /** @description Subject is the subject line of the commit message */
                            subject?: string;
                        };
                        /** @description Hydrated is the hydrated state of the branch, which is the commit that is currently being worked on. */
                        hydrated?: {
                            /** @description Author is the author of the commit */
                            author?: string;
                            /** @description Body is the body of the commit message without the subject line */
                            body?: string;
                            /**
                             * Format: date-time
                             * @description CommitTime is the time the commit was made
                             */
                            commitTime?: string;
                            /** @description References are the references to other commits, that went into the hydration of the branch */
                            references?: {
                                /** @description Commit contains metadata about the commit that is related in some way to another commit. */
                                commit?: {
                                    /** @description Author is the author of the commit. */
                                    author?: string;
                                    /** @description Body is the body of the commit message, excluding the subject line, i.e. `git show --format=%b`. */
                                    body?: string;
                                    /**
                                     * Format: date-time
                                     * @description Date is the date of the commit, formatted as by `git show -s --format=%aI`.
                                     */
                                    date?: string;
                                    /** @description RepoURL is the URL of the repository where the commit is located. */
                                    repoURL?: string;
                                    /**
                                     * @description Sha is the commit hash.
                                     *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                                     */
                                    sha?: string;
                                    /** @description Subject is the subject line of the commit message, i.e. `git show --format=%s`. */
                                    subject?: string;
                                };
                            }[];
                            /** @description RepoURL is the URL of the repository where the commit is located */
                            repoURL?: string;
                            /**
                             * @description Sha is the SHA of the commit in the branch
                             *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                             */
                            sha?: string;
                            /** @description Subject is the subject line of the commit message */
                            subject?: string;
                        };
                        /** @description Note is the hydrator metadata from the git note attached to the hydrated commit. */
                        note?: {
                            /** @description Author is the author of the dry commit that was used to hydrate the branch. */
                            author?: string;
                            /** @description Body is the body of the dry commit that was used to hydrate the branch without the subject. */
                            body?: string;
                            /**
                             * Format: date-time
                             * @description Date is the date of the dry commit that was used to hydrate the branch.
                             */
                            date?: string;
                            /**
                             * @description DrySha is the SHA of the commit that was used as the dry source for hydration.
                             *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                             */
                            drySha?: string;
                            /** @description References are the references to other commits, that went into the hydration of the branch. */
                            references?: {
                                /** @description Commit contains metadata about the commit that is related in some way to another commit. */
                                commit?: {
                                    /** @description Author is the author of the commit. */
                                    author?: string;
                                    /** @description Body is the body of the commit message, excluding the subject line, i.e. `git show --format=%b`. */
                                    body?: string;
                                    /**
                                     * Format: date-time
                                     * @description Date is the date of the commit, formatted as by `git show -s --format=%aI`.
                                     */
                                    date?: string;
                                    /** @description RepoURL is the URL of the repository where the commit is located. */
                                    repoURL?: string;
                                    /**
                                     * @description Sha is the commit hash.
                                     *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                                     */
                                    sha?: string;
                                    /** @description Subject is the subject line of the commit message, i.e. `git show --format=%s`. */
                                    subject?: string;
                                };
                            }[];
                            /** @description RepoURL is the URL of the repository where the commit is located. */
                            repoURL?: string;
                            /** @description Subject is the subject line of the dry commit that was used to hydrate the branch. */
                            subject?: string;
                        };
                    };
                    /** @description Branch is the name of the active branch for the environment. */
                    branch: string;
                    /**
                     * @description History defines the history of promoted changes done by the PromotionStrategy for each environment.
                     *     You can think of it as a list of PRs merged by GitOps Promoter. It will not include changes that were
                     *     manually merged. The history length is hard-coded to be at most 5 entries. This may change in the future.
                     *     History is constructed on a best-effort basis and should be used for informational purposes only.
                     *     History is in reverse chronological order (newest is first).
                     */
                    history?: {
                        /** @description Active is the state of the active branch at the time the PR was merged. */
                        active?: {
                            /** @description CommitStatuses is a list of commit statuses that are being monitored for this branch. */
                            commitStatuses?: {
                                /** @description Description is the description of the commit status */
                                description?: string;
                                /** @description Key staging hydrated branch */
                                key: string;
                                /**
                                 * @description Phase what phase is the status in
                                 * @enum {string}
                                 */
                                phase: "pending" | "success" | "failure";
                                /** @description Url is the URL of the commit status */
                                url?: string;
                            }[];
                            /** @description Dry is the dry state of the branch, which is the commit that is being proposed. */
                            dry?: {
                                /** @description Author is the author of the commit */
                                author?: string;
                                /** @description Body is the body of the commit message without the subject line */
                                body?: string;
                                /**
                                 * Format: date-time
                                 * @description CommitTime is the time the commit was made
                                 */
                                commitTime?: string;
                                /** @description References are the references to other commits, that went into the hydration of the branch */
                                references?: {
                                    /** @description Commit contains metadata about the commit that is related in some way to another commit. */
                                    commit?: {
                                        /** @description Author is the author of the commit. */
                                        author?: string;
                                        /** @description Body is the body of the commit message, excluding the subject line, i.e. `git show --format=%b`. */
                                        body?: string;
                                        /**
                                         * Format: date-time
                                         * @description Date is the date of the commit, formatted as by `git show -s --format=%aI`.
                                         */
                                        date?: string;
                                        /** @description RepoURL is the URL of the repository where the commit is located. */
                                        repoURL?: string;
                                        /**
                                         * @description Sha is the commit hash.
                                         *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                                         */
                                        sha?: string;
                                        /** @description Subject is the subject line of the commit message, i.e. `git show --format=%s`. */
                                        subject?: string;
                                    };
                                }[];
                                /** @description RepoURL is the URL of the repository where the commit is located */
                                repoURL?: string;
                                /**
                                 * @description Sha is the SHA of the commit in the branch
                                 *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                                 */
                                sha?: string;
                                /** @description Subject is the subject line of the commit message */
                                subject?: string;
                            };
                            /** @description Hydrated is the hydrated state of the branch, which is the commit that is currently being worked on. */
                            hydrated?: {
                                /** @description Author is the author of the commit */
                                author?: string;
                                /** @description Body is the body of the commit message without the subject line */
                                body?: string;
                                /**
                                 * Format: date-time
                                 * @description CommitTime is the time the commit was made
                                 */
                                commitTime?: string;
                                /** @description References are the references to other commits, that went into the hydration of the branch */
                                references?: {
                                    /** @description Commit contains metadata about the commit that is related in some way to another commit. */
                                    commit?: {
                                        /** @description Author is the author of the commit. */
                                        author?: string;
                                        /** @description Body is the body of the commit message, excluding the subject line, i.e. `git show --format=%b`. */
                                        body?: string;
                                        /**
                                         * Format: date-time
                                         * @description Date is the date of the commit, formatted as by `git show -s --format=%aI`.
                                         */
                                        date?: string;
                                        /** @description RepoURL is the URL of the repository where the commit is located. */
                                        repoURL?: string;
                                        /**
                                         * @description Sha is the commit hash.
                                         *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                                         */
                                        sha?: string;
                                        /** @description Subject is the subject line of the commit message, i.e. `git show --format=%s`. */
                                        subject?: string;
                                    };
                                }[];
                                /** @description RepoURL is the URL of the repository where the commit is located */
                                repoURL?: string;
                                /**
                                 * @description Sha is the SHA of the commit in the branch
                                 *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                                 */
                                sha?: string;
                                /** @description Subject is the subject line of the commit message */
                                subject?: string;
                            };
                            /** @description Note is the hydrator metadata from the git note attached to the hydrated commit. */
                            note?: {
                                /** @description Author is the author of the dry commit that was used to hydrate the branch. */
                                author?: string;
                                /** @description Body is the body of the dry commit that was used to hydrate the branch without the subject. */
                                body?: string;
                                /**
                                 * Format: date-time
                                 * @description Date is the date of the dry commit that was used to hydrate the branch.
                                 */
                                date?: string;
                                /**
                                 * @description DrySha is the SHA of the commit that was used as the dry source for hydration.
                                 *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                                 */
                                drySha?: string;
                                /** @description References are the references to other commits, that went into the hydration of the branch. */
                                references?: {
                                    /** @description Commit contains metadata about the commit that is related in some way to another commit. */
                                    commit?: {
                                        /** @description Author is the author of the commit. */
                                        author?: string;
                                        /** @description Body is the body of the commit message, excluding the subject line, i.e. `git show --format=%b`. */
                                        body?: string;
                                        /**
                                         * Format: date-time
                                         * @description Date is the date of the commit, formatted as by `git show -s --format=%aI`.
                                         */
                                        date?: string;
                                        /** @description RepoURL is the URL of the repository where the commit is located. */
                                        repoURL?: string;
                                        /**
                                         * @description Sha is the commit hash.
                                         *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                                         */
                                        sha?: string;
                                        /** @description Subject is the subject line of the commit message, i.e. `git show --format=%s`. */
                                        subject?: string;
                                    };
                                }[];
                                /** @description RepoURL is the URL of the repository where the commit is located. */
                                repoURL?: string;
                                /** @description Subject is the subject line of the dry commit that was used to hydrate the branch. */
                                subject?: string;
                            };
                        };
                        /** @description Proposed is the state of the proposed branch at the time the PR was merged. */
                        proposed?: {
                            /**
                             * @description CommitStatuses is a list of commit statuses that were being monitored for this branch.
                             *     This contains the state frozen at the moment the PR was merged.
                             */
                            commitStatuses?: {
                                /** @description Description is the description of the commit status */
                                description?: string;
                                /** @description Key staging hydrated branch */
                                key: string;
                                /**
                                 * @description Phase what phase is the status in
                                 * @enum {string}
                                 */
                                phase: "pending" | "success" | "failure";
                                /** @description Url is the URL of the commit status */
                                url?: string;
                            }[];
                            /** @description Hydrated is the hydrated state of the branch, which is the commit that is currently being worked on. */
                            hydrated?: {
                                /** @description Author is the author of the commit */
                                author?: string;
                                /** @description Body is the body of the commit message without the subject line */
                                body?: string;
                                /**
                                 * Format: date-time
                                 * @description CommitTime is the time the commit was made
                                 */
                                commitTime?: string;
                                /** @description References are the references to other commits, that went into the hydration of the branch */
                                references?: {
                                    /** @description Commit contains metadata about the commit that is related in some way to another commit. */
                                    commit?: {
                                        /** @description Author is the author of the commit. */
                                        author?: string;
                                        /** @description Body is the body of the commit message, excluding the subject line, i.e. `git show --format=%b`. */
                                        body?: string;
                                        /**
                                         * Format: date-time
                                         * @description Date is the date of the commit, formatted as by `git show -s --format=%aI`.
                                         */
                                        date?: string;
                                        /** @description RepoURL is the URL of the repository where the commit is located. */
                                        repoURL?: string;
                                        /**
                                         * @description Sha is the commit hash.
                                         *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                                         */
                                        sha?: string;
                                        /** @description Subject is the subject line of the commit message, i.e. `git show --format=%s`. */
                                        subject?: string;
                                    };
                                }[];
                                /** @description RepoURL is the URL of the repository where the commit is located */
                                repoURL?: string;
                                /**
                                 * @description Sha is the SHA of the commit in the branch
                                 *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                                 */
                                sha?: string;
                                /** @description Subject is the subject line of the commit message */
                                subject?: string;
                            };
                        };
                        /** @description PullRequest is the state of the pull request that was created for this ChangeTransferPolicy. */
                        pullRequest?: {
                            /**
                             * @description ExternallyMergedOrClosed indicates that the pull request is no longer open on the SCM while the
                             *     PullRequest still desired it open: merged or closed outside the controller, or closed on the SCM
                             *     because the PullRequest resource was deleted (finalizer) before this status was reconciled.
                             *     When true, the State field will be empty ("") since we cannot tell merge vs. close from the provider.
                             *     This status is preserved even after the PullRequest resource is deleted, maintaining a historical
                             *     record until a new pull request is created for this environment.
                             */
                            externallyMergedOrClosed?: boolean;
                            /** @description ID is the unique identifier of the pull request, set by the SCM. */
                            id?: string;
                            /**
                             * Format: date-time
                             * @description PRCreationTime is the time when the pull request was created.
                             */
                            prCreationTime?: string;
                            /**
                             * Format: date-time
                             * @description PRMergeTime is the time when the pull request was merged. This time can vary slightly from the actual merge time because
                             *     it is the time when the ChangeTransferPolicy controller sets the pull requests spec to merge. In the future we plan on making
                             *     this time more accurate by fetching the actual merge time from the SCM via the webhook this would then be updated in the git note
                             *     for that commit.
                             */
                            prMergeTime?: string;
                            /**
                             * @description State is the state of the pull request.
                             * @enum {string}
                             */
                            state?: "closed" | "merged" | "open";
                            /** @description Url is the URL of the pull request. */
                            url?: string;
                        };
                    }[];
                    /** @description LastHealthyDryShas is a list of dry commits that were observed to be healthy in the environment. */
                    lastHealthyDryShas?: {
                        /**
                         * @description Sha is the commit SHA of the dry commit that was observed to be healthy.
                         *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                         */
                        sha: string;
                        /**
                         * Format: date-time
                         * @description Time is the time when the proposed commit for the given dry SHA was merged into the active branch.
                         */
                        time: string;
                    }[];
                    /** @description Proposed is the state of the proposed branch for the environment. */
                    proposed: {
                        /** @description CommitStatuses is a list of commit statuses that are being monitored for this branch. */
                        commitStatuses?: {
                            /** @description Description is the description of the commit status */
                            description?: string;
                            /** @description Key staging hydrated branch */
                            key: string;
                            /**
                             * @description Phase what phase is the status in
                             * @enum {string}
                             */
                            phase: "pending" | "success" | "failure";
                            /** @description Url is the URL of the commit status */
                            url?: string;
                        }[];
                        /** @description Dry is the dry state of the branch, which is the commit that is being proposed. */
                        dry?: {
                            /** @description Author is the author of the commit */
                            author?: string;
                            /** @description Body is the body of the commit message without the subject line */
                            body?: string;
                            /**
                             * Format: date-time
                             * @description CommitTime is the time the commit was made
                             */
                            commitTime?: string;
                            /** @description References are the references to other commits, that went into the hydration of the branch */
                            references?: {
                                /** @description Commit contains metadata about the commit that is related in some way to another commit. */
                                commit?: {
                                    /** @description Author is the author of the commit. */
                                    author?: string;
                                    /** @description Body is the body of the commit message, excluding the subject line, i.e. `git show --format=%b`. */
                                    body?: string;
                                    /**
                                     * Format: date-time
                                     * @description Date is the date of the commit, formatted as by `git show -s --format=%aI`.
                                     */
                                    date?: string;
                                    /** @description RepoURL is the URL of the repository where the commit is located. */
                                    repoURL?: string;
                                    /**
                                     * @description Sha is the commit hash.
                                     *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                                     */
                                    sha?: string;
                                    /** @description Subject is the subject line of the commit message, i.e. `git show --format=%s`. */
                                    subject?: string;
                                };
                            }[];
                            /** @description RepoURL is the URL of the repository where the commit is located */
                            repoURL?: string;
                            /**
                             * @description Sha is the SHA of the commit in the branch
                             *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                             */
                            sha?: string;
                            /** @description Subject is the subject line of the commit message */
                            subject?: string;
                        };
                        /** @description Hydrated is the hydrated state of the branch, which is the commit that is currently being worked on. */
                        hydrated?: {
                            /** @description Author is the author of the commit */
                            author?: string;
                            /** @description Body is the body of the commit message without the subject line */
                            body?: string;
                            /**
                             * Format: date-time
                             * @description CommitTime is the time the commit was made
                             */
                            commitTime?: string;
                            /** @description References are the references to other commits, that went into the hydration of the branch */
                            references?: {
                                /** @description Commit contains metadata about the commit that is related in some way to another commit. */
                                commit?: {
                                    /** @description Author is the author of the commit. */
                                    author?: string;
                                    /** @description Body is the body of the commit message, excluding the subject line, i.e. `git show --format=%b`. */
                                    body?: string;
                                    /**
                                     * Format: date-time
                                     * @description Date is the date of the commit, formatted as by `git show -s --format=%aI`.
                                     */
                                    date?: string;
                                    /** @description RepoURL is the URL of the repository where the commit is located. */
                                    repoURL?: string;
                                    /**
                                     * @description Sha is the commit hash.
                                     *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                                     */
                                    sha?: string;
                                    /** @description Subject is the subject line of the commit message, i.e. `git show --format=%s`. */
                                    subject?: string;
                                };
                            }[];
                            /** @description RepoURL is the URL of the repository where the commit is located */
                            repoURL?: string;
                            /**
                             * @description Sha is the SHA of the commit in the branch
                             *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                             */
                            sha?: string;
                            /** @description Subject is the subject line of the commit message */
                            subject?: string;
                        };
                        /** @description Note is the hydrator metadata from the git note attached to the hydrated commit. */
                        note?: {
                            /** @description Author is the author of the dry commit that was used to hydrate the branch. */
                            author?: string;
                            /** @description Body is the body of the dry commit that was used to hydrate the branch without the subject. */
                            body?: string;
                            /**
                             * Format: date-time
                             * @description Date is the date of the dry commit that was used to hydrate the branch.
                             */
                            date?: string;
                            /**
                             * @description DrySha is the SHA of the commit that was used as the dry source for hydration.
                             *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                             */
                            drySha?: string;
                            /** @description References are the references to other commits, that went into the hydration of the branch. */
                            references?: {
                                /** @description Commit contains metadata about the commit that is related in some way to another commit. */
                                commit?: {
                                    /** @description Author is the author of the commit. */
                                    author?: string;
                                    /** @description Body is the body of the commit message, excluding the subject line, i.e. `git show --format=%b`. */
                                    body?: string;
                                    /**
                                     * Format: date-time
                                     * @description Date is the date of the commit, formatted as by `git show -s --format=%aI`.
                                     */
                                    date?: string;
                                    /** @description RepoURL is the URL of the repository where the commit is located. */
                                    repoURL?: string;
                                    /**
                                     * @description Sha is the commit hash.
                                     *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                                     */
                                    sha?: string;
                                    /** @description Subject is the subject line of the commit message, i.e. `git show --format=%s`. */
                                    subject?: string;
                                };
                            }[];
                            /** @description RepoURL is the URL of the repository where the commit is located. */
                            repoURL?: string;
                            /** @description Subject is the subject line of the dry commit that was used to hydrate the branch. */
                            subject?: string;
                        };
                    };
                    /** @description PullRequest is the state of the pull request that was created for this environment. */
                    pullRequest?: {
                        /**
                         * @description ExternallyMergedOrClosed indicates that the pull request is no longer open on the SCM while the
                         *     PullRequest still desired it open: merged or closed outside the controller, or closed on the SCM
                         *     because the PullRequest resource was deleted (finalizer) before this status was reconciled.
                         *     When true, the State field will be empty ("") since we cannot tell merge vs. close from the provider.
                         *     This status is preserved even after the PullRequest resource is deleted, maintaining a historical
                         *     record until a new pull request is created for this environment.
                         */
                        externallyMergedOrClosed?: boolean;
                        /** @description ID is the unique identifier of the pull request, set by the SCM. */
                        id?: string;
                        /**
                         * Format: date-time
                         * @description PRCreationTime is the time when the pull request was created.
                         */
                        prCreationTime?: string;
                        /**
                         * Format: date-time
                         * @description PRMergeTime is the time when the pull request was merged. This time can vary slightly from the actual merge time because
                         *     it is the time when the ChangeTransferPolicy controller sets the pull requests spec to merge. In the future we plan on making
                         *     this time more accurate by fetching the actual merge time from the SCM via the webhook this would then be updated in the git note
                         *     for that commit.
                         */
                        prMergeTime?: string;
                        /**
                         * @description State is the state of the pull request.
                         * @enum {string}
                         */
                        state?: "closed" | "merged" | "open";
                        /** @description Url is the URL of the pull request. */
                        url?: string;
                    };
                }[];
                /**
                 * Format: int64
                 * @description ObservedGeneration is the .metadata.generation that this status was reconciled from.
                 *     Because status is written via Server-Side Apply with ForceOwnership (which has no
                 *     optimistic-concurrency check), this field is the canonical way to detect stale
                 *     status writes: compare status.observedGeneration with metadata.generation.
                 */
                observedGeneration?: number;
            };
        };
        /** @description PullRequest is the Schema for the pullrequests API */
        PullRequest: {
            /**
             * @description APIVersion defines the versioned schema of this representation of an object.
             *     Servers should convert recognized schemas to the latest internal value, and
             *     may reject unrecognized values.
             *     More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: string;
            /**
             * @description Kind is a string value representing the REST resource this object represents.
             *     Servers may infer this from the endpoint the client submits requests to.
             *     Cannot be updated.
             *     In CamelCase.
             *     More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: string;
            metadata: Record<string, never>;
            /** @description PullRequestSpec defines the desired state of PullRequest */
            spec?: {
                /** @description Commit contains configuration for how we will merge/squash/etc the pull request. */
                commit?: {
                    /** @description Message is the commit message that will be written for the commit that's made when merging the PR. */
                    message: string;
                };
                /** @description Description is the description body of the pull/merge request */
                description?: string;
                /** @description RepositoryReference indicates what repository to open the PR on. */
                gitRepositoryRef: {
                    /** @description Name is the name of the object to refer to. */
                    name: string;
                };
                /**
                 * @description MergeSha is the commit SHA that the head branch must match before the PR can be merged.
                 *     This prevents a race condition where a PR is merged with a different commit than intended.
                 *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                 */
                mergeSha: string;
                /** @description SourceBranch is the base the git reference that we are merging into Head ---> Base */
                sourceBranch: string;
                /**
                 * @description State of the pull request (closed, merged, or open). Must always be "open" when creating a new pull request.
                 *     This value may not be changed to "closed" or "merged" unless the pull request status.id is set.
                 * @default open
                 * @enum {string}
                 */
                state: "closed" | "merged" | "open";
                /** @description TargetBranch is the head the git reference we are merging from Head ---> Base */
                targetBranch: string;
                /** @description Title is the title of the pull request. */
                title: string;
            };
            /** @description PullRequestStatus defines the observed state of PullRequest */
            status?: {
                /** @description Conditions Represents the observations of the current state. */
                conditions?: {
                    /**
                     * Format: date-time
                     * @description lastTransitionTime is the last time the condition transitioned from one status to another.
                     *     This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                     */
                    lastTransitionTime: string;
                    /**
                     * @description message is a human readable message indicating details about the transition.
                     *     This may be an empty string.
                     */
                    message: string;
                    /**
                     * Format: int64
                     * @description observedGeneration represents the .metadata.generation that the condition was set based upon.
                     *     For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                     *     with respect to the current state of the instance.
                     */
                    observedGeneration?: number;
                    /**
                     * @description reason contains a programmatic identifier indicating the reason for the condition's last transition.
                     *     Producers of specific condition types may define expected values and meanings for this field,
                     *     and whether the values are considered a guaranteed API.
                     *     The value should be a CamelCase string.
                     *     This field may not be empty.
                     */
                    reason: string;
                    /**
                     * @description status of the condition, one of True, False, Unknown.
                     * @enum {string}
                     */
                    status: "True" | "False" | "Unknown";
                    /** @description type of condition in CamelCase or in foo.example.com/CamelCase. */
                    type: string;
                }[];
                /**
                 * @description ExternallyMergedOrClosed indicates that the pull request is no longer open on the SCM while the
                 *     resource still desired it open (spec.state is "open"): either it was merged or closed outside the
                 *     controller, or it was closed on the SCM because the PullRequest resource was deleted (finalizer)
                 *     and a subsequent sync observed it missing. The controller does not distinguish those cases here.
                 *     When true, the State field will be empty ("") since we cannot tell merge vs. close from the provider.
                 *     The PullRequest resource will be deleted after this flag is set when possible, but the status is
                 *     preserved in the owning ChangeTransferPolicy to maintain a record.
                 */
                externallyMergedOrClosed?: boolean;
                /** @description ID the id of the pull request */
                id?: string;
                /**
                 * Format: int64
                 * @description ObservedGeneration is the .metadata.generation that this status was reconciled from.
                 *     Because status is written via Server-Side Apply with ForceOwnership (which has no
                 *     optimistic-concurrency check), this field is the canonical way to detect stale
                 *     status writes: compare status.observedGeneration with metadata.generation.
                 */
                observedGeneration?: number;
                /**
                 * Format: date-time
                 * @description PRCreationTime the time the PR was created
                 */
                prCreationTime?: string;
                /**
                 * @description State of the merge request closed/merged/open
                 * @enum {string}
                 */
                state?: "" | "closed" | "merged" | "open";
                /** @description Url is the URL of the pull request. */
                url?: string;
            };
        };
        /** @description RevertCommit is the Schema for the revertcommits API */
        RevertCommit: {
            /**
             * @description APIVersion defines the versioned schema of this representation of an object.
             *     Servers should convert recognized schemas to the latest internal value, and
             *     may reject unrecognized values.
             *     More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: string;
            /**
             * @description Kind is a string value representing the REST resource this object represents.
             *     Servers may infer this from the endpoint the client submits requests to.
             *     Cannot be updated.
             *     In CamelCase.
             *     More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: string;
            metadata: Record<string, never>;
            /** @description RevertCommitSpec defines the desired state of RevertCommit */
            spec?: {
                /** @description Foo is an example field of RevertCommit. Edit revertcommit_types.go to remove/update */
                foo?: string;
            };
            /** @description RevertCommitStatus defines the observed state of RevertCommit */
            status?: Record<string, never>;
        };
        /** @description ScmProvider is the Schema for the scmproviders API */
        ScmProvider: {
            /**
             * @description APIVersion defines the versioned schema of this representation of an object.
             *     Servers should convert recognized schemas to the latest internal value, and
             *     may reject unrecognized values.
             *     More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: string;
            /**
             * @description Kind is a string value representing the REST resource this object represents.
             *     Servers may infer this from the endpoint the client submits requests to.
             *     Cannot be updated.
             *     In CamelCase.
             *     More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: string;
            metadata: Record<string, never>;
            /** @description ScmProviderSpec defines the desired state of ScmProvider */
            spec?: {
                /** @description AzureDevOps required configuration for Azure DevOps as the SCM provider */
                azureDevOps?: {
                    /** @description Domain is the Azure DevOps domain, such as "dev.azure.com". If using the default Azure DevOps domain, leave this field empty. */
                    domain?: string;
                    /** @description Organization is the Azure DevOps organization name. */
                    organization: string;
                };
                /** @description BitbucketCloud required configuration for Bitbucket Cloud as the SCM provider */
                bitbucketCloud?: Record<string, never>;
                /** @description Fake required configuration for Fake as the SCM provider */
                fake?: {
                    /** @description Domain is the domain of the fake SCM provider. This is used for testing purposes. */
                    domain?: string;
                };
                /** @description Forgejo required configuration for Forgejo as the SCM provider */
                forgejo?: {
                    /**
                     * @description Domain is the Forgejo domain, such as "codeberg.org" or "forgejo.mycompany.com".
                     *     There is no default domain since Forgejo is not a service like Gitlab or Github.
                     */
                    domain: string;
                };
                /** @description Gitea required configuration for Gitea as the SCM provider */
                gitea?: {
                    /**
                     * @description Domain is the Gitea domain, such as "gitea.com" or "gitea.mycompany.com".
                     *     There is no default domain since Gitea is self-hosted.
                     */
                    domain: string;
                };
                /** @description GitHub required configuration for GitHub as the SCM provider */
                github?: {
                    /**
                     * Format: int64
                     * @description AppID is the GitHub App ID.
                     */
                    appID: number;
                    /**
                     * @description Domain is the GitHub domain, such as "github.mycompany.com". If using the default GitHub domain, leave this field
                     *     empty.
                     */
                    domain?: string;
                    /**
                     * Format: int64
                     * @description InstallationID is the GitHub App Installation ID. If you want to use this ScmProvider for multiple
                     *     GitHub orgs, do not specify this field. The installation ID will be inferred from the repo owner
                     *     when needed.
                     */
                    installationID?: number;
                };
                /** @description GitLab required configuration for GitLab as the SCM provider */
                gitlab?: {
                    /**
                     * @description Domain is the GitLab domain, such as "gitlab.mycompany.com". If using the default GitLab domain, leave this field
                     *     empty.
                     */
                    domain?: string;
                };
                /** @description SecretRef contains the credentials required to auth to a specific provider */
                secretRef?: {
                    /**
                     * @description Name of the referent.
                     *     This field is effectively required, but due to backwards compatibility is
                     *     allowed to be empty. Instances of this type with an empty value here are
                     *     almost certainly wrong.
                     *     More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                     * @default
                     */
                    name?: string;
                };
            };
            /** @description ScmProviderStatus defines the observed state of ScmProvider */
            status?: {
                /** @description Conditions Represents the observations of the current state. */
                conditions?: {
                    /**
                     * Format: date-time
                     * @description lastTransitionTime is the last time the condition transitioned from one status to another.
                     *     This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                     */
                    lastTransitionTime: string;
                    /**
                     * @description message is a human readable message indicating details about the transition.
                     *     This may be an empty string.
                     */
                    message: string;
                    /**
                     * Format: int64
                     * @description observedGeneration represents the .metadata.generation that the condition was set based upon.
                     *     For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                     *     with respect to the current state of the instance.
                     */
                    observedGeneration?: number;
                    /**
                     * @description reason contains a programmatic identifier indicating the reason for the condition's last transition.
                     *     Producers of specific condition types may define expected values and meanings for this field,
                     *     and whether the values are considered a guaranteed API.
                     *     The value should be a CamelCase string.
                     *     This field may not be empty.
                     */
                    reason: string;
                    /**
                     * @description status of the condition, one of True, False, Unknown.
                     * @enum {string}
                     */
                    status: "True" | "False" | "Unknown";
                    /** @description type of condition in CamelCase or in foo.example.com/CamelCase. */
                    type: string;
                }[];
                /**
                 * Format: int64
                 * @description ObservedGeneration is the .metadata.generation that this status was reconciled from.
                 *     Because status is written via Server-Side Apply with ForceOwnership (which has no
                 *     optimistic-concurrency check), this field is the canonical way to detect stale
                 *     status writes: compare status.observedGeneration with metadata.generation.
                 */
                observedGeneration?: number;
            };
        };
        /** @description TimedCommitStatus is the Schema for the timedcommitstatuses API */
        TimedCommitStatus: {
            /**
             * @description APIVersion defines the versioned schema of this representation of an object.
             *     Servers should convert recognized schemas to the latest internal value, and
             *     may reject unrecognized values.
             *     More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: string;
            /**
             * @description Kind is a string value representing the REST resource this object represents.
             *     Servers may infer this from the endpoint the client submits requests to.
             *     Cannot be updated.
             *     In CamelCase.
             *     More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: string;
            metadata: Record<string, never>;
            /** @description spec defines the desired state of TimedCommitStatus */
            spec: {
                environments: {
                    /** @description Branch is the name of the branch/environment you want to gate for the configured duration. */
                    branch: string;
                    /**
                     * @description Duration is the time duration to wait before considering the commit status as success.
                     *     The duration should be in a format accepted by Go's time.ParseDuration function, e.g., "5m", "1h30m".
                     */
                    duration: string;
                }[];
                /**
                 * @description Key is the gate name referenced in the PromotionStrategy's activeCommitStatuses or
                 *     proposedCommitStatuses. When omitted, the CRD default is timer. Set Key explicitly, even if you use the CRD default.
                 *     Must be lowercase alphanumeric with hyphens, 1–63 characters (pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$).
                 * @default timer
                 */
                key?: string;
                /** @description PromotionStrategyRef is a reference to the promotion strategy that this timed commit status applies to. */
                promotionStrategyRef: {
                    /** @description Name is the name of the object to refer to. */
                    name: string;
                };
            };
            /** @description status defines the observed state of TimedCommitStatus */
            status?: {
                /** @description Conditions represent the latest available observations of an object's state */
                conditions?: {
                    /**
                     * Format: date-time
                     * @description lastTransitionTime is the last time the condition transitioned from one status to another.
                     *     This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                     */
                    lastTransitionTime: string;
                    /**
                     * @description message is a human readable message indicating details about the transition.
                     *     This may be an empty string.
                     */
                    message: string;
                    /**
                     * Format: int64
                     * @description observedGeneration represents the .metadata.generation that the condition was set based upon.
                     *     For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                     *     with respect to the current state of the instance.
                     */
                    observedGeneration?: number;
                    /**
                     * @description reason contains a programmatic identifier indicating the reason for the condition's last transition.
                     *     Producers of specific condition types may define expected values and meanings for this field,
                     *     and whether the values are considered a guaranteed API.
                     *     The value should be a CamelCase string.
                     *     This field may not be empty.
                     */
                    reason: string;
                    /**
                     * @description status of the condition, one of True, False, Unknown.
                     * @enum {string}
                     */
                    status: "True" | "False" | "Unknown";
                    /** @description type of condition in CamelCase or in foo.example.com/CamelCase. */
                    type: string;
                }[];
                /** @description Environments holds the status of each environment being tracked. */
                environments?: {
                    /**
                     * @description AtMostDurationRemaining is the maximum amount of time remaining until the gate is satisfied.
                     *     This is calculated at reconciliation time. When the gate is satisfied (phase=success), this will be 0.
                     */
                    atMostDurationRemaining: string;
                    /** @description Branch is the name of the branch/environment. */
                    branch: string;
                    /**
                     * Format: date-time
                     * @description CommitTime is when the commit was deployed to the active environment.
                     */
                    commitTime: string;
                    /**
                     * @description Phase represents the current phase of the timed gate.
                     * @enum {string}
                     */
                    phase: "pending" | "success";
                    /**
                     * @description RequiredDuration is the duration that must elapse before promotion is allowed.
                     *     If this is updated in the spec with a pending time. The new duration will be calculated from the original CommitTime.
                     *     Allowing you to extend the duration of an already deployed commit, if the time has not yet elapsed.
                     */
                    requiredDuration: string;
                    /**
                     * @description Sha is the commit SHA being tracked for this environment.
                     *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                     */
                    sha: string;
                }[];
                /**
                 * Format: int64
                 * @description ObservedGeneration is the .metadata.generation that this status was reconciled from.
                 *     Because status is written via Server-Side Apply with ForceOwnership (which has no
                 *     optimistic-concurrency check), this field is the canonical way to detect stale
                 *     status writes: compare status.observedGeneration with metadata.generation.
                 */
                observedGeneration?: number;
            };
        };
        /** @description WebRequestCommitStatus is the Schema for the webrequestcommitstatuses API */
        WebRequestCommitStatus: {
            /**
             * @description APIVersion defines the versioned schema of this representation of an object.
             *     Servers should convert recognized schemas to the latest internal value, and
             *     may reject unrecognized values.
             *     More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: string;
            /**
             * @description Kind is a string value representing the REST resource this object represents.
             *     Servers may infer this from the endpoint the client submits requests to.
             *     Cannot be updated.
             *     In CamelCase.
             *     More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: string;
            metadata: Record<string, never>;
            /** @description spec defines the desired state of WebRequestCommitStatus */
            spec: {
                /**
                 * @description DescriptionTemplate is the human-readable commit status description in the SCM provider (GitHub, GitLab, etc.).
                 *     Uses Go templates; variable list and Sprig rules match HTTPRequestSpec (spec.httpRequest). Rendered with the latest
                 *     data after the most recent HTTP request and trigger evaluation (unlike spec.httpRequest URL/body/headers, which use pre-request data — see HTTPRequestSpec).
                 *     How spec.mode.context restricts template data: see ModeSpec.
                 *
                 *     Additional template variables (not in HTTPRequestSpec):
                 *       - {{ index .TriggerVariables "key" }}: result of trigger.when.variables.expression this reconcile (nil if not configured)
                 *       - {{ index .SuccessVariables "key" }}: result of success.when.variables.expression this reconcile (nil if not configured)
                 *
                 *     Examples: "External approval for {{ .Branch }}", "{{ .Phase }} - waiting for external approval".
                 *
                 *     If not specified, defaults to empty string.
                 */
                descriptionTemplate?: string;
                /** @description HTTPRequest configures the outbound HTTP call. See HTTPRequestSpec. */
                httpRequest: {
                    /**
                     * @description Authentication specifies authentication configuration for the HTTP request.
                     *
                     *     Supports multiple authentication methods:
                     *     - Basic Auth: HTTP Basic Authentication with username/password
                     *     - Bearer Token: Bearer token authentication (e.g., API keys, JWTs)
                     *     - OAuth2: OAuth2 client credentials flow for obtaining access tokens
                     *     - TLS: Mutual TLS (mTLS) with client certificates
                     *     - SCM: Reuses credentials from the ScmProvider referenced by the PromotionStrategy (no extra secret needed)
                     *
                     *     For Basic, Bearer, OAuth2, and TLS, credentials must be stored in Kubernetes secrets and referenced via secretRef fields.
                     *     For SCM, credentials are obtained automatically from the SCM provider; just set scm: {}.
                     *
                     *     Examples:
                     *       # Basic Auth
                     *       authentication:
                     *         basic:
                     *           secretRef:
                     *             name: my-creds
                     *
                     *       # Bearer Token
                     *       authentication:
                     *         bearer:
                     *           secretRef:
                     *             name: api-token
                     *
                     *       # OAuth2 Client Credentials
                     *       authentication:
                     *         oauth2:
                     *           tokenURL: "https://auth.example.com/oauth/token"
                     *           secretRef:
                     *             name: oauth-creds
                     *
                     *       # TLS Client Certificate
                     *       authentication:
                     *         tls:
                     *           secretRef:
                     *             name: my-tls-cert
                     *
                     *       # SCM Provider Credentials
                     *       authentication:
                     *         scm: {}
                     */
                    authentication?: {
                        /**
                         * @description Basic specifies HTTP Basic Authentication.
                         *     Credentials can be provided inline (with secret references) or via secretRef.
                         */
                        basic?: {
                            /**
                             * @description SecretRef references a secret containing username and password.
                             *     The secret must contain keys "username" and "password".
                             */
                            secretRef: {
                                /**
                                 * @description Name of the referent.
                                 *     This field is effectively required, but due to backwards compatibility is
                                 *     allowed to be empty. Instances of this type with an empty value here are
                                 *     almost certainly wrong.
                                 *     More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                 * @default
                                 */
                                name?: string;
                            };
                        };
                        /**
                         * @description Bearer specifies Bearer token authentication.
                         *     Token can be provided inline (with secret reference) or via secretRef.
                         */
                        bearer?: {
                            /**
                             * @description SecretRef references a secret containing the bearer token.
                             *     The secret must contain key "token".
                             */
                            secretRef: {
                                /**
                                 * @description Name of the referent.
                                 *     This field is effectively required, but due to backwards compatibility is
                                 *     allowed to be empty. Instances of this type with an empty value here are
                                 *     almost certainly wrong.
                                 *     More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                 * @default
                                 */
                                name?: string;
                            };
                        };
                        /**
                         * @description OAuth2 specifies OAuth2 client credentials authentication.
                         *     The controller will automatically obtain access tokens from the specified tokenURL.
                         */
                        oauth2?: {
                            /**
                             * @description Scopes to request from the OAuth2 provider.
                             *     Optional - some providers don't require scopes for client credentials.
                             *     Example: ["read:api", "write:api"]
                             */
                            scopes?: string[];
                            /**
                             * @description SecretRef references a secret containing clientID and clientSecret.
                             *     The secret must contain keys "clientID" and "clientSecret".
                             */
                            secretRef: {
                                /**
                                 * @description Name of the referent.
                                 *     This field is effectively required, but due to backwards compatibility is
                                 *     allowed to be empty. Instances of this type with an empty value here are
                                 *     almost certainly wrong.
                                 *     More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                 * @default
                                 */
                                name?: string;
                            };
                            /**
                             * @description TokenURL is the OAuth2 token endpoint where access tokens are obtained.
                             *     Example: "https://auth.example.com/oauth/token"
                             */
                            tokenURL: string;
                        };
                        /**
                         * @description Scm specifies authentication using credentials from the SCM provider.
                         *     This uses the credentials configured in the ScmProvider referenced by the PromotionStrategy,
                         *     applying the appropriate authentication method based on the SCM provider type
                         *     (GitHub App, GitLab token, Azure DevOps PAT, etc.).
                         *     To use this auth type, just set it to an empty object, i.e. scm: {}.
                         */
                        scm?: Record<string, never>;
                        /**
                         * @description TLS specifies TLS client certificate authentication (mutual TLS).
                         *     Requires a secret containing the client certificate and private key.
                         */
                        tls?: {
                            /**
                             * @description SecretRef references a secret containing TLS certificate and key.
                             *     The secret should be of type kubernetes.io/tls or contain keys "tls.crt" and "tls.key", and optionally "ca.crt".
                             */
                            secretRef: {
                                /**
                                 * @description Name of the referent.
                                 *     This field is effectively required, but due to backwards compatibility is
                                 *     allowed to be empty. Instances of this type with an empty value here are
                                 *     almost certainly wrong.
                                 *     More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                 * @default
                                 */
                                name?: string;
                            };
                        };
                    };
                    /**
                     * @description BodyTemplate is the request body to send.
                     *     Supports Go templates (see HTTPRequestSpec for available variables).
                     */
                    bodyTemplate?: string;
                    /**
                     * @description HeaderTemplates are additional HTTP headers to include in the request.
                     *     The map key is the header name and the value is the header value (supports Go templates).
                     *     See HTTPRequestSpec for available template variables.
                     */
                    headerTemplates?: {
                        [key: string]: string;
                    };
                    /**
                     * @description Method is the static HTTP method to use. Mutually exclusive with MethodTemplate.
                     *
                     *     Deprecated: Use MethodTemplate instead. A literal value such as `methodTemplate: GET` behaves
                     *     identically to `method: GET` and avoids needing two separate fields. Existing resources that
                     *     set Method continue to work, but new resources should set MethodTemplate. Method may be
                     *     removed in a future release.
                     * @enum {string}
                     */
                    method?: "GET" | "POST" | "PUT" | "PATCH" | "DELETE";
                    /**
                     * @description MethodTemplate is the HTTP method, rendered as a Go template with the same variables and
                     *     Sprig functions as URLTemplate/BodyTemplate/HeaderTemplates. The rendered string is trimmed
                     *     of surrounding whitespace and uppercased; the final value must be one of GET/POST/PUT/PATCH/DELETE
                     *     or the reconcile returns an error. A literal value such as `methodTemplate: GET` works
                     *     identically to a static method; use templating when the method must vary by reconcile
                     *     state (e.g. issuing a search GET on one reconcile and a close POST on the next).
                     *     Mutually exclusive with Method (deprecated).
                     */
                    methodTemplate?: string;
                    /**
                     * @description Timeout is the maximum time to wait for the HTTP request to complete.
                     * @default 30s
                     */
                    timeout?: string;
                    /**
                     * @description URLTemplate is the HTTP endpoint to request.
                     *     Supports Go templates (see HTTPRequestSpec for available variables).
                     */
                    urlTemplate: string;
                };
                /**
                 * @description Key is the unique identifier for this validation rule.
                 *     It is used as the commit status key and in status messages.
                 *     This key is matched against PromotionStrategy's proposedCommitStatuses or activeCommitStatuses
                 *     to determine which environments this validation applies to.
                 *     Must be lowercase alphanumeric with hyphens, 1–63 characters (pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$).
                 */
                key: string;
                /** @description Mode selects polling vs trigger and request scope (context). See ModeSpec. */
                mode: {
                    /**
                     * @description Context is "environments" (default) or "promotionstrategy". See the ModeSpec type documentation for behavior, template limits, and success expression rules.
                     * @default environments
                     * @enum {string}
                     */
                    context?: "environments" | "promotionstrategy";
                    /**
                     * @description Polling enables interval-based polling mode.
                     *     The controller will poll the HTTP endpoint at the specified interval.
                     */
                    polling?: {
                        /**
                         * @description Interval controls how often to retry the HTTP request while in pending state.
                         *     When reportOn is "proposed": stops polling after success for a given SHA.
                         *     When reportOn is "active": always polls at this interval.
                         * @default 1m
                         */
                        interval?: string;
                    };
                    /**
                     * @description Trigger enables expression-based triggering mode.
                     *     The controller will evaluate the expression to determine when to make HTTP requests.
                     */
                    trigger?: {
                        /**
                         * @description RequeueDuration specifies how long to wait before requeuing to re-evaluate the trigger expression.
                         * @default 1m
                         */
                        requeueDuration?: string;
                        /** @description Response optionally configures an expression that extracts data from the HTTP response into ResponseOutput. */
                        response?: {
                            /**
                             * @description Output is evaluated after the HTTP request completes (any status). Response variables are the same as for spec.success.when.expression — see WhenWithOutputSpec.Expression.
                             *     The result is stored in status (environments[].responseOutput or promotionStrategyContext.responseOutput) and exposed on the next reconcile as ResponseOutput in trigger expressions and templates.
                             *     Must return a map/object.
                             */
                            output: {
                                /** @description Expression is an expr expression that must return a map/object. */
                                expression: string;
                            };
                        };
                        /** @description When configures the boolean guard and optional output expression that control whether the HTTP request is made. */
                        when: {
                            /**
                             * @description Expression is a boolean expr expression that decides whether the HTTP request should be made.
                             *     It is evaluated BEFORE each potential HTTP request. When it returns true the request is made;
                             *     when false the controller keeps the previous phase and skips the request.
                             *
                             *     Available variables:
                             *       - Branch (string): the environment branch currently being processed (empty for the shared HTTP request in promotionstrategy context)
                             *       - Phase (string): previous reconcile's phase — per-environment in environments context; aggregate of all branches in promotionstrategy context (success only if all succeeded, failure if any failed, pending otherwise)
                             *       - PromotionStrategy (PromotionStrategy): the full PromotionStrategy spec and status
                             *       - WebRequestCommitStatus (WebRequestCommitStatus): the full WebRequestCommitStatus spec and status (snapshot from the previous reconcile)
                             *       - TriggerOutput (map[string]any): custom data from the previous when.output.expression evaluation
                             *       - ResponseOutput (map[string]any): response data from the previous HTTP request (if any)
                             *       - SuccessOutput (map[string]any): custom data from the previous success.when.output.expression evaluation
                             *       - Variables (map[string]any): when spec.variables is set, the map returned by variables.expression this reconcile; omitted otherwise
                             *
                             *     Note: PromotionStrategy.Status.Environments is an ordered array representing the promotion sequence.
                             *     Use Branch + filter/find to look up environment-specific data:
                             *       find(PromotionStrategy.Status.Environments, {.Branch == Branch}).Proposed.Hydrated.Sha
                             *
                             *     Examples:
                             *       # Always trigger (equivalent to polling mode)
                             *       - "true"
                             *
                             *       # Only trigger when SHA changes from what we last tracked
                             *       - "find(PromotionStrategy.Status.Environments, {.Branch == Branch}).Proposed.Hydrated.Sha != (TriggerOutput['lastCheckedSha'] ?? '')"
                             *
                             *       # Only trigger when a particular commit status is success (e.g. argocd-health)
                             *       - "let env = find(PromotionStrategy.Status.Environments, {.Branch == Branch}); any(env.Proposed.CommitStatuses, {.Key == 'argocd-health' && .Phase == 'success'})"
                             *
                             *       # Only retry if the previous response indicated we should
                             *       - "ResponseOutput == nil || ResponseOutput.status == 'retry'"
                             */
                            expression: string;
                            /**
                             * @description Output optionally holds an expression that produces a map of data to persist across reconcile cycles.
                             *     The expression runs on every reconcile (whether or not the HTTP request is made). Its result is stored in
                             *     status (per-environment under environments[].triggerOutput, or under promotionStrategyContext.triggerOutput when context is promotionstrategy)
                             *     and is available in the next reconcile as TriggerOutput in when.expression, when.output.expression, and in templates.
                             *     Use it to track state such as attempt counts, last-seen SHAs, or timestamps.
                             *
                             *     Delivery semantics caveat: persisted output is read from the controller's informer cache at the start of the next
                             *     reconcile. Under cache-propagation lag, controller restarts, or status-write retries, the next reconcile may not
                             *     see the most recently persisted output and may re-fire the HTTP request. Treat this as AT-LEAST-ONCE delivery —
                             *     counters built on TriggerOutput are eventually-consistent (a stale read can cause a duplicate increment), so use
                             *     them for backoff hints, not for hard "fail after N attempts" gates.
                             *
                             *     Variables are the same as for Expression (see above). The expression must return a map/object; every key is stored in TriggerOutput.
                             *
                             *     Examples:
                             *       # Track SHA to detect changes (idempotent: replays produce the same trackedSha for the same input)
                             *       - "{ trackedSha: find(PromotionStrategy.Status.Environments, {.Branch == Branch}).Proposed.Hydrated.Sha }"
                             *
                             *       # Increment attempt counter (eventually-consistent; may briefly under-count under cache lag)
                             *       - "{ attemptCount: (TriggerOutput[\"attemptCount\"] ?? 0) + 1 }"
                             */
                            output?: {
                                /** @description Expression is an expr expression that must return a map/object. */
                                expression: string;
                            };
                            /**
                             * @description Variables optionally holds an expression that runs before Expression and Output.Expression.
                             *     It receives the same variables as Expression (see Expression documentation below) and must return a map/object.
                             *     The result is injected as top-level binding Variables (map) for Expression and Output.Expression only — use Variables.<key> in those expressions.
                             *     The Variables binding is not set when spec.variables is omitted. It is not available to response.output.expression.
                             *     The result is also available in Go templates for DescriptionTemplate and UrlTemplate:
                             *       - trigger.when.variables result → {{ index .TriggerVariables "key" }}
                             *       - success.when.variables result → {{ index .SuccessVariables "key" }}
                             */
                            variables?: {
                                /** @description Expression is an expr expression that must return a map/object. */
                                expression: string;
                            };
                        };
                    };
                };
                /**
                 * @description PromotionStrategyRef references the PromotionStrategy this applies to.
                 *     The controller will check commits from ALL environments in the referenced PromotionStrategy
                 *     where this WebRequestCommitStatus.Spec.Key matches an entry in either:
                 *       - PromotionStrategy.Spec.ProposedCommitStatuses (applies to all environments), OR
                 *       - PromotionStrategy.Spec.ActiveCommitStatuses (applies to all environments), OR
                 *       - Environment.ProposedCommitStatuses (applies to specific environment), OR
                 *       - Environment.ActiveCommitStatuses (applies to specific environment)
                 */
                promotionStrategyRef: {
                    /** @description Name is the name of the object to refer to. */
                    name: string;
                };
                /**
                 * @description ReportOn specifies which commit SHA to report the CommitStatus on.
                 *     - "proposed": Reports on the proposed hydrated commit SHA (default)
                 *     - "active": Reports on the active hydrated commit SHA
                 *
                 *     When "proposed": Polls until success, then stops polling for that SHA.
                 *     Use "proposed" for checks that need to run just once before a change is promoted, like an approval step.
                 *
                 *     When "active": Polls forever, even after success (active state can change).
                 *     Use "active" for checks that monitor the change after it's released, for example a metrics monitoring service.
                 * @default proposed
                 * @enum {string}
                 */
                reportOn?: "proposed" | "active";
                /** @description Success defines when the commit status phase is success. Evaluated every reconcile. See SuccessSpec. */
                success: {
                    /** @description When is evaluated every reconcile. See WhenWithOutputSpec.Expression. */
                    when: {
                        /**
                         * @description Expression is a boolean expr expression that decides whether the HTTP request should be made.
                         *     It is evaluated BEFORE each potential HTTP request. When it returns true the request is made;
                         *     when false the controller keeps the previous phase and skips the request.
                         *
                         *     Available variables:
                         *       - Branch (string): the environment branch currently being processed (empty for the shared HTTP request in promotionstrategy context)
                         *       - Phase (string): previous reconcile's phase — per-environment in environments context; aggregate of all branches in promotionstrategy context (success only if all succeeded, failure if any failed, pending otherwise)
                         *       - PromotionStrategy (PromotionStrategy): the full PromotionStrategy spec and status
                         *       - WebRequestCommitStatus (WebRequestCommitStatus): the full WebRequestCommitStatus spec and status (snapshot from the previous reconcile)
                         *       - TriggerOutput (map[string]any): custom data from the previous when.output.expression evaluation
                         *       - ResponseOutput (map[string]any): response data from the previous HTTP request (if any)
                         *       - SuccessOutput (map[string]any): custom data from the previous success.when.output.expression evaluation
                         *       - Variables (map[string]any): when spec.variables is set, the map returned by variables.expression this reconcile; omitted otherwise
                         *
                         *     Note: PromotionStrategy.Status.Environments is an ordered array representing the promotion sequence.
                         *     Use Branch + filter/find to look up environment-specific data:
                         *       find(PromotionStrategy.Status.Environments, {.Branch == Branch}).Proposed.Hydrated.Sha
                         *
                         *     Examples:
                         *       # Always trigger (equivalent to polling mode)
                         *       - "true"
                         *
                         *       # Only trigger when SHA changes from what we last tracked
                         *       - "find(PromotionStrategy.Status.Environments, {.Branch == Branch}).Proposed.Hydrated.Sha != (TriggerOutput['lastCheckedSha'] ?? '')"
                         *
                         *       # Only trigger when a particular commit status is success (e.g. argocd-health)
                         *       - "let env = find(PromotionStrategy.Status.Environments, {.Branch == Branch}); any(env.Proposed.CommitStatuses, {.Key == 'argocd-health' && .Phase == 'success'})"
                         *
                         *       # Only retry if the previous response indicated we should
                         *       - "ResponseOutput == nil || ResponseOutput.status == 'retry'"
                         */
                        expression: string;
                        /**
                         * @description Output optionally holds an expression that produces a map of data to persist across reconcile cycles.
                         *     The expression runs on every reconcile (whether or not the HTTP request is made). Its result is stored in
                         *     status (per-environment under environments[].triggerOutput, or under promotionStrategyContext.triggerOutput when context is promotionstrategy)
                         *     and is available in the next reconcile as TriggerOutput in when.expression, when.output.expression, and in templates.
                         *     Use it to track state such as attempt counts, last-seen SHAs, or timestamps.
                         *
                         *     Delivery semantics caveat: persisted output is read from the controller's informer cache at the start of the next
                         *     reconcile. Under cache-propagation lag, controller restarts, or status-write retries, the next reconcile may not
                         *     see the most recently persisted output and may re-fire the HTTP request. Treat this as AT-LEAST-ONCE delivery —
                         *     counters built on TriggerOutput are eventually-consistent (a stale read can cause a duplicate increment), so use
                         *     them for backoff hints, not for hard "fail after N attempts" gates.
                         *
                         *     Variables are the same as for Expression (see above). The expression must return a map/object; every key is stored in TriggerOutput.
                         *
                         *     Examples:
                         *       # Track SHA to detect changes (idempotent: replays produce the same trackedSha for the same input)
                         *       - "{ trackedSha: find(PromotionStrategy.Status.Environments, {.Branch == Branch}).Proposed.Hydrated.Sha }"
                         *
                         *       # Increment attempt counter (eventually-consistent; may briefly under-count under cache lag)
                         *       - "{ attemptCount: (TriggerOutput[\"attemptCount\"] ?? 0) + 1 }"
                         */
                        output?: {
                            /** @description Expression is an expr expression that must return a map/object. */
                            expression: string;
                        };
                        /**
                         * @description Variables optionally holds an expression that runs before Expression and Output.Expression.
                         *     It receives the same variables as Expression (see Expression documentation below) and must return a map/object.
                         *     The result is injected as top-level binding Variables (map) for Expression and Output.Expression only — use Variables.<key> in those expressions.
                         *     The Variables binding is not set when spec.variables is omitted. It is not available to response.output.expression.
                         *     The result is also available in Go templates for DescriptionTemplate and UrlTemplate:
                         *       - trigger.when.variables result → {{ index .TriggerVariables "key" }}
                         *       - success.when.variables result → {{ index .SuccessVariables "key" }}
                         */
                        variables?: {
                            /** @description Expression is an expr expression that must return a map/object. */
                            expression: string;
                        };
                    };
                };
                /**
                 * @description UrlTemplate is the commit status target URL in the SCM provider. Same Go templates, variables (including .TriggerVariables and .SuccessVariables), timing, and context rules as DescriptionTemplate.
                 *     Typical uses: approval UI, dashboard, API, or runbook links.
                 *     To include the reported SHA, walk PromotionStrategy.Status.Environments by Branch:
                 *       "https://approvals.example.com/{{ range .PromotionStrategy.Status.Environments }}{{ if eq .Branch $.Branch }}{{ .Proposed.Hydrated.Sha }}{{ end }}{{ end }}"
                 *     For branch-only links: "https://dashboard.example.com/{{ .Branch }}/status".
                 *
                 *     If not specified, defaults to empty string (no URL shown).
                 */
                urlTemplate?: string;
            };
            /** @description status defines the observed state of WebRequestCommitStatus */
            status?: {
                /** @description Conditions represent the latest available observations of an object's state */
                conditions?: {
                    /**
                     * Format: date-time
                     * @description lastTransitionTime is the last time the condition transitioned from one status to another.
                     *     This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                     */
                    lastTransitionTime: string;
                    /**
                     * @description message is a human readable message indicating details about the transition.
                     *     This may be an empty string.
                     */
                    message: string;
                    /**
                     * Format: int64
                     * @description observedGeneration represents the .metadata.generation that the condition was set based upon.
                     *     For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                     *     with respect to the current state of the instance.
                     */
                    observedGeneration?: number;
                    /**
                     * @description reason contains a programmatic identifier indicating the reason for the condition's last transition.
                     *     Producers of specific condition types may define expected values and meanings for this field,
                     *     and whether the values are considered a guaranteed API.
                     *     The value should be a CamelCase string.
                     *     This field may not be empty.
                     */
                    reason: string;
                    /**
                     * @description status of the condition, one of True, False, Unknown.
                     * @enum {string}
                     */
                    status: "True" | "False" | "Unknown";
                    /** @description type of condition in CamelCase or in foo.example.com/CamelCase. */
                    type: string;
                }[];
                /**
                 * @description Environments holds the status of each environment when context is "environments".
                 *     When context is "promotionstrategy", this slice is empty and PromotionStrategyContext is used instead.
                 */
                environments?: {
                    /** @description Branch is the name of the branch/environment. */
                    branch: string;
                    /**
                     * Format: date-time
                     * @description LastRequestTime is when the last HTTP request was made.
                     */
                    lastRequestTime?: string;
                    /** @description LastResponseStatusCode is the HTTP status code from the last request. */
                    lastResponseStatusCode?: number;
                    /**
                     * @description LastSuccessfulSha is the last commit SHA that achieved success status for this environment.
                     *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                     */
                    lastSuccessfulSha?: string;
                    /**
                     * @description Phase represents the current phase of the validation.
                     *     This controller sets only "pending" or "success"; it never sets "failure" (failure is allowed by the enum for API consistency).
                     * @enum {string}
                     */
                    phase: "pending" | "success" | "failure";
                    /**
                     * @description ReportedSha is the commit SHA being reported on for this environment.
                     *     This is determined by the reportOn field (proposed or active).
                     *     Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                     */
                    reportedSha?: string;
                    /** @description ResponseOutput is the map from spec.mode.trigger.response.output.expression when spec.mode.trigger.response is set; otherwise nil. */
                    responseOutput?: Record<string, never>;
                    /**
                     * @description SuccessOutput is the map from spec.success.when.output.expression (arbitrary JSON keys).
                     *     Evaluated every reconcile (whether or not an HTTP request was made). The result is stored here and
                     *     exposed on the next reconcile as SuccessOutput in trigger/success expressions and templates.
                     */
                    successOutput?: Record<string, never>;
                    /** @description TriggerOutput is the map from spec.mode.trigger.when.output.expression (arbitrary JSON keys). */
                    triggerOutput?: Record<string, never>;
                }[];
                /**
                 * Format: int64
                 * @description ObservedGeneration is the .metadata.generation that this status was reconciled from.
                 *     Because status is written via Server-Side Apply with ForceOwnership (which has no
                 *     optimistic-concurrency check), this field is the canonical way to detect stale
                 *     status writes: compare status.observedGeneration with metadata.generation.
                 */
                observedGeneration?: number;
                /**
                 * @description PromotionStrategyContext holds the result of the one HTTP run when context is "promotionstrategy".
                 *     At most one request is made per WebRequestCommitStatus; phase(s) are reported on each environment's CommitStatus.
                 */
                promotionStrategyContext?: {
                    /**
                     * Format: date-time
                     * @description LastRequestTime is when the last HTTP request was made.
                     */
                    lastRequestTime?: string;
                    /** @description LastResponseStatusCode is the HTTP status code from the last request. */
                    lastResponseStatusCode?: number;
                    /**
                     * @description LastSuccessfulShas tracks the last SHA that achieved success for each branch.
                     *     Used with reportOn "proposed" + polling to skip HTTP requests when all environments
                     *     have already succeeded for their current SHAs.
                     */
                    lastSuccessfulShas?: {
                        /** @description Branch is the environment branch name (merge key for lastSuccessfulShas list). */
                        branch: string;
                        /** @description LastSuccessfulSha is the SHA that last achieved success for this branch. */
                        lastSuccessfulSha?: string;
                    }[];
                    /**
                     * @description PhasePerBranch holds the resolved phase for each applicable branch.
                     *     Every applicable branch is always present after reconciliation.
                     */
                    phasePerBranch?: {
                        /** @description Branch is the environment branch name (merge key for phasePerBranch list). */
                        branch: string;
                        /**
                         * @description Phase is "pending", "success", or "failure".
                         * @enum {string}
                         */
                        phase: "pending" | "success" | "failure";
                    }[];
                    /** @description ResponseOutput: same semantics as WebRequestCommitStatusEnvironmentStatus.ResponseOutput, one shared map for context=promotionstrategy. */
                    responseOutput?: Record<string, never>;
                    /** @description SuccessOutput: same semantics as WebRequestCommitStatusEnvironmentStatus.SuccessOutput, one shared map for context=promotionstrategy. */
                    successOutput?: Record<string, never>;
                    /** @description TriggerOutput: same semantics as WebRequestCommitStatusEnvironmentStatus.TriggerOutput, one shared map for context=promotionstrategy. */
                    triggerOutput?: Record<string, never>;
                };
            };
        };
    };
    responses: never;
    parameters: never;
    requestBodies: never;
    headers: never;
    pathItems: never;
};
export type $defs = Record<string, never>;
export type operations = Record<string, never>;
