// Code generated by `make generate-ui-types`. DO NOT EDIT.

/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export type paths = Record<string, never>;
export type webhooks = Record<string, never>;
export type components = {
    schemas: {
        /** @description ApplicationsSelected represents the Argo CD applications that are selected by the commit status. The fields in this struct are all required, since the controller should always fully construct this information. */
        ApplicationsSelected: {
            /**
             * @description ClusterName is the name of the cluster that the application manifest is deployed to. An empty string indicates the local cluster. There is no minimum length, since the local cluster is represented by an empty string.
             * @default
             */
            clusterName: string;
            /**
             * @description Environment is the syncSource.targetBranch of the Argo CD application (in effect, its environment).
             * @default
             */
            environment: string;
            /** @description LastTransitionTime is the last time the phase transitioned. */
            lastTransitionTime?: components["schemas"]["Time"];
            /**
             * @description Name is the name of the Argo CD application.
             * @default
             */
            name: string;
            /**
             * @description Namespace is the namespace of the Argo CD application.
             * @default
             */
            namespace: string;
            /**
             * @description Phase is the current phase of the commit status.
             * @default
             */
            phase: string;
            /**
             * @description Sha is the commit SHA that the application is synced to. This field is only populated when the application's sync status is Synced. When the sync status is OutOfSync or Unknown, this field will be empty because Status.Sync.Revision may contain a branch name instead of a SHA. Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
             * @default
             */
            sha: string;
        };
        /** @description ArgoCDCommitStatus is the Schema for the argocdcommitstatuses API. */
        ArgoCDCommitStatus: {
            /** @description APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
            apiVersion?: string;
            /** @description Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
            kind?: string;
            /** @default {} */
            metadata?: components["schemas"]["ObjectMeta"];
            /** @default {} */
            spec?: components["schemas"]["ArgoCDCommitStatusSpec"];
            /** @default {} */
            status?: components["schemas"]["ArgoCDCommitStatusStatus"];
        };
        /** @description ArgoCDCommitStatusSpec defines the desired state of ArgoCDCommitStatus. */
        ArgoCDCommitStatusSpec: {
            /** @description ApplicationSelector is a label selector that selects the Argo CD applications to which this commit status applies. */
            applicationSelector?: components["schemas"]["LabelSelector"];
            /** @description Key is the gate name referenced in the PromotionStrategy's activeCommitStatuses. When omitted, the CRD default is argocd-health. Set Key explicitly, even if you use the CRD default. Must be lowercase alphanumeric with hyphens, 1–63 characters (pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$). */
            key?: string;
            /**
             * @description PromotionStrategyRef is a reference to the promotion strategy that this commit status applies to.
             * @default {}
             */
            promotionStrategyRef?: components["schemas"]["io_argoproj_promoter_v1alpha1_ObjectReference"];
            /**
             * @description URL generates the URL to use in the CommitStatus, for example a link to the Argo CD UI.
             * @default {}
             */
            url?: components["schemas"]["URLConfig"];
        };
        /** @description ArgoCDCommitStatusStatus defines the observed state of ArgoCDCommitStatus. */
        ArgoCDCommitStatusStatus: {
            /** @description ApplicationsSelected represents the Argo CD applications that are selected by the commit status. This field is sorted by environment (same order as the referenced PromotionStrategy), then namespace, then name. */
            applicationsSelected?: components["schemas"]["ApplicationsSelected"][];
            /** @description Conditions Represents the observations of the current state. */
            conditions?: components["schemas"]["Condition"][];
            /** @description InstanceID mirrors metadata.labels[promoter.argoproj.io/instance-id] stamped on each reconcile attempt by this install's controller, including when Ready=False; omitted when the resource has no instance-id label (default install). */
            instanceID?: string;
            /**
             * Format: int64
             * @description ObservedGeneration is the .metadata.generation that this status was reconciled from. Because status is written via Server-Side Apply with ForceOwnership (which has no optimistic-concurrency check), this field is the canonical way to detect stale status writes: compare status.observedGeneration with metadata.generation.
             */
            observedGeneration?: number;
        };
        /** @description AzureDevOps is an Azure DevOps SCM provider configuration. It is used to configure the Azure DevOps settings. */
        AzureDevOps: {
            /** @description Domain is the Azure DevOps domain, such as "dev.azure.com". If using the default Azure DevOps domain, leave this field empty. */
            domain?: string;
            /**
             * @description Organization is the Azure DevOps organization name.
             * @default
             */
            organization: string;
        };
        /** @description AzureDevOpsRepo is a repository in Azure DevOps, identified by its project and name. */
        AzureDevOpsRepo: {
            /**
             * @description Name is the name of the repository.
             * @default
             */
            name: string;
            /**
             * @description Project is the project name in Azure DevOps.
             * @default
             */
            project: string;
        };
        /**
         * @description BasicAuth defines HTTP Basic Authentication.
         *
         *     HTTP Basic Auth encodes the username and password as base64 and sends them in the Authorization header: "Authorization: Basic <base64(username:password)>"
         *
         *     Credentials must be stored in a Kubernetes secret and referenced via secretRef:
         *
         *     	basic:
         *     	  secretRef:
         *     	    name: my-basic-auth-secret  # secret must contain keys "username" and "password"
         */
        BasicAuth: {
            /** @description SecretRef references a secret containing username and password. The secret must contain keys "username" and "password". */
            secretRef: components["schemas"]["LocalObjectReference"];
        };
        /**
         * @description BearerAuth defines Bearer token authentication.
         *
         *     Bearer tokens are commonly used for API authentication. The token is sent in the Authorization header: "Authorization: Bearer <token>"
         *
         *     Common use cases: - API keys - JWT tokens - Personal access tokens - Static authentication tokens
         *
         *     The token must be stored in a Kubernetes secret and referenced via secretRef:
         *
         *     	bearer:
         *     	  secretRef:
         *     	    name: my-bearer-token-secret  # secret must contain key "token"
         */
        BearerAuth: {
            /** @description SecretRef references a secret containing the bearer token. The secret must contain key "token". */
            secretRef: components["schemas"]["LocalObjectReference"];
        };
        /** @description BitbucketCloud is a Bitbucket Cloud SCM provider configuration. It is used to configure the Bitbucket Cloud settings. */
        BitbucketCloud: Record<string, never>;
        /** @description BitbucketCloudRepo is a repository in Bitbucket Cloud, identified by its owner and name. */
        BitbucketCloudRepo: {
            /**
             * @description Name is the name of the repository.
             * @default
             */
            name: string;
            /**
             * @description Owner is the owner of the repository (can be a user or workspace).
             * @default
             */
            owner: string;
        };
        /** @description ChangeRequestPolicyCommitStatusPhase defines the phase of a commit status in a ChangeTransferPolicy. */
        ChangeRequestPolicyCommitStatusPhase: {
            /** @description Description is the description of the commit status */
            description?: string;
            /**
             * @description Key staging hydrated branch
             * @default
             */
            key: string;
            /**
             * @description Phase what phase is the status in
             * @default
             */
            phase: string;
            /** @description Url is the URL of the commit status */
            url?: string;
        };
        /** @description ChangeTransferPolicy is the Schema for the changetransferpolicies API */
        ChangeTransferPolicy: {
            /** @description APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
            apiVersion?: string;
            /** @description Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
            kind?: string;
            /** @default {} */
            metadata?: components["schemas"]["ObjectMeta"];
            /** @default {} */
            spec?: components["schemas"]["ChangeTransferPolicySpec"];
            /** @default {} */
            status?: components["schemas"]["ChangeTransferPolicyStatus"];
        };
        /** @description ChangeTransferPolicySpec defines the desired state of ChangeTransferPolicy */
        ChangeTransferPolicySpec: {
            /**
             * @description ActiveBranch staging hydrated branch Must not start with '-', contain ':', or contain '..'.
             * @default
             */
            activeBranch: string;
            /** @description ActiveCommitStatuses lists the statuses to be monitored on the active branch */
            activeCommitStatuses: components["schemas"]["CommitStatusSelector"][];
            /** @description ActivePath is an optional repository subpath for this policy's active state. When set, hydrator metadata is read from <activePath>/hydrator.metadata. */
            activePath?: string;
            autoMerge?: boolean;
            /**
             * @description RepositoryReference what repository to open the PR on.
             * @default {}
             */
            gitRepositoryRef: components["schemas"]["io_argoproj_promoter_v1alpha1_ObjectReference"];
            /**
             * @description ProposedBranch staging hydrated branch Must not start with '-', contain ':', or contain '..'.
             * @default
             */
            proposedBranch: string;
            /** @description ProposedCommitStatuses lists the statuses to be monitored on the proposed branch */
            proposedCommitStatuses: components["schemas"]["CommitStatusSelector"][];
        };
        /** @description ChangeTransferPolicyStatus defines the observed state of ChangeTransferPolicy */
        ChangeTransferPolicyStatus: {
            /**
             * @description Active is the state of the active branch.
             * @default {}
             */
            active?: components["schemas"]["CommitBranchState"];
            /** @description Conditions Represents the observations of the current state. */
            conditions?: components["schemas"]["Condition"][];
            /** @description History defines the history of promoted changes done by the ChangeTransferPolicy. You can think of it as a list of PRs merged by GitOps Promoter. It will not include changes that were manually merged. The history length is hard-coded to be at most 5 entries. This may change in the future. History is constructed on a best-effort basis and should be used for informational purposes only. History is in reverse chronological order (newest is first). */
            history?: components["schemas"]["History"][];
            /** @description InstanceID mirrors metadata.labels[promoter.argoproj.io/instance-id] stamped on each reconcile attempt by this install's controller, including when Ready=False; omitted when the resource has no instance-id label (default install). */
            instanceID?: string;
            /**
             * Format: int64
             * @description ObservedGeneration is the .metadata.generation that this status was reconciled from. Because status is written via Server-Side Apply with ForceOwnership (which has no optimistic-concurrency check), this field is the canonical way to detect stale status writes: compare status.observedGeneration with metadata.generation.
             */
            observedGeneration?: number;
            /**
             * @description Proposed is the state of the proposed branch.
             * @default {}
             */
            proposed?: components["schemas"]["CommitBranchState"];
            /** @description PullRequest is the state of the pull request that was created for this ChangeTransferPolicy. */
            pullRequest?: components["schemas"]["PullRequestCommonStatus"];
        };
        /** @description ClusterScmProvider is the Schema for the clusterscmproviders API. */
        ClusterScmProvider: {
            /** @description APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
            apiVersion?: string;
            /** @description Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
            kind?: string;
            /** @default {} */
            metadata?: components["schemas"]["ObjectMeta"];
            /** @default {} */
            spec?: components["schemas"]["ScmProviderSpec"];
            /** @default {} */
            status?: components["schemas"]["ScmProviderStatus"];
        };
        /** @description CommitBranchState defines the state of a branch in a ChangeTransferPolicy. */
        CommitBranchState: {
            /** @description CommitStatuses is a list of commit statuses that are being monitored for this branch. */
            commitStatuses?: components["schemas"]["ChangeRequestPolicyCommitStatusPhase"][];
            /**
             * @description Dry is the dry state of the branch, which is the commit that is being proposed.
             * @default {}
             */
            dry?: components["schemas"]["CommitShaState"];
            /**
             * @description Hydrated is the hydrated state of the branch, which is the commit that is currently being worked on.
             * @default {}
             */
            hydrated?: components["schemas"]["CommitShaState"];
            /** @description Note is the hydrator metadata from the git note attached to the hydrated commit. */
            note?: components["schemas"]["HydratorMetadata"];
        };
        /** @description CommitBranchStateHistoryProposed is identical to CommitBranchState minus the Dry state. In the context of History, the Dry state is not relevant as the proposed dry side at merge becomes the Active. */
        CommitBranchStateHistoryProposed: {
            /** @description CommitStatuses is a list of commit statuses that were being monitored for this branch. This contains the state frozen at the moment the PR was merged. */
            commitStatuses?: components["schemas"]["ChangeRequestPolicyCommitStatusPhase"][];
            /**
             * @description Hydrated is the hydrated state of the branch, which is the commit that is currently being worked on.
             * @default {}
             */
            hydrated?: components["schemas"]["CommitShaState"];
        };
        /** @description CommitConfiguration defines the commit configuration for how we will merge/squash/etc the pull request. */
        CommitConfiguration: {
            /**
             * @description Message is the commit message that will be written for the commit that's made when merging the PR.
             * @default
             */
            message: string;
        };
        /** @description CommitMetadata contains metadata about a commit that is related in some way to another commit. */
        CommitMetadata: {
            /** @description Author is the author of the commit. */
            author?: string;
            /** @description Body is the body of the commit message, excluding the subject line, i.e. `git show --format=%b`. */
            body?: string;
            /** @description Date is the date of the commit, formatted as by `git show -s --format=%aI`. */
            date?: components["schemas"]["Time"];
            /** @description RepoURL is the URL of the repository where the commit is located. */
            repoURL?: string;
            /** @description Sha is the commit hash. Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats. */
            sha?: string;
            /** @description Subject is the subject line of the commit message, i.e. `git show --format=%s`. */
            subject?: string;
        };
        /** @description CommitShaState defines the state of a commit in a branch. */
        CommitShaState: {
            /** @description Author is the author of the commit */
            author?: string;
            /** @description Body is the body of the commit message without the subject line */
            body?: string;
            /** @description CommitTime is the time the commit was made */
            commitTime?: components["schemas"]["Time"];
            /** @description References are the references to other commits, that went into the hydration of the branch */
            references?: components["schemas"]["RevisionReference"][];
            /** @description RepoURL is the URL of the repository where the commit is located */
            repoURL?: string;
            /** @description Sha is the SHA of the commit in the branch Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats. */
            sha?: string;
            /** @description Subject is the subject line of the commit message */
            subject?: string;
        };
        /** @description CommitStatus is the Schema for the commitstatuses API */
        CommitStatus: {
            /** @description APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
            apiVersion?: string;
            /** @description Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
            kind?: string;
            /** @default {} */
            metadata?: components["schemas"]["ObjectMeta"];
            /** @default {} */
            spec?: components["schemas"]["CommitStatusSpec"];
            /** @default {} */
            status?: components["schemas"]["CommitStatusStatus"];
        };
        /** @description CommitStatusSelector is used to select commit statuses by their key. */
        CommitStatusSelector: {
            /** @default  */
            key: string;
        };
        /** @description CommitStatusSpec defines the desired state of CommitStatus */
        CommitStatusSpec: {
            /**
             * @description Description is a human-readable description of the commit status. This is shown in the SCM provider (GitHub, GitLab, etc.) as the commit status description. Use an action-oriented message to convey that the system is actively working. For example, "Waiting for approval" instead of "Approval pending."
             * @default
             */
            description: string;
            /** @default {} */
            gitRepositoryRef: components["schemas"]["io_argoproj_promoter_v1alpha1_ObjectReference"];
            /**
             * @description Name is the name of the commit status.
             * @default
             */
            name: string;
            /**
             * @description Phase is the state of the commit status. This will be mapped to the appropriate equivalent in the SCM.
             * @default
             */
            phase: string;
            /**
             * @description SHA is the commit SHA to set the status on. Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
             * @default
             */
            sha: string;
            /** @description Url is a URL that the user can follow to see more details about the status */
            url?: string;
        };
        /** @description CommitStatusStatus defines the observed state of CommitStatus */
        CommitStatusStatus: {
            /** @description Conditions Represents the observations of the current state. */
            conditions?: components["schemas"]["Condition"][];
            /** @description Id is the unique identifier of the commit status, set by the SCM */
            id?: string;
            /** @description InstanceID mirrors metadata.labels[promoter.argoproj.io/instance-id] stamped on each reconcile attempt by this install's controller, including when Ready=False; omitted when the resource has no instance-id label (default install). */
            instanceID?: string;
            /**
             * Format: int64
             * @description ObservedGeneration is the .metadata.generation that this status was reconciled from. Because status is written via Server-Side Apply with ForceOwnership (which has no optimistic-concurrency check), this field is the canonical way to detect stale status writes: compare status.observedGeneration with metadata.generation.
             */
            observedGeneration?: number;
            /** @description Phase is the state of the commit status. This will be mapped to the appropriate equivalent in the SCM. */
            phase?: string;
            /** @description Sha is the commit SHA that the status is set on. Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats. */
            sha?: string;
        };
        /** @description Condition contains details for one aspect of the current state of this API Resource. */
        Condition: {
            /** @description lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable. */
            lastTransitionTime: components["schemas"]["Time"];
            /**
             * @description message is a human readable message indicating details about the transition. This may be an empty string.
             * @default
             */
            message: string;
            /**
             * Format: int64
             * @description observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
             */
            observedGeneration?: number;
            /**
             * @description reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
             * @default
             */
            reason: string;
            /**
             * @description status of the condition, one of True, False, Unknown.
             * @default
             */
            status: string;
            /**
             * @description type of condition in CamelCase or in foo.example.com/CamelCase.
             * @default
             */
            type: string;
        };
        /** @description CronWindow defines a recurring time window using a cron expression and a duration. */
        CronWindow: {
            /**
             * @description Cron is a standard 5-field cron expression defining the start of the window (minute hour day-of-month month day-of-week). For example "0 9 * * 1-5" means Monday–Friday at 09:00 in the configured timezone.
             * @default
             */
            cron: string;
            /** @description Description is an optional human-readable explanation of this window (e.g. "US East business hours", "Holiday deployment freeze"). */
            description?: string;
            /** @description Duration is how long the window remains open after the cron trigger. The duration should be in a format accepted by Go's time.ParseDuration function, e.g., "8h", "30m", "2h30m". */
            duration: components["schemas"]["Duration"];
            /** @description Timezone overrides the spec-level default timezone for this specific window. If not set, the spec-level timezone (or UTC if that is also not set) is used. */
            timezone?: string;
        };
        /** @description DAGCommitStatus is the Schema for the dagcommitstatuses API */
        DAGCommitStatus: {
            /** @description APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
            apiVersion?: string;
            /** @description Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
            kind?: string;
            /**
             * @description metadata is a standard object metadata
             * @default {}
             */
            metadata?: components["schemas"]["ObjectMeta"];
            /**
             * @description spec defines the desired state of DAGCommitStatus
             * @default {}
             */
            spec: components["schemas"]["DAGCommitStatusSpec"];
            /**
             * @description status defines the observed state of DAGCommitStatus
             * @default {}
             */
            status?: components["schemas"]["DAGCommitStatusStatus"];
        };
        /** @description DAGCommitStatusSpec defines the desired state of DAGCommitStatus. */
        DAGCommitStatusSpec: {
            /** @description Environments declares the promotion dependency graph. Each entry names an environment branch and the upstream branches it depends on. An environment becomes eligible for promotion once all of its dependsOn upstreams are satisfied. An entry with no dependsOn is a graph root. The graph must be acyclic; cycles and references to unknown branches are rejected. */
            environments: components["schemas"]["DAGEnvironment"][];
            /**
             * @description Key is the commit status key referenced in the PromotionStrategy's proposedCommitStatuses. It must match a key declared there so the gate this controller produces is enforced. Must be lowercase alphanumeric with hyphens, 1–63 characters (pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$).
             * @default
             */
            key: string;
            /**
             * @description PromotionStrategyRef is a reference to the promotion strategy that this DAG commit status applies to. The controller watches this PromotionStrategy and, for each environment, reports whether the environment's upstream dependencies (as declared in Environments) are satisfied.
             * @default {}
             */
            promotionStrategyRef: components["schemas"]["io_argoproj_promoter_v1alpha1_ObjectReference"];
            /**
             * @description URL generates the URL to use on the per-environment CommitStatus (SCM details link), for example a link into the Promoter UI that highlights this environment's dependsOn upstreams. Optional; when empty, no URL is set on the child CommitStatus. The template receives .Environment, .DAGCommitStatus, .PromotionStrategy, .DependsOn, and .DependsOnQuery (see controller docs).
             * @default {}
             */
            url?: components["schemas"]["URLConfig"];
        };
        /** @description DAGCommitStatusStatus defines the observed state of DAGCommitStatus. */
        DAGCommitStatusStatus: {
            /**
             * @description conditions represent the current state of the DAGCommitStatus resource. Each condition has a unique type and reflects the status of a specific aspect of the resource.
             *
             *     Standard condition types include: - "Available": the resource is fully functional - "Progressing": the resource is being created or updated - "Degraded": the resource failed to reach or maintain its desired state
             *
             *     The status of each condition is one of True, False, or Unknown.
             */
            conditions?: components["schemas"]["Condition"][];
            /** @description InstanceID mirrors metadata.labels[promoter.argoproj.io/instance-id] stamped on each reconcile attempt by this install's controller, including when Ready=False; omitted when the resource has no instance-id label (default install). */
            instanceID?: string;
            /**
             * Format: int64
             * @description ObservedGeneration is the .metadata.generation that this status was reconciled from. Because status is written via Server-Side Apply with ForceOwnership (which has no optimistic-concurrency check), this field is the canonical way to detect stale status writes: compare status.observedGeneration with metadata.generation.
             */
            observedGeneration?: number;
        };
        /** @description DAGEnvironment is a single node in the promotion dependency graph. */
        DAGEnvironment: {
            /**
             * @description Branch is the name of the active branch for the environment. It must match a branch declared in the referenced PromotionStrategy's environments. Must not start with '-', contain ':', or contain '..'.
             * @default
             */
            branch: string;
            /** @description DependsOn is the list of upstream branches this environment depends on. The environment is only eligible for promotion once every branch listed here is satisfied. An empty or omitted list makes this environment a root of the graph. Each item must not start with '-', contain ':', or contain '..'. */
            dependsOn?: string[];
        };
        /** @description Duration is a wrapper around time.Duration which supports correct marshaling to YAML and JSON. In particular, it marshals into strings, which can be used as map keys in json. */
        Duration: string;
        /** @description Environment defines a single environment in the promotion sequence. */
        Environment: {
            /**
             * @description ActiveCommitStatuses are commit statuses describing an actively running dry commit. If an active commit status is failing for an environment, subsequent environments will not deploy the failing commit.
             *
             *     The commit statuses specified in this field apply to this environment only. You can also specify commit statuses for all environments in the `spec.activeCommitStatuses` field.
             */
            activeCommitStatuses?: components["schemas"]["CommitStatusSelector"][];
            /** @description ActivePath optionally overrides the strategy-level activePath for this environment. When set, this environment's CTP uses this path instead of spec.activePath. */
            activePath?: string;
            /** @description AutoMerge determines whether the dry commit should be automatically merged into the next branch in the sequence. If false, the dry commit will be proposed but not merged. */
            autoMerge?: boolean;
            /**
             * @description Branch is the name of the active branch for the environment. Must not start with '-', contain ':', or contain '..'.
             * @default
             */
            branch: string;
            /**
             * @description ProposedCommitStatuses are commit statuses describing a proposed dry commit, i.e. one that is not yet running in a live environment. If a proposed commit status is failing for a given environment, the dry commit will not be promoted to that environment.
             *
             *     The commit statuses specified in this field apply to this environment only. You can also specify commit statuses for all environments in the `spec.proposedCommitStatuses` field.
             */
            proposedCommitStatuses?: components["schemas"]["CommitStatusSelector"][];
        };
        /** @description EnvironmentStatus defines the observed state of an environment in a PromotionStrategy. */
        EnvironmentStatus: {
            /**
             * @description Active is the state of the active branch for the environment.
             * @default {}
             */
            active: components["schemas"]["CommitBranchState"];
            /**
             * @description Branch is the name of the active branch for the environment.
             * @default
             */
            branch: string;
            /** @description History defines the history of promoted changes done by the PromotionStrategy for each environment. You can think of it as a list of PRs merged by GitOps Promoter. It will not include changes that were manually merged. The history length is hard-coded to be at most 5 entries. This may change in the future. History is constructed on a best-effort basis and should be used for informational purposes only. History is in reverse chronological order (newest is first). */
            history?: components["schemas"]["History"][];
            /** @description LastHealthyDryShas is a list of dry commits that were observed to be healthy in the environment. */
            lastHealthyDryShas: components["schemas"]["HealthyDryShas"][];
            /**
             * @description Proposed is the state of the proposed branch for the environment.
             * @default {}
             */
            proposed: components["schemas"]["CommitBranchState"];
            /** @description PullRequest is the state of the pull request that was created for this environment. */
            pullRequest?: components["schemas"]["PullRequestCommonStatus"];
        };
        /** @description Fake is a placeholder for a fake SCM provider, used for testing purposes. */
        Fake: {
            /** @description Domain is the domain of the fake SCM provider. This is used for testing purposes. */
            domain?: string;
        };
        /** @description FakeRepo is a placeholder for a repository in the fake SCM provider, used for testing purposes. */
        FakeRepo: {
            /**
             * @description Name is the name of the repository.
             * @default
             */
            name: string;
            /**
             * @description Owner is the owner of the repository.
             * @default
             */
            owner: string;
        };
        /**
         * @description FieldsV1 stores a set of fields in a data structure like a Trie, in JSON format.
         *
         *     Each key is either a '.' representing the field itself, and will always map to an empty set, or a string representing a sub-field or item. The string will follow one of these four formats: 'f:<name>', where <name> is the name of a field in a struct, or key in a map 'v:<value>', where <value> is the exact json formatted value of a list item 'i:<index>', where <index> is position of a item in a list 'k:<keys>', where <keys> is a map of  a list item's key fields to their unique values If a key maps to an empty Fields value, the field that key represents is part of the set.
         *
         *     The exact format is defined in sigs.k8s.io/structured-merge-diff
         */
        FieldsV1: Record<string, never>;
        /** @description Forgejo is a Forgejo SCM provider configuration. It is used to configure the Forgejo settings. */
        Forgejo: {
            /**
             * @description Domain is the Forgejo domain, such as "codeberg.org" or "forgejo.mycompany.com". There is no default domain since Forgejo is not a service like Gitlab or Github.
             * @default
             */
            domain: string;
        };
        /** @description ForgejoRepo is a repository in Forgejo, identified by its owner and name. */
        ForgejoRepo: {
            /**
             * @description Name is the name of the repository.
             * @default
             */
            name: string;
            /**
             * @description Owner is the owner of the repository.
             * @default
             */
            owner: string;
        };
        /**
         * @description GitCommitStatus is the Schema for the gitcommitstatuses API.
         *
         *     It validates commits from PromotionStrategy environments using configurable expressions and creates CommitStatus resources with the validation results.
         *
         *     Use the Target field to control which commit is validated: - "active" (default): Validates the currently deployed commit - "proposed": Validates the incoming commit
         *
         *     The validation result is always reported on the PROPOSED commit to enable promotion gating, regardless of which commit was validated.
         *
         *     Workflow:
         *      1. Controller reads PromotionStrategy to get ProposedHydratedSha and ActiveHydratedSha
         *      2. Controller selects SHA to validate based on Target field
         *      3. Controller fetches commit data (subject, body, author, trailers) for selected SHA
         *      4. Controller evaluates expression against selected commit data
         *      5. Controller creates/updates CommitStatus with result attached to PROPOSED SHA
         *      6. PromotionStrategy checks CommitStatus on PROPOSED SHA before allowing promotion
         *
         *     Common use cases:
         *       - "Ensure active commit is not a revert before promoting"
         */
        GitCommitStatus: {
            /** @description APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
            apiVersion?: string;
            /** @description Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
            kind?: string;
            /**
             * @description metadata is a standard object metadata
             * @default {}
             */
            metadata?: components["schemas"]["ObjectMeta"];
            /**
             * @description spec defines the desired state of GitCommitStatus
             * @default {}
             */
            spec: components["schemas"]["GitCommitStatusSpec"];
            /**
             * @description status defines the observed state of GitCommitStatus
             * @default {}
             */
            status?: components["schemas"]["GitCommitStatusStatus"];
        };
        /** @description GitCommitStatusEnvironmentStatus defines the observed validation status for a specific environment. */
        GitCommitStatusEnvironmentStatus: {
            /** @description ActiveHydratedSha is the currently active (deployed) hydrated commit SHA that was validated. This comes from the PromotionStrategy's environment status. The expression is evaluated against THIS commit's data, not the proposed commit. May be empty if the PromotionStrategy hasn't reconciled yet. Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats. */
            activeHydratedSha?: string;
            /**
             * @description Branch is the environment branch name being validated.
             * @default
             */
            branch: string;
            /** @description ExpressionResult contains the boolean result of the expression evaluation. Only set when the expression successfully evaluates to a boolean. nil indicates the expression has not yet been evaluated, failed to compile, or failed to evaluate. */
            expressionResult?: boolean;
            /**
             * @description Phase represents the current validation state of the commit. - "pending": validation has not completed, commit data is not yet available, or SHAs are empty - "success": expression evaluated to true, validation passed - "failure": expression evaluated to false, validation failed, or expression compilation failed
             * @default
             */
            phase: string;
            /**
             * @description ProposedHydratedSha is the proposed hydrated commit SHA where the validation result is reported. This comes from the PromotionStrategy's environment status. The CommitStatus resource is created with this SHA, allowing the PromotionStrategy to gate promotions based on the validation of the ACTIVE commit. May be empty if the PromotionStrategy hasn't reconciled yet. Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
             * @default
             */
            proposedHydratedSha: string;
            /** @description TargetedSha is the commit SHA that was actually validated by the expression. This will match either ProposedHydratedSha or ActiveHydratedSha depending on the Target setting ("proposed" or "active"). This field clarifies which commit's data was used in the expression evaluation. Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats. */
            targetedSha?: string;
        };
        /** @description GitCommitStatusSpec defines the desired state of GitCommitStatus */
        GitCommitStatusSpec: {
            /** @description Description is a human-readable description of this validation that will be shown in the SCM provider (GitHub, GitLab, etc.) as the commit status description. If not specified, defaults to empty string. */
            description?: string;
            /**
             * @description Expression is evaluated using the expr library (github.com/expr-lang/expr) against commit data for environments in the referenced PromotionStrategy. The expression must return a boolean value where true indicates the validation passed.
             *
             *     The commit validated is determined by the Target field: - "active" (default): Validates the ACTIVE (currently deployed) commit - "proposed": Validates the PROPOSED commit (what will be promoted)
             *
             *     The validation result is always reported on the PROPOSED commit to enable promotion gating.
             *
             *     Use Cases by Mode:
             *       Active mode: State-based gating - validate current environment before allowing promotion
             *         - "Don't promote until active commit has required sign-offs"
             *         - "Verify current deployment meets compliance before next promotion"
             *         - "Ensure active commit is not a revert before promoting"
             *
             *       Proposed mode: Change-based gating - validate the incoming change itself
             *         - "Don't promote unless new commit follows naming convention"
             *         - "Ensure proposed commit has proper JIRA ticket reference"
             *         - "Require specific author for proposed changes"
             *
             *     Available variables in the expression context:
             *       - Commit.SHA (string): the commit SHA being validated (active or proposed based on Target)
             *       - Commit.Subject (string): the first line of the commit message
             *       - Commit.Body (string): the commit message body (everything after the subject line)
             *       - Commit.Author (string): commit author email address
             *       - Commit.Trailers (map[string][]string): git trailers parsed from commit message
             * @default
             */
            expression: string;
            /**
             * @description Key is the unique identifier for this validation rule. It is used as the commit status key and in status messages. This key is matched against PromotionStrategy's proposedCommitStatuses or activeCommitStatuses to determine which environments this validation applies to.
             * @default
             */
            key: string;
            /**
             * @description PromotionStrategyRef is a reference to the promotion strategy that this commit status applies to. The controller will validate commits from ALL environments in the referenced PromotionStrategy where this GitCommitStatus.Spec.Key matches an entry in either:
             *       - PromotionStrategy.Spec.ProposedCommitStatuses (applies to all environments), OR
             *       - Environment.ProposedCommitStatuses (applies to specific environment)
             * @default {}
             */
            promotionStrategyRef: components["schemas"]["io_argoproj_promoter_v1alpha1_ObjectReference"];
            /**
             * @description Target specifies which commit SHA to validate with the expression. - "active": Validates the currently active/deployed commit (default behavior) - "proposed": Validates the proposed commit that will be promoted
             *
             *     The validation result is always reported on the PROPOSED commit (for gating), but this field controls which commit's data is used in the expression evaluation.
             *
             *     Examples:
             *       target: "active" - "Don't promote if a revert commit is detected"
             *       target: "proposed" - "Don't promote unless new commit follows naming convention"
             */
            target?: string;
        };
        /** @description GitCommitStatusStatus defines the observed state of GitCommitStatus. */
        GitCommitStatusStatus: {
            /** @description Conditions represent the latest available observations of the GitCommitStatus's state. Standard condition types include "Ready" which aggregates the status of all environments. */
            conditions?: components["schemas"]["Condition"][];
            /**
             * @description Environments holds the validation results for each environment where this validation applies. Each entry corresponds to an environment from the PromotionStrategy where the Key matches either global or environment-specific proposedCommitStatuses.
             *
             *     The controller validates the commit specified by Target ("active" or "proposed") but the CommitStatus is always reported on the PROPOSED commit for promotion gating. Each environment entry tracks both the ProposedHydratedSha (where status is reported) and the ActiveHydratedSha, with TargetedSha indicating which one was actually evaluated.
             */
            environments?: components["schemas"]["GitCommitStatusEnvironmentStatus"][];
            /** @description InstanceID mirrors metadata.labels[promoter.argoproj.io/instance-id] stamped on each reconcile attempt by this install's controller, including when Ready=False; omitted when the resource has no instance-id label (default install). */
            instanceID?: string;
            /**
             * Format: int64
             * @description ObservedGeneration is the .metadata.generation that this status was reconciled from. Because status is written via Server-Side Apply with ForceOwnership (which has no optimistic-concurrency check), this field is the canonical way to detect stale status writes: compare status.observedGeneration with metadata.generation.
             */
            observedGeneration?: number;
        };
        /** @description GitHub is a GitHub SCM provider configuration. It is used to configure the GitHub settings. */
        GitHub: {
            /**
             * Format: int64
             * @description AppID is the GitHub App ID.
             * @default 0
             */
            appID: number;
            /** @description Domain is the GitHub domain, such as "github.mycompany.com". If using the default GitHub domain, leave this field empty. */
            domain?: string;
            /**
             * Format: int64
             * @description InstallationID is the GitHub App Installation ID. If you want to use this ScmProvider for multiple GitHub orgs, do not specify this field. The installation ID will be inferred from the repo owner when needed.
             */
            installationID?: number;
        };
        /** @description GitHubRepo is a repository in GitHub, identified by its owner and name. */
        GitHubRepo: {
            /**
             * @description Name is the name of the repository.
             * @default
             */
            name: string;
            /**
             * @description Owner is the owner of the repository, which can be a user or an organization.
             * @default
             */
            owner: string;
        };
        /** @description GitLab is a GitLab SCM provider configuration. It is used to configure the GitLab settings. */
        GitLab: {
            /** @description Domain is the GitLab domain, such as "gitlab.mycompany.com". If using the default GitLab domain, leave this field empty. */
            domain?: string;
        };
        /** @description GitLabRepo is a repository in GitLab, identified by its namespace, name, and project ID. */
        GitLabRepo: {
            /**
             * @description Name is the project slug of the repository.
             * @default
             */
            name: string;
            /**
             * @description Namespace is the user, group or group with subgroup (e.g. group/subgroup).
             * @default
             */
            namespace: string;
            /**
             * Format: int32
             * @description ProjectID is the ID of the project in GitLab.
             * @default 0
             */
            projectId: number;
        };
        /** @description GitRepository is the Schema for the gitrepositories API */
        GitRepository: {
            /** @description APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
            apiVersion?: string;
            /** @description Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
            kind?: string;
            /** @default {} */
            metadata?: components["schemas"]["ObjectMeta"];
            /** @default {} */
            spec?: components["schemas"]["GitRepositorySpec"];
            /** @default {} */
            status?: components["schemas"]["GitRepositoryStatus"];
        };
        /** @description GitRepositorySpec defines the desired state of GitRepository */
        GitRepositorySpec: {
            azureDevOps?: components["schemas"]["AzureDevOpsRepo"];
            bitbucketCloud?: components["schemas"]["BitbucketCloudRepo"];
            fake?: components["schemas"]["FakeRepo"];
            forgejo?: components["schemas"]["ForgejoRepo"];
            gitea?: components["schemas"]["GiteaRepo"];
            github?: components["schemas"]["GitHubRepo"];
            gitlab?: components["schemas"]["GitLabRepo"];
            /** @default {} */
            scmProviderRef: components["schemas"]["ScmProviderObjectReference"];
        };
        /** @description GitRepositoryStatus defines the observed state of GitRepository */
        GitRepositoryStatus: {
            /** @description Conditions Represents the observations of the current state. */
            conditions?: components["schemas"]["Condition"][];
            /** @description InstanceID mirrors metadata.labels[promoter.argoproj.io/instance-id] stamped on each reconcile attempt by this install's controller, including when Ready=False; omitted when the resource has no instance-id label (default install). */
            instanceID?: string;
            /**
             * Format: int64
             * @description ObservedGeneration is the .metadata.generation that this status was reconciled from. Because status is written via Server-Side Apply with ForceOwnership (which has no optimistic-concurrency check), this field is the canonical way to detect stale status writes: compare status.observedGeneration with metadata.generation.
             */
            observedGeneration?: number;
        };
        /** @description Gitea is a Gitea SCM provider configuration. It is used to configure the Gitea settings. */
        Gitea: {
            /**
             * @description Domain is the Gitea domain, such as "gitea.com" or "gitea.mycompany.com". There is no default domain since Gitea is self-hosted.
             * @default
             */
            domain: string;
        };
        /** @description GiteaRepo is a repository in Gitea, identified by its owner and name. */
        GiteaRepo: {
            /**
             * @description Name is the name of the repository.
             * @default
             */
            name: string;
            /**
             * @description Owner is the owner of the repository.
             * @default
             */
            owner: string;
        };
        /**
         * @description HTTPAuthentication defines authentication options for HTTP requests.
         *
         *     Only one authentication method should be specified.
         *
         *     Authentication methods:
         *
         *      1. Basic Auth - Traditional username/password authentication
         *         Applied as: Authorization: Basic <base64(username:password)>
         *
         *      2. Bearer Token - Token-based authentication
         *         Applied as: Authorization: Bearer <token>
         *
         *      3. OAuth2 - Automatically obtains and refreshes access tokens using client credentials flow
         *         Applied as: Authorization: Bearer <access-token>
         *
         *      4. TLS - Mutual TLS authentication using client certificates
         *         Applied at: Transport layer (not as HTTP header)
         *
         *      5. SCM - Uses credentials from the SCM provider referenced in the PromotionStrategy.
         *         Automatically applies the appropriate authentication method based on the SCM provider type.
         */
        HTTPAuthentication: {
            /** @description Basic specifies HTTP Basic Authentication. Credentials can be provided inline (with secret references) or via secretRef. */
            basic?: components["schemas"]["BasicAuth"];
            /** @description Bearer specifies Bearer token authentication. Token can be provided inline (with secret reference) or via secretRef. */
            bearer?: components["schemas"]["BearerAuth"];
            /** @description OAuth2 specifies OAuth2 client credentials authentication. The controller will automatically obtain access tokens from the specified tokenURL. */
            oauth2?: components["schemas"]["OAuth2Auth"];
            /** @description Scm specifies authentication using credentials from the SCM provider. This uses the credentials configured in the ScmProvider referenced by the PromotionStrategy, applying the appropriate authentication method based on the SCM provider type (GitHub App, GitLab token, Azure DevOps PAT, etc.). To use this auth type, just set it to an empty object, i.e. scm: {}. */
            scm?: components["schemas"]["Scm"];
            /** @description TLS specifies TLS client certificate authentication (mutual TLS). Requires a secret containing the client certificate and private key. */
            tls?: components["schemas"]["TLSAuth"];
        };
        /**
         * @description HTTPRequestSpec defines the HTTP request configuration.
         *
         *     URLTemplate, HeaderTemplates, and BodyTemplate support Go templates. Sprig functions are available except env, expandenv, and getHostByName; urlQueryEscape is also available. These fields are rendered with previous-attempt data: they are evaluated before the current HTTP request is made, so they never contain the response from the request being built. Use TriggerOutput/ResponseOutput for state from the previous run (trigger mode only).
         *
         *     Template variables:
         *       - {{ .Branch }}: the environment branch currently being processed (empty for shared HTTP request in promotionstrategy context)
         *       - {{ .Phase }}: phase from previous reconcile (success/pending/failure, defaults to pending); in promotionstrategy context, aggregate of all branches
         *       - {{ .PromotionStrategy }}: full PromotionStrategy object (use with Branch to look up per-environment data)
         *       - {{ .WebRequestCommitStatus }}: full WebRequestCommitStatus spec and status (snapshot from previous reconcile)
         *       - {{ .NamespaceMetadata.Labels }}: map of labels from the namespace
         *       - {{ .NamespaceMetadata.Annotations }}: map of annotations from the namespace
         *       - {{ index .TriggerOutput "key" }}, {{ index .ResponseOutput "key" }}: (trigger mode only) from previous reconcile
         *       - {{ index .SuccessOutput "key" }}: custom data from the previous success.when.output.expression evaluation
         *       - {{ index .TriggerVariables "key" }}: (trigger mode only) result of trigger.when.variables.expression for this reconcile
         *
         *     Example: "https://api.example.com/validate/{{ range .PromotionStrategy.Status.Environments }}{{ if eq .Branch $.Branch }}{{ .Proposed.Hydrated.Sha }}{{ end }}{{ end }}"
         *
         *     Exactly one of Method or MethodTemplate must be set.
         */
        HTTPRequestSpec: {
            /**
             * @description Authentication specifies authentication configuration for the HTTP request.
             *
             *     Supports multiple authentication methods: - Basic Auth: HTTP Basic Authentication with username/password - Bearer Token: Bearer token authentication (e.g., API keys, JWTs) - OAuth2: OAuth2 client credentials flow for obtaining access tokens - TLS: Mutual TLS (mTLS) with client certificates - SCM: Reuses credentials from the ScmProvider referenced by the PromotionStrategy (no extra secret needed)
             *
             *     For Basic, Bearer, OAuth2, and TLS, credentials must be stored in Kubernetes secrets and referenced via secretRef fields. For SCM, credentials are obtained automatically from the SCM provider; just set scm: {}.
             *
             *     Examples:
             *       # Basic Auth
             *       authentication:
             *         basic:
             *           secretRef:
             *             name: my-creds
             *
             *       # Bearer Token
             *       authentication:
             *         bearer:
             *           secretRef:
             *             name: api-token
             *
             *       # OAuth2 Client Credentials
             *       authentication:
             *         oauth2:
             *           tokenURL: "https://auth.example.com/oauth/token"
             *           secretRef:
             *             name: oauth-creds
             *
             *       # TLS Client Certificate
             *       authentication:
             *         tls:
             *           secretRef:
             *             name: my-tls-cert
             *
             *       # SCM Provider Credentials
             *       authentication:
             *         scm: {}
             */
            authentication?: components["schemas"]["HTTPAuthentication"];
            /** @description BodyTemplate is the request body to send. Supports Go templates (see HTTPRequestSpec for available variables). */
            bodyTemplate?: string;
            /** @description HeaderTemplates are additional HTTP headers to include in the request. The map key is the header name and the value is the header value (supports Go templates). See HTTPRequestSpec for available template variables. */
            headerTemplates?: {
                [key: string]: string;
            };
            /**
             * @description Method is the static HTTP method to use. Mutually exclusive with MethodTemplate.
             *
             *     Deprecated: Use MethodTemplate instead. A literal value such as `methodTemplate: GET` behaves identically to `method: GET` and avoids needing two separate fields. Existing resources that set Method continue to work, but new resources should set MethodTemplate. Method may be removed in a future release.
             */
            method?: string;
            /** @description MethodTemplate is the HTTP method, rendered as a Go template with the same variables and Sprig functions as URLTemplate/BodyTemplate/HeaderTemplates. The rendered string is trimmed of surrounding whitespace and uppercased; the final value must be one of GET/POST/PUT/PATCH/DELETE or the reconcile returns an error. A literal value such as `methodTemplate: GET` works identically to a static method; use templating when the method must vary by reconcile state (e.g. issuing a search GET on one reconcile and a close POST on the next). Mutually exclusive with Method (deprecated). */
            methodTemplate?: string;
            /** @description Timeout is the maximum time to wait for the HTTP request to complete. */
            timeout?: components["schemas"]["Duration"];
            /**
             * @description URLTemplate is the HTTP endpoint to request. Supports Go templates (see HTTPRequestSpec for available variables).
             * @default
             */
            urlTemplate: string;
        };
        /** @description HealthyDryShas is a list of dry commits that were observed to be healthy in the environment. */
        HealthyDryShas: {
            /**
             * @description Sha is the commit SHA of the dry commit that was observed to be healthy. Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
             * @default
             */
            sha: string;
            /** @description Time is the time when the proposed commit for the given dry SHA was merged into the active branch. */
            time: components["schemas"]["Time"];
        };
        /** @description History describes a particular change that was promoted by the ChangeTransferPolicy. */
        History: {
            /**
             * @description Active is the state of the active branch at the time the PR was merged.
             * @default {}
             */
            active?: components["schemas"]["CommitBranchState"];
            /**
             * @description Proposed is the state of the proposed branch at the time the PR was merged.
             * @default {}
             */
            proposed?: components["schemas"]["CommitBranchStateHistoryProposed"];
            /** @description PullRequest is the state of the pull request that was created for this ChangeTransferPolicy. */
            pullRequest?: components["schemas"]["PullRequestCommonStatus"];
        };
        /** @description HydratorMetadata contains metadata about the hydrated commit. This is extracted from the git note or metadata file. */
        HydratorMetadata: {
            /** @description Author is the author of the dry commit that was used to hydrate the branch. */
            author?: string;
            /** @description Body is the body of the dry commit that was used to hydrate the branch without the subject. */
            body?: string;
            /** @description Date is the date of the dry commit that was used to hydrate the branch. */
            date?: components["schemas"]["Time"];
            /** @description DrySha is the SHA of the commit that was used as the dry source for hydration. Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats. */
            drySha?: string;
            /** @description References are the references to other commits, that went into the hydration of the branch. */
            references?: components["schemas"]["RevisionReference"][];
            /** @description RepoURL is the URL of the repository where the commit is located. */
            repoURL?: string;
            /** @description Subject is the subject line of the dry commit that was used to hydrate the branch. */
            subject?: string;
        };
        /** @description JSON represents any valid JSON value. These types are supported: bool, int64, float64, string, []interface{}, map[string]interface{} and nil. */
        JSON: unknown;
        /** @description A label selector is a label query over a set of resources. The result of matchLabels and matchExpressions are ANDed. An empty label selector matches all objects. A null label selector matches no objects. */
        LabelSelector: {
            /** @description matchExpressions is a list of label selector requirements. The requirements are ANDed. */
            matchExpressions?: components["schemas"]["LabelSelectorRequirement"][];
            /** @description matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed. */
            matchLabels?: {
                [key: string]: string;
            };
        };
        /** @description A label selector requirement is a selector that contains values, a key, and an operator that relates the key and values. */
        LabelSelectorRequirement: {
            /**
             * @description key is the label key that the selector applies to.
             * @default
             */
            key: string;
            /**
             * @description operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
             * @default
             */
            operator: string;
            /** @description values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch. */
            values?: string[];
        };
        /** @description ListMeta describes metadata that synthetic resources must have, including lists and various status objects. A resource may have only one of {ObjectMeta, ListMeta}. */
        ListMeta: {
            /** @description continue may be set if the user set a limit on the number of items returned, and indicates that the server has more data available. The value is opaque and may be used to issue another request to the endpoint that served this list to retrieve the next set of available objects. Continuing a consistent list may not be possible if the server configuration has changed or more than a few minutes have passed. The resourceVersion field returned when using this continue value will be identical to the value in the first response, unless you have received this token from an error message. */
            continue?: string;
            /**
             * Format: int64
             * @description remainingItemCount is the number of subsequent items in the list which are not included in this list response. If the list request contained label or field selectors, then the number of remaining items is unknown and the field will be left unset and omitted during serialization. If the list is complete (either because it is not chunking or because this is the last chunk), then there are no more remaining items and this field will be left unset and omitted during serialization. Servers older than v1.15 do not set this field. The intended use of the remainingItemCount is *estimating* the size of a collection. Clients should not rely on the remainingItemCount to be set or to be exact.
             */
            remainingItemCount?: number;
            /** @description String that identifies the server's internal version of this object that can be used by clients to determine when objects have changed. Value must be treated as opaque by clients and passed unmodified back to the server. Populated by the system. Read-only. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency */
            resourceVersion?: string;
            /** @description Deprecated: selfLink is a legacy read-only field that is no longer populated by the system. */
            selfLink?: string;
            /**
             * @description shardInfo is set when the list is a filtered subset of the full collection, as selected by a shard selector on the request. It echoes back the selector so clients can verify which shard they received and merge sharded responses. Clients should not cache sharded list responses as a full representation of the collection.
             *
             *     This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
             */
            shardInfo?: components["schemas"]["ShardInfo"];
        };
        /** @description LocalObjectReference contains enough information to let you locate the referenced object inside the same namespace. */
        LocalObjectReference: {
            /**
             * @description Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
             * @default
             */
            name?: string;
        };
        /** @description ManagedFieldsEntry is a workflow-id, a FieldSet and the group version of the resource that the fieldset applies to. */
        ManagedFieldsEntry: {
            /** @description APIVersion defines the version of this resource that this field set applies to. The format is "group/version" just like the top-level APIVersion field. It is necessary to track the version of a field set because it cannot be automatically converted. */
            apiVersion?: string;
            /** @description FieldsType is the discriminator for the different fields format and version. There is currently only one possible value: "FieldsV1" */
            fieldsType?: string;
            /** @description FieldsV1 holds the first JSON version format as described in the "FieldsV1" type. */
            fieldsV1?: components["schemas"]["FieldsV1"];
            /** @description Manager is an identifier of the workflow managing these fields. */
            manager?: string;
            /** @description Operation is the type of operation which lead to this ManagedFieldsEntry being created. The only valid values for this field are 'Apply' and 'Update'. */
            operation?: string;
            /** @description Subresource is the name of the subresource used to update that object, or empty string if the object was updated through the main resource. The value of this field is used to distinguish between managers, even if they share the same name. For example, a status update will be distinct from a regular update using the same manager name. Note that the APIVersion field is not related to the Subresource field and it always corresponds to the version of the main resource. */
            subresource?: string;
            /** @description Time is the timestamp of when the ManagedFields entry was added. The timestamp will also be updated if a field is added, the manager changes any of the owned fields value or removes a field. The timestamp does not update when a field is removed from the entry because another manager took it over. */
            time?: components["schemas"]["Time"];
        };
        /**
         * @description ModeSpec defines how the WebRequestCommitStatus controller issues HTTP requests.
         *
         *     Exactly one of Polling or Trigger must be set.
         *
         *     Context (the context field below) controls request fan-out and what data is available in templates and trigger expressions:
         *
         *       - "environments" (default): one HTTP request per environment; each environment has its own phase and status; success.when.expression is evaluated per response and must return a boolean (true → success, false → pending; failure is not expressible).
         *
         *       - "promotionstrategy": at most one HTTP request per WebRequestCommitStatus resource; CommitStatuses remain one per environment on each environment's reportOn SHA. success.when.expression runs once on that shared response — see WhenWithOutputSpec.Expression for boolean vs per-branch object return shapes.
         *
         *     When context is "promotionstrategy", Branch is empty for the shared HTTP request and trigger expressions. Use PromotionStrategy (e.g. status environments) for branch-specific values. For description and url templates, {{ .Branch }} and {{ .Phase }} are set per environment when rendering that environment's CommitStatus.
         */
        ModeSpec: {
            /** @description Context is "environments" (default) or "promotionstrategy". See the ModeSpec type documentation for behavior, template limits, and success expression rules. */
            context?: string;
            /** @description Polling enables interval-based polling mode. The controller will poll the HTTP endpoint at the specified interval. */
            polling?: components["schemas"]["PollingModeSpec"];
            /** @description Trigger enables expression-based triggering mode. The controller will evaluate the expression to determine when to make HTTP requests. */
            trigger?: components["schemas"]["TriggerModeSpec"];
        };
        /**
         * @description OAuth2Auth defines OAuth2 client credentials authentication.
         *
         *     The OAuth2 client credentials flow is used for server-to-server authentication. The controller automatically: 1. Requests an access token from the tokenURL using client credentials 2. Caches the token until it expires 3. Automatically refreshes the token when needed 4. Adds the token to requests as: "Authorization: Bearer <access-token>"
         *
         *     This is ideal for: - Service-to-service authentication - APIs that require OAuth2 but don't involve user interaction - Systems that provide machine credentials (client ID/secret)
         *
         *     Example:
         *
         *     	oauth2:
         *     	  tokenURL: "https://auth.example.com/oauth/token"
         *     	  scopes: ["read:api", "write:api"]
         *     	  secretRef:
         *     	    name: oauth-creds
         *
         *     Note: This uses the OAuth2 client credentials grant type (RFC 6749 Section 4.4). It does NOT support authorization code flow or user-interactive flows.
         */
        OAuth2Auth: {
            /** @description Scopes to request from the OAuth2 provider. Optional - some providers don't require scopes for client credentials. Example: ["read:api", "write:api"] */
            scopes?: string[];
            /** @description SecretRef references a secret containing clientID and clientSecret. The secret must contain keys "clientID" and "clientSecret". */
            secretRef: components["schemas"]["LocalObjectReference"];
            /**
             * @description TokenURL is the OAuth2 token endpoint where access tokens are obtained. Example: "https://auth.example.com/oauth/token"
             * @default
             */
            tokenURL: string;
        };
        /** @description ObjectMeta is metadata that all persisted resources must have, which includes all objects users must create. */
        ObjectMeta: {
            /** @description Annotations is an unstructured key value map stored with a resource that may be set by external tools to store and retrieve arbitrary metadata. They are not queryable and should be preserved when modifying objects. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations */
            annotations?: {
                [key: string]: string;
            };
            /**
             * @description CreationTimestamp is a timestamp representing the server time when this object was created. It is not guaranteed to be set in happens-before order across separate operations. Clients may not set this value. It is represented in RFC3339 form and is in UTC.
             *
             *     Populated by the system. Read-only. Null for lists. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            creationTimestamp?: components["schemas"]["Time"];
            /**
             * Format: int64
             * @description Number of seconds allowed for this object to gracefully terminate before it will be removed from the system. Only set when deletionTimestamp is also set. May only be shortened. Read-only.
             */
            deletionGracePeriodSeconds?: number;
            /**
             * @description DeletionTimestamp is RFC 3339 date and time at which this resource will be deleted. This field is set by the server when a graceful deletion is requested by the user, and is not directly settable by a client. The resource is expected to be deleted (no longer visible from resource lists, and not reachable by name) after the time in this field, once the finalizers list is empty. As long as the finalizers list contains items, deletion is blocked. Once the deletionTimestamp is set, this value may not be unset or be set further into the future, although it may be shortened or the resource may be deleted prior to this time. For example, a user may request that a pod is deleted in 30 seconds. The Kubelet will react by sending a graceful termination signal to the containers in the pod. After that 30 seconds, the Kubelet will send a hard termination signal (SIGKILL) to the container and after cleanup, remove the pod from the API. In the presence of network partitions, this object may still exist after this timestamp, until an administrator or automated process can determine the resource is fully terminated. If not set, graceful deletion of the object has not been requested.
             *
             *     Populated by the system when a graceful deletion is requested. Read-only. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            deletionTimestamp?: components["schemas"]["Time"];
            /** @description Must be empty before the object is deleted from the registry. Each entry is an identifier for the responsible component that will remove the entry from the list. If the deletionTimestamp of the object is non-nil, entries in this list can only be removed. Finalizers may be processed and removed in any order.  Order is NOT enforced because it introduces significant risk of stuck finalizers. finalizers is a shared field, any actor with permission can reorder it. If the finalizer list is processed in order, then this can lead to a situation in which the component responsible for the first finalizer in the list is waiting for a signal (field value, external system, or other) produced by a component responsible for a finalizer later in the list, resulting in a deadlock. Without enforced ordering finalizers are free to order amongst themselves and are not vulnerable to ordering changes in the list. */
            finalizers?: string[];
            /**
             * @description GenerateName is an optional prefix, used by the server, to generate a unique name ONLY IF the Name field has not been provided. If this field is used, the name returned to the client will be different than the name passed. This value will also be combined with a unique suffix. The provided value has the same validation rules as the Name field, and may be truncated by the length of the suffix required to make the value unique on the server.
             *
             *     If this field is specified and the generated name exists, the server will return a 409.
             *
             *     Applied only if Name is not specified. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#idempotency
             */
            generateName?: string;
            /**
             * Format: int64
             * @description A sequence number representing a specific generation of the desired state. Populated by the system. Read-only.
             */
            generation?: number;
            /** @description Map of string keys and values that can be used to organize and categorize (scope and select) objects. May match selectors of replication controllers and services. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels */
            labels?: {
                [key: string]: string;
            };
            /** @description ManagedFields maps workflow-id and version to the set of fields that are managed by that workflow. This is mostly for internal housekeeping, and users typically shouldn't need to set or understand this field. A workflow can be the user's name, a controller's name, or the name of a specific apply path like "ci-cd". The set of fields is always in the version that the workflow used when modifying the object. */
            managedFields?: components["schemas"]["ManagedFieldsEntry"][];
            /** @description Name must be unique within a namespace. Is required when creating resources, although some resources may allow a client to request the generation of an appropriate name automatically. Name is primarily intended for creation idempotence and configuration definition. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names */
            name?: string;
            /**
             * @description Namespace defines the space within which each name must be unique. An empty namespace is equivalent to the "default" namespace, but "default" is the canonical representation. Not all objects are required to be scoped to a namespace - the value of this field for those objects will be empty.
             *
             *     Must be a DNS_LABEL. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces
             */
            namespace?: string;
            /** @description List of objects depended by this object. If ALL objects in the list have been deleted, this object will be garbage collected. If this object is managed by a controller, then an entry in this list will point to this controller, with the controller field set to true. There cannot be more than one managing controller. */
            ownerReferences?: components["schemas"]["OwnerReference"][];
            /**
             * @description An opaque value that represents the internal version of this object that can be used by clients to determine when objects have changed. May be used for optimistic concurrency, change detection, and the watch operation on a resource or set of resources. Clients must treat these values as opaque and passed unmodified back to the server. They may only be valid for a particular resource or set of resources.
             *
             *     Populated by the system. Read-only. Value must be treated as opaque by clients and . More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
             */
            resourceVersion?: string;
            /** @description Deprecated: selfLink is a legacy read-only field that is no longer populated by the system. */
            selfLink?: string;
            /**
             * @description UID is the unique in time and space value for this object. It is typically generated by the server on successful creation of a resource and is not allowed to change on PUT operations.
             *
             *     Populated by the system. Read-only. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids
             */
            uid?: string;
        };
        /** @description OutputSpec holds an expression that returns a map/object to persist (e.g. TriggerOutput or ResponseOutput). */
        OutputSpec: {
            /**
             * @description Expression is an expr expression that must return a map/object.
             * @default
             */
            expression: string;
        };
        /** @description OwnerReference contains enough information to let you identify an owning object. An owning object must be in the same namespace as the dependent, or be cluster-scoped, so there is no namespace field. */
        OwnerReference: {
            /**
             * @description API version of the referent.
             * @default
             */
            apiVersion: string;
            /** @description If true, AND if the owner has the "foregroundDeletion" finalizer, then the owner cannot be deleted from the key-value store until this reference is removed. See https://kubernetes.io/docs/concepts/architecture/garbage-collection/#foreground-deletion for how the garbage collector interacts with this field and enforces the foreground deletion. Defaults to false. To set this field, a user needs "delete" permission of the owner, otherwise 422 (Unprocessable Entity) will be returned. */
            blockOwnerDeletion?: boolean;
            /** @description If true, this reference points to the managing controller. */
            controller?: boolean;
            /**
             * @description Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             * @default
             */
            kind: string;
            /**
             * @description Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names
             * @default
             */
            name: string;
            /**
             * @description UID of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids
             * @default
             */
            uid: string;
        };
        /** @description PollingModeSpec defines interval-based polling configuration. */
        PollingModeSpec: {
            /** @description Interval controls how often to retry the HTTP request while in pending state. When reportOn is "proposed": stops polling after success for a given SHA. When reportOn is "active": always polls at this interval. */
            interval?: components["schemas"]["Duration"];
        };
        /** @description PreviousEnvironmentCommitStatus is the Schema for the previousenvironmentcommitstatuses API */
        PreviousEnvironmentCommitStatus: {
            /** @description APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
            apiVersion?: string;
            /** @description Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
            kind?: string;
            /**
             * @description metadata is a standard object metadata
             * @default {}
             */
            metadata?: components["schemas"]["ObjectMeta"];
            /**
             * @description spec defines the desired state of PreviousEnvironmentCommitStatus
             * @default {}
             */
            spec: components["schemas"]["PreviousEnvironmentCommitStatusSpec"];
            /**
             * @description status defines the observed state of PreviousEnvironmentCommitStatus
             * @default {}
             */
            status?: components["schemas"]["PreviousEnvironmentCommitStatusStatus"];
        };
        /** @description PreviousEnvironmentCommitStatusSpec defines the desired state of PreviousEnvironmentCommitStatus. */
        PreviousEnvironmentCommitStatusSpec: {
            /**
             * @description Key is the commit status key referenced in the PromotionStrategy's proposedCommitStatuses. It must match a key declared there so the gate this controller produces is enforced. Must be lowercase alphanumeric with hyphens, 1–63 characters (pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$).
             * @default
             */
            key: string;
            /**
             * @description PromotionStrategyRef is a reference to the promotion strategy that this previous environment commit status applies to. The controller watches this PromotionStrategy and, for each environment, reports whether the preceding environment is synced and healthy.
             * @default {}
             */
            promotionStrategyRef: components["schemas"]["io_argoproj_promoter_v1alpha1_ObjectReference"];
            /**
             * @description URL is passed through to the owned chain-shaped DAGCommitStatus. The DAG controller renders it when writing per-environment CommitStatuses. Optional; when empty, no URL is set.
             * @default {}
             */
            url?: components["schemas"]["URLConfig"];
        };
        /** @description PreviousEnvironmentCommitStatusStatus defines the observed state of PreviousEnvironmentCommitStatus. */
        PreviousEnvironmentCommitStatusStatus: {
            /**
             * @description conditions represent the current state of the PreviousEnvironmentCommitStatus resource. Each condition has a unique type and reflects the status of a specific aspect of the resource.
             *
             *     Standard condition types include: - "Available": the resource is fully functional - "Progressing": the resource is being created or updated - "Degraded": the resource failed to reach or maintain its desired state
             *
             *     The status of each condition is one of True, False, or Unknown.
             */
            conditions?: components["schemas"]["Condition"][];
            /** @description InstanceID mirrors metadata.labels[promoter.argoproj.io/instance-id] stamped on each reconcile attempt by this install's controller, including when Ready=False; omitted when the resource has no instance-id label (default install). */
            instanceID?: string;
            /**
             * Format: int64
             * @description ObservedGeneration is the .metadata.generation that this status was reconciled from. Because status is written via Server-Side Apply with ForceOwnership (which has no optimistic-concurrency check), this field is the canonical way to detect stale status writes: compare status.observedGeneration with metadata.generation.
             */
            observedGeneration?: number;
        };
        /** @description PromotionStrategy is the Schema for the promotionstrategies API */
        PromotionStrategy: {
            /** @description APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
            apiVersion: string;
            /** @description Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
            kind: string;
            /** @default {} */
            metadata: components["schemas"]["ObjectMeta"];
            /** @default {} */
            spec: components["schemas"]["PromotionStrategySpec"];
            /** @default {} */
            status?: components["schemas"]["PromotionStrategyStatus"];
        };
        /**
         * @description PromotionStrategyDetails is a read-only, server-computed bundle that joins a PromotionStrategy with all of its related resources. It is served by the view aggregation layer (an extension apiserver) and is not persisted in etcd. The name of a PromotionStrategyDetails always matches the name of the PromotionStrategy it describes (1:1 mapping within a namespace).
         *
         *     Secrets are never included in the bundle.
         */
        PromotionStrategyDetails: {
            /** @description APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
            apiVersion: string;
            /** @description ArgoCDCommitStatuses are the ArgoCDCommitStatus managers that reference the PromotionStrategy. */
            argoCDCommitStatuses?: components["schemas"]["ArgoCDCommitStatus"][];
            /** @description ChangeTransferPolicies are the CTPs owned by the PromotionStrategy (selected by the promoter.argoproj.io/promotion-strategy label). */
            changeTransferPolicies?: components["schemas"]["ChangeTransferPolicy"][];
            /** @description ClusterScmProvider is the cluster-scoped ScmProvider referenced by the GitRepository, if applicable. The credentials Secret referenced by the provider is never resolved or included. */
            clusterScmProvider?: components["schemas"]["ClusterScmProvider"];
            /** @description CommitStatuses are the base CommitStatus resources associated with the PromotionStrategy (selected by the promoter.argoproj.io/promotion-strategy label). */
            commitStatuses?: components["schemas"]["CommitStatus"][];
            /** @description DAGCommitStatuses are the DAGCommitStatus managers that reference the PromotionStrategy. */
            dagCommitStatuses?: components["schemas"]["DAGCommitStatus"][];
            /** @description GitCommitStatuses are the GitCommitStatus managers that reference the PromotionStrategy. */
            gitCommitStatuses?: components["schemas"]["GitCommitStatus"][];
            /** @description GitRepository is the GitRepository referenced by the PromotionStrategy, if resolvable. */
            gitRepository?: components["schemas"]["GitRepository"];
            /** @description Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
            kind: string;
            /** @default {} */
            metadata: components["schemas"]["ObjectMeta"];
            /** @description PreviousEnvironmentCommitStatuses are the PreviousEnvironmentCommitStatus managers that reference the PromotionStrategy. */
            previousEnvironmentCommitStatuses?: components["schemas"]["PreviousEnvironmentCommitStatus"][];
            /**
             * @description PromotionStrategy is the source PromotionStrategy this bundle describes.
             * @default {}
             */
            promotionStrategy: components["schemas"]["PromotionStrategy"];
            /** @description PullRequests are the PullRequests associated with the PromotionStrategy (selected by the promoter.argoproj.io/promotion-strategy label). */
            pullRequests?: components["schemas"]["PullRequest"][];
            /** @description ScheduledCommitStatuses are the ScheduledCommitStatus managers that reference the PromotionStrategy. */
            scheduledCommitStatuses?: components["schemas"]["ScheduledCommitStatus"][];
            /** @description ScmProvider is the namespaced ScmProvider referenced by the GitRepository, if applicable. The credentials Secret referenced by the provider is never resolved or included. */
            scmProvider?: components["schemas"]["ScmProvider"];
            /** @description TimedCommitStatuses are the TimedCommitStatus managers that reference the PromotionStrategy. */
            timedCommitStatuses?: components["schemas"]["TimedCommitStatus"][];
            /** @description WebRequestCommitStatuses are the WebRequestCommitStatus managers that reference the PromotionStrategy. */
            webRequestCommitStatuses?: components["schemas"]["WebRequestCommitStatus"][];
        };
        /** @description PromotionStrategyDetailsList contains a list of PromotionStrategyDetails. */
        PromotionStrategyDetailsList: {
            /** @description APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
            apiVersion?: string;
            items: components["schemas"]["PromotionStrategyDetails"][];
            /** @description Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
            kind?: string;
            /** @default {} */
            metadata?: components["schemas"]["ListMeta"];
        };
        /** @description PromotionStrategySpec defines the desired state of PromotionStrategy */
        PromotionStrategySpec: {
            /**
             * @description ActiveCommitStatuses are commit statuses describing an actively running dry commit. If an active commit status is failing for an environment, subsequent environments will not deploy the failing commit.
             *
             *     The commit statuses specified in this field apply to all environments in the promotion sequence. You can also specify commit statuses for individual environments in the `environments` field.
             */
            activeCommitStatuses?: components["schemas"]["CommitStatusSelector"][];
            /** @description ActivePath is the default repository subpath for this strategy's active state. When set, proposed branches are created as <environment-branch>-next/<activePath>. Individual environments can override this value via their own activePath field. */
            activePath?: string;
            /** @description Environments is the sequence of environments that a dry commit will be promoted through. */
            environments: components["schemas"]["Environment"][];
            /**
             * @description RepositoryReference indicates what repository to promote commits in.
             * @default {}
             */
            gitRepositoryRef: components["schemas"]["io_argoproj_promoter_v1alpha1_ObjectReference"];
            /**
             * @description ProposedCommitStatuses are commit statuses describing a proposed dry commit, i.e. one that is not yet running in a live environment. If a proposed commit status is failing for a given environment, the dry commit will not be promoted to that environment.
             *
             *     The commit statuses specified in this field apply to all environments in the promotion sequence. You can also specify commit statuses for individual environments in the `environments` field.
             */
            proposedCommitStatuses?: components["schemas"]["CommitStatusSelector"][];
        };
        /** @description PromotionStrategyStatus defines the observed state of PromotionStrategy */
        PromotionStrategyStatus: {
            /** @description Conditions Represents the observations of the current state. */
            conditions?: components["schemas"]["Condition"][];
            /** @description Environments holds the status of each environment in the promotion sequence. */
            environments?: components["schemas"]["EnvironmentStatus"][];
            /** @description InstanceID mirrors metadata.labels[promoter.argoproj.io/instance-id] stamped on each reconcile attempt by this install's controller, including when Ready=False; omitted when the resource has no instance-id label (default install). */
            instanceID?: string;
            /**
             * Format: int64
             * @description ObservedGeneration is the .metadata.generation that this status was reconciled from. Because status is written via Server-Side Apply with ForceOwnership (which has no optimistic-concurrency check), this field is the canonical way to detect stale status writes: compare status.observedGeneration with metadata.generation.
             */
            observedGeneration?: number;
        };
        /** @description PullRequest is the Schema for the pullrequests API */
        PullRequest: {
            /** @description APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
            apiVersion?: string;
            /** @description Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
            kind?: string;
            /** @default {} */
            metadata?: components["schemas"]["ObjectMeta"];
            /** @default {} */
            spec?: components["schemas"]["PullRequestSpec"];
            /** @default {} */
            status?: components["schemas"]["PullRequestStatus"];
        };
        /** @description PullRequestCommonStatus defines the common status fields for a pull request. */
        PullRequestCommonStatus: {
            /** @description ExternallyMergedOrClosed indicates that the pull request is no longer open on the SCM while the PullRequest still desired it open: merged or closed outside the controller, or closed on the SCM because the PullRequest resource was deleted (finalizer) before this status was reconciled. When true, the State field will be empty ("") since we cannot tell merge vs. close from the provider. This status is preserved even after the PullRequest resource is deleted, maintaining a historical record until a new pull request is created for this environment. */
            externallyMergedOrClosed?: boolean;
            /** @description ID is the unique identifier of the pull request, set by the SCM. */
            id?: string;
            /** @description PRCreationTime is the time when the pull request was created. */
            prCreationTime?: components["schemas"]["Time"];
            /** @description PRMergeTime is the time when the pull request was merged. This time can vary slightly from the actual merge time because it is the time when the ChangeTransferPolicy controller sets the pull requests spec to merge. In the future we plan on making this time more accurate by fetching the actual merge time from the SCM via the webhook this would then be updated in the git note for that commit. */
            prMergeTime?: components["schemas"]["Time"];
            /** @description State is the state of the pull request. */
            state?: string;
            /** @description Url is the URL of the pull request. */
            url?: string;
        };
        /** @description PullRequestSpec defines the desired state of PullRequest */
        PullRequestSpec: {
            /**
             * @description Commit contains configuration for how we will merge/squash/etc the pull request.
             * @default {}
             */
            commit?: components["schemas"]["CommitConfiguration"];
            /** @description Description is the description body of the pull/merge request */
            description?: string;
            /**
             * @description RepositoryReference indicates what repository to open the PR on.
             * @default {}
             */
            gitRepositoryRef: components["schemas"]["io_argoproj_promoter_v1alpha1_ObjectReference"];
            /**
             * @description MergeSha is the commit SHA that the head branch must match before the PR can be merged. This prevents a race condition where a PR is merged with a different commit than intended. Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
             * @default
             */
            mergeSha: string;
            /**
             * @description SourceBranch is the base the git reference that we are merging into Head ---> Base Must not start with '-', contain ':', or contain '..'.
             * @default
             */
            sourceBranch: string;
            /**
             * @description State of the pull request (closed, merged, or open). Must always be "open" when creating a new pull request. This value may not be changed to "closed" or "merged" unless the pull request status.id is set.
             * @default
             */
            state: string;
            /**
             * @description TargetBranch is the head the git reference we are merging from Head ---> Base Must not start with '-', contain ':', or contain '..'.
             * @default
             */
            targetBranch: string;
            /**
             * @description Title is the title of the pull request.
             * @default
             */
            title: string;
        };
        /** @description PullRequestStatus defines the observed state of PullRequest */
        PullRequestStatus: {
            /** @description Conditions Represents the observations of the current state. */
            conditions?: components["schemas"]["Condition"][];
            /** @description ExternallyMergedOrClosed indicates that the pull request is no longer open on the SCM while the resource still desired it open (spec.state is "open"): either it was merged or closed outside the controller, or it was closed on the SCM because the PullRequest resource was deleted (finalizer) and a subsequent sync observed it missing. The controller does not distinguish those cases here. When true, the State field will be empty ("") since we cannot tell merge vs. close from the provider. The PullRequest resource will be deleted after this flag is set when possible, but the status is preserved in the owning ChangeTransferPolicy to maintain a record. */
            externallyMergedOrClosed?: boolean;
            /** @description ID the id of the pull request */
            id?: string;
            /** @description InstanceID mirrors metadata.labels[promoter.argoproj.io/instance-id] stamped on each reconcile attempt by this install's controller, including when Ready=False; omitted when the resource has no instance-id label (default install). */
            instanceID?: string;
            /**
             * Format: int64
             * @description ObservedGeneration is the .metadata.generation that this status was reconciled from. Because status is written via Server-Side Apply with ForceOwnership (which has no optimistic-concurrency check), this field is the canonical way to detect stale status writes: compare status.observedGeneration with metadata.generation.
             */
            observedGeneration?: number;
            /** @description PRCreationTime the time the PR was created */
            prCreationTime?: components["schemas"]["Time"];
            /** @description SCMSyncedSpecDigest fingerprints title and description last successfully synced to the SCM via provider.Update on an open pull request. */
            scmSyncedSpecDigest?: string;
            /** @description State of the merge request closed/merged/open */
            state?: string;
            /** @description Url is the URL of the pull request. */
            url?: string;
        };
        /** @description ResponseOutputSpec holds the expression that extracts data from the HTTP response into ResponseOutput. */
        ResponseOutputSpec: {
            /**
             * @description Output is evaluated after the HTTP request completes (any status). Response variables are the same as for spec.success.when.expression — see WhenWithOutputSpec.Expression. The result is stored in status (environments[].responseOutput or promotionStrategyContext.responseOutput) and exposed on the next reconcile as ResponseOutput in trigger expressions and templates. Must return a map/object.
             * @default {}
             */
            output: components["schemas"]["OutputSpec"];
        };
        /** @description RevisionReference contains a reference to a some information that is related in some way to another commit. For now, it supports only references to a commit. In the future, it may support other types of references. */
        RevisionReference: {
            /** @description Commit contains metadata about the commit that is related in some way to another commit. */
            commit?: components["schemas"]["CommitMetadata"];
        };
        /** @description ScheduledCommitStatus is the Schema for the scheduledcommitstatuses API. */
        ScheduledCommitStatus: {
            /** @description APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
            apiVersion?: string;
            /** @description Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
            kind?: string;
            /**
             * @description metadata is a standard object metadata
             * @default {}
             */
            metadata?: components["schemas"]["ObjectMeta"];
            /**
             * @description spec defines the desired state of ScheduledCommitStatus
             * @default {}
             */
            spec: components["schemas"]["ScheduledCommitStatusSpec"];
            /**
             * @description status defines the observed state of ScheduledCommitStatus
             * @default {}
             */
            status?: components["schemas"]["ScheduledCommitStatusStatus"];
        };
        /** @description ScheduledCommitStatusSpec defines the desired state of ScheduledCommitStatus. */
        ScheduledCommitStatusSpec: {
            /** @description Allow defines global allow windows applied to all listed environments in addition to per-environment allow windows (OR semantics across all). */
            allow?: components["schemas"]["CronWindow"][];
            /** @description Environments defines the list of environments to gate. Only listed environments are gated; unlisted environments default to success (24/7 open). Each environment inherits global allow/exclude windows and may add its own. An environment with no per-environment windows is valid when global windows are defined. */
            environments: components["schemas"]["ScheduledEnvironment"][];
            /** @description Exclude defines global exclusion windows applied to all listed environments in addition to per-environment exclusions. Exclusions take precedence over allow windows. */
            exclude?: components["schemas"]["CronWindow"][];
            /**
             * @description Key is the gate name referenced in the PromotionStrategy's proposedCommitStatuses. Must be lowercase alphanumeric with hyphens, 1–63 characters (pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$).
             * @default
             */
            key: string;
            /**
             * @description PromotionStrategyRef is a reference to the promotion strategy that this scheduled commit status applies to.
             * @default {}
             */
            promotionStrategyRef: components["schemas"]["io_argoproj_promoter_v1alpha1_ObjectReference"];
            /** @description Timezone is the default IANA timezone name used for evaluating cron expressions across all windows. Individual windows may override this with their own timezone field. Defaults to UTC. */
            timezone?: string;
        };
        /** @description ScheduledCommitStatusStatus defines the observed state of ScheduledCommitStatus. */
        ScheduledCommitStatusStatus: {
            /** @description Conditions represent the latest available observations of an object's state */
            conditions?: components["schemas"]["Condition"][];
            /** @description Environments holds the status of each environment being tracked. */
            environments?: components["schemas"]["ScheduledEnvironmentStatus"][];
            /** @description InstanceID mirrors metadata.labels[promoter.argoproj.io/instance-id] stamped on each reconcile attempt by this install's controller, including when Ready=False; omitted when the resource has no instance-id label (default install). */
            instanceID?: string;
            /**
             * Format: int64
             * @description ObservedGeneration is the .metadata.generation that this status was reconciled from. Because status is written via Server-Side Apply with ForceOwnership (which has no optimistic-concurrency check), this field is the canonical way to detect stale status writes: compare status.observedGeneration with metadata.generation.
             */
            observedGeneration?: number;
        };
        /** @description ScheduledEnvironment defines the window configuration for a single environment. */
        ScheduledEnvironment: {
            /** @description Allow defines the allowed promotion windows. Promotions are allowed when the current time falls inside any of these windows (OR semantics). These are combined with any global spec.allow windows. If no allow windows are defined (neither here nor globally), the environment operates in exclusion-only mode. */
            allow?: components["schemas"]["CronWindow"][];
            /**
             * @description Branch is the name of the branch/environment you want to gate with time windows. Must not start with '-', contain ':', or contain '..'.
             * @default
             */
            branch: string;
            /** @description Exclude defines blackout periods during which promotions are blocked. Exclusions take precedence over allow windows — if the current time falls inside any exclusion, promotions are blocked regardless of allow windows. These are combined with any global spec.exclude windows. If empty, no blackout periods are applied. */
            exclude?: components["schemas"]["CronWindow"][];
        };
        /** @description ScheduledEnvironmentStatus defines the observed window state for a specific environment. */
        ScheduledEnvironmentStatus: {
            /** @description Active holds details of the currently active window driving the phase. Nil when no specific window is active (e.g. exclusion-only mode with no active exclusion). */
            active?: components["schemas"]["WindowStatus"];
            /**
             * @description Branch is the name of the branch/environment.
             * @default
             */
            branch: string;
            /** @description Next holds details of the next expected window transition. Used by UIs for countdown timers and by the controller for precise requeuing. */
            next?: components["schemas"]["WindowStatus"];
            /**
             * @description Phase represents the current phase of the scheduled gate.
             * @default
             */
            phase: string;
            /**
             * @description Sha is the proposed commit SHA being tracked for this environment. Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
             * @default
             */
            sha: string;
        };
        /**
         * @description Scm specifies authentication using SCM provider credentials.
         *
         *     This authentication method uses the credentials from the ScmProvider referenced by the PromotionStrategy. The controller retrieves the SCM provider and secret and applies the appropriate authentication based on the provider type.
         */
        Scm: Record<string, never>;
        /** @description ScmProvider is the Schema for the scmproviders API */
        ScmProvider: {
            /** @description APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
            apiVersion?: string;
            /** @description Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
            kind?: string;
            /** @default {} */
            metadata?: components["schemas"]["ObjectMeta"];
            /** @default {} */
            spec?: components["schemas"]["ScmProviderSpec"];
            /** @default {} */
            status?: components["schemas"]["ScmProviderStatus"];
        };
        /** @description ScmProviderObjectReference is a reference to a SCM provider object. */
        ScmProviderObjectReference: {
            /**
             * @description Kind is the type of resource being referenced
             * @default
             */
            kind: string;
            /**
             * @description Name is the name of the resource being referenced
             * @default
             */
            name: string;
        };
        /** @description ScmProviderSpec defines the desired state of ScmProvider */
        ScmProviderSpec: {
            /** @description AzureDevOps required configuration for Azure DevOps as the SCM provider */
            azureDevOps?: components["schemas"]["AzureDevOps"];
            /** @description BitbucketCloud required configuration for Bitbucket Cloud as the SCM provider */
            bitbucketCloud?: components["schemas"]["BitbucketCloud"];
            /** @description Fake required configuration for Fake as the SCM provider */
            fake?: components["schemas"]["Fake"];
            /** @description Forgejo required configuration for Forgejo as the SCM provider */
            forgejo?: components["schemas"]["Forgejo"];
            /** @description Gitea required configuration for Gitea as the SCM provider */
            gitea?: components["schemas"]["Gitea"];
            /** @description GitHub required configuration for GitHub as the SCM provider */
            github?: components["schemas"]["GitHub"];
            /** @description GitLab required configuration for GitLab as the SCM provider */
            gitlab?: components["schemas"]["GitLab"];
            /** @description SecretRef contains the credentials required to auth to a specific provider */
            secretRef?: components["schemas"]["LocalObjectReference"];
        };
        /** @description ScmProviderStatus defines the observed state of ScmProvider */
        ScmProviderStatus: {
            /** @description Conditions Represents the observations of the current state. */
            conditions?: components["schemas"]["Condition"][];
            /** @description InstanceID mirrors metadata.labels[promoter.argoproj.io/instance-id] stamped on each reconcile attempt by this install's controller, including when Ready=False; omitted when the resource has no instance-id label (default install). */
            instanceID?: string;
            /**
             * Format: int64
             * @description ObservedGeneration is the .metadata.generation that this status was reconciled from. Because status is written via Server-Side Apply with ForceOwnership (which has no optimistic-concurrency check), this field is the canonical way to detect stale status writes: compare status.observedGeneration with metadata.generation.
             */
            observedGeneration?: number;
        };
        /** @description ShardInfo describes the shard selector that was applied to produce a list response. Its presence on a list response indicates the list is a filtered subset. */
        ShardInfo: {
            /**
             * @description selector is the shard selector string from the request, echoed back so clients can verify which shard they received and merge responses from multiple shards.
             * @default
             */
            selector: string;
        };
        /** @description SuccessSpec defines when the commit status phase is success. */
        SuccessSpec: {
            /**
             * @description When is evaluated every reconcile. See WhenWithOutputSpec.Expression.
             * @default {}
             */
            when: components["schemas"]["WhenWithOutputSpec"];
        };
        /**
         * @description TLSAuth defines TLS client certificate authentication (mutual TLS / mTLS).
         *
         *     Mutual TLS authentication proves the client's identity using a certificate, rather than a password or token. This is configured at the TLS transport layer, not as an HTTP header.
         *
         *     Use cases: - High-security environments requiring certificate-based authentication - APIs that require client certificates for access - Service mesh authentication - Zero-trust network architectures
         *
         *     The secret must contain: - Client certificate (key: "tls.crt") - Private key (key: "tls.key") - Optional CA certificate for custom CAs (key: "ca.crt")
         *
         *     Example:
         *
         *     	tls:
         *     	  secretRef:
         *     	    name: my-client-cert
         *
         *     Note: TLS auth is applied at the HTTP client transport layer, unlike other authentication methods which are applied as HTTP headers.
         */
        TLSAuth: {
            /** @description SecretRef references a secret containing TLS certificate and key. The secret should be of type kubernetes.io/tls or contain keys "tls.crt" and "tls.key", and optionally "ca.crt". */
            secretRef: components["schemas"]["LocalObjectReference"];
        };
        /**
         * Format: date-time
         * @description Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON.  Wrappers are provided for many of the factory methods that the time package offers.
         */
        Time: string;
        /** @description TimedCommitStatus is the Schema for the timedcommitstatuses API */
        TimedCommitStatus: {
            /** @description APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
            apiVersion?: string;
            /** @description Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
            kind?: string;
            /**
             * @description metadata is a standard object metadata
             * @default {}
             */
            metadata?: components["schemas"]["ObjectMeta"];
            /**
             * @description spec defines the desired state of TimedCommitStatus
             * @default {}
             */
            spec: components["schemas"]["TimedCommitStatusSpec"];
            /**
             * @description status defines the observed state of TimedCommitStatus
             * @default {}
             */
            status?: components["schemas"]["TimedCommitStatusStatus"];
        };
        /** @description TimedCommitStatusEnvironments defines the branch/environment and duration to wait before reporting the gate as success. */
        TimedCommitStatusEnvironments: {
            /**
             * @description Branch is the name of the branch/environment you want to gate for the configured duration.
             * @default
             */
            branch: string;
            /** @description Duration is the time duration to wait before considering the commit status as success. The duration should be in a format accepted by Go's time.ParseDuration function, e.g., "5m", "1h30m". */
            duration: components["schemas"]["Duration"];
        };
        /** @description TimedCommitStatusEnvironmentsStatus defines the observed timing status for a specific environment. */
        TimedCommitStatusEnvironmentsStatus: {
            /** @description AtMostDurationRemaining is the maximum amount of time remaining until the gate is satisfied. This is calculated at reconciliation time. When the gate is satisfied (phase=success), this will be 0. */
            atMostDurationRemaining: components["schemas"]["Duration"];
            /**
             * @description Branch is the name of the branch/environment.
             * @default
             */
            branch: string;
            /** @description CommitTime is when the commit was deployed to the active environment. */
            commitTime: components["schemas"]["Time"];
            /**
             * @description Phase represents the current phase of the timed gate.
             * @default
             */
            phase: string;
            /** @description RequiredDuration is the duration that must elapse before promotion is allowed. If this is updated in the spec with a pending time. The new duration will be calculated from the original CommitTime. Allowing you to extend the duration of an already deployed commit, if the time has not yet elapsed. */
            requiredDuration: components["schemas"]["Duration"];
            /**
             * @description Sha is the commit SHA being tracked for this environment. Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
             * @default
             */
            sha: string;
        };
        /** @description TimedCommitStatusSpec defines the desired state of TimedCommitStatus */
        TimedCommitStatusSpec: {
            environments: components["schemas"]["TimedCommitStatusEnvironments"][];
            /** @description Key is the gate name referenced in the PromotionStrategy's activeCommitStatuses or proposedCommitStatuses. When omitted, the CRD default is timer. Set Key explicitly, even if you use the CRD default. Must be lowercase alphanumeric with hyphens, 1–63 characters (pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$). */
            key?: string;
            /**
             * @description PromotionStrategyRef is a reference to the promotion strategy that this timed commit status applies to.
             * @default {}
             */
            promotionStrategyRef: components["schemas"]["io_argoproj_promoter_v1alpha1_ObjectReference"];
        };
        /** @description TimedCommitStatusStatus defines the observed state of TimedCommitStatus. */
        TimedCommitStatusStatus: {
            /** @description Conditions represent the latest available observations of an object's state */
            conditions?: components["schemas"]["Condition"][];
            /** @description Environments holds the status of each environment being tracked. */
            environments?: components["schemas"]["TimedCommitStatusEnvironmentsStatus"][];
            /** @description InstanceID mirrors metadata.labels[promoter.argoproj.io/instance-id] stamped on each reconcile attempt by this install's controller, including when Ready=False; omitted when the resource has no instance-id label (default install). */
            instanceID?: string;
            /**
             * Format: int64
             * @description ObservedGeneration is the .metadata.generation that this status was reconciled from. Because status is written via Server-Side Apply with ForceOwnership (which has no optimistic-concurrency check), this field is the canonical way to detect stale status writes: compare status.observedGeneration with metadata.generation.
             */
            observedGeneration?: number;
        };
        /**
         * @description TriggerModeSpec defines expression-based trigger configuration.
         *
         *     Delivery semantics: the controller dedupes HTTP requests by comparing fresh state against state it persisted in WebRequestCommitStatus.status on the PREVIOUS reconcile (TriggerOutput, ResponseOutput, SuccessOutput, LastRequestTime, LastSuccessfulShas). That state is written via Server-Side Apply at the END of a reconcile and read from the controller's informer cache at the START of the next reconcile. Under cache- propagation lag, controller restarts, or status-write retries, a reconcile may not see the most recently persisted state and may re-fire the HTTP request even though the trigger would otherwise have been false.
         *
         *     Treat WebRequestCommitStatus as AT-LEAST-ONCE HTTP delivery, not exactly-once. Make external endpoints idempotent for the same (branch, sha) input. Don't use trigger output to count attempts authoritatively (counters built on TriggerOutput are eventually-consistent and may briefly observe a stale older value under cache lag).
         */
        TriggerModeSpec: {
            /** @description RequeueDuration specifies how long to wait before requeuing to re-evaluate the trigger expression. */
            requeueDuration?: components["schemas"]["Duration"];
            /** @description Response optionally configures an expression that extracts data from the HTTP response into ResponseOutput. */
            response?: components["schemas"]["ResponseOutputSpec"];
            /**
             * @description When configures the boolean guard and optional output expression that control whether the HTTP request is made.
             * @default {}
             */
            when: components["schemas"]["WhenWithOutputSpec"];
        };
        /** @description URLConfig is a template that can be rendered using the Go template engine. */
        URLConfig: {
            /**
             * @description Options sets options for the template. Options are described by strings, either a simple string or "key=value". There can be at most one equals sign in an option string. If the option string is unrecognized or otherwise invalid, Option panics.
             *
             *     Known options:
             *
             *     missingkey: Control the behavior during execution if a map is indexed with a key that is not present in the map.
             *
             *     	"missingkey=default" or "missingkey=invalid"
             *     		The default behavior: Do nothing and continue execution.
             *     		If printed, the result of the index operation is the string
             *     		"<no value>".
             *     	"missingkey=zero"
             *     		The operation returns the zero value for the map type's element.
             *     	"missingkey=error"
             *     		Execution stops immediately with an error.
             */
            options?: string[];
            /**
             * @description Template is a go text template and receives .Environment and .ArgoCDCommitStatus variables. A function called urlQueryEscape is available to escape url query parameters. The template can be configured with options to control the behavior during execution if a variable is not present.
             *
             *     Example:
             *
             *       {{- $baseURL := "https://dev.argocd.local" -}}
             *       {{- if eq .Environment "environment/development" -}}
             *       {{- $baseURL = "https://dev.argocd.local" -}}
             *       {{- else if eq .Environment "environment/staging" -}}
             *       {{- $baseURL = "https://staging.argocd.local" -}}
             *       {{- else if eq .Environment "environment/production" -}}
             *       {{- $baseURL = "https://prod.argocd.local" -}}
             *       {{- end -}}
             *       {{- $labels := "" -}}
             *       {{- range $key, $value := .ArgoCDCommitStatus.Spec.ApplicationSelector.MatchLabels -}}
             *       {{- $labels = printf "%s%s=%s," $labels $key $value -}}
             *       {{- end -}}
             *       {{ printf "%s/applications?labels=%s" $baseURL (urlQueryEscape $labels) }}
             */
            template?: string;
        };
        /** @description WebRequestCommitStatus is the Schema for the webrequestcommitstatuses API */
        WebRequestCommitStatus: {
            /** @description APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
            apiVersion?: string;
            /** @description Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
            kind?: string;
            /**
             * @description metadata is a standard object metadata
             * @default {}
             */
            metadata?: components["schemas"]["ObjectMeta"];
            /**
             * @description spec defines the desired state of WebRequestCommitStatus
             * @default {}
             */
            spec: components["schemas"]["WebRequestCommitStatusSpec"];
            /**
             * @description status defines the observed state of WebRequestCommitStatus
             * @default {}
             */
            status?: components["schemas"]["WebRequestCommitStatusStatus"];
        };
        /**
         * @description WebRequestCommitStatusEnvironmentStatus defines the observed status for a specific environment.
         *
         *     TriggerOutput, ResponseOutput, and SuccessOutput hold JSON maps written by their respective output expressions. They are surfaced on the next reconcile as TriggerOutput, ResponseOutput, and SuccessOutput in expr and templates (see WhenWithOutputSpec and HTTPRequestSpec).
         */
        WebRequestCommitStatusEnvironmentStatus: {
            /**
             * @description Branch is the name of the branch/environment.
             * @default
             */
            branch: string;
            /** @description LastRequestTime is when the last HTTP request was made. */
            lastRequestTime?: components["schemas"]["Time"];
            /**
             * Format: int32
             * @description LastResponseStatusCode is the HTTP status code from the last request.
             */
            lastResponseStatusCode?: number;
            /** @description LastSuccessfulSha is the last commit SHA that achieved success status for this environment. Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats. */
            lastSuccessfulSha?: string;
            /**
             * @description Phase represents the current phase of the validation. This controller sets only "pending" or "success"; it never sets "failure" (failure is allowed by the enum for API consistency).
             * @default
             */
            phase: string;
            /** @description ReportedSha is the commit SHA being reported on for this environment. This is determined by the reportOn field (proposed or active). Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats. */
            reportedSha?: string;
            /** @description ResponseOutput is the map from spec.mode.trigger.response.output.expression when spec.mode.trigger.response is set; otherwise nil. */
            responseOutput?: components["schemas"]["JSON"];
            /** @description SuccessOutput is the map from spec.success.when.output.expression (arbitrary JSON keys). Evaluated every reconcile (whether or not an HTTP request was made). The result is stored here and exposed on the next reconcile as SuccessOutput in trigger/success expressions and templates. */
            successOutput?: components["schemas"]["JSON"];
            /** @description TriggerOutput is the map from spec.mode.trigger.when.output.expression (arbitrary JSON keys). */
            triggerOutput?: components["schemas"]["JSON"];
        };
        /** @description WebRequestCommitStatusLastSuccessfulShaItem records the last successful SHA for one branch in promotionstrategy context status. */
        WebRequestCommitStatusLastSuccessfulShaItem: {
            /**
             * @description Branch is the environment branch name (merge key for lastSuccessfulShas list).
             * @default
             */
            branch: string;
            /** @description LastSuccessfulSha is the SHA that last achieved success for this branch. */
            lastSuccessfulSha?: string;
        };
        /** @description WebRequestCommitStatusPhasePerBranchItem is one branch's resolved phase in promotionstrategy context status. */
        WebRequestCommitStatusPhasePerBranchItem: {
            /**
             * @description Branch is the environment branch name (merge key for phasePerBranch list).
             * @default
             */
            branch: string;
            /**
             * @description Phase is "pending", "success", or "failure".
             * @default
             */
            phase: string;
        };
        /** @description WebRequestCommitStatusPromotionStrategyContextStatus holds the observed state for context=promotionstrategy (at most one request per WebRequestCommitStatus). */
        WebRequestCommitStatusPromotionStrategyContextStatus: {
            /** @description LastRequestTime is when the last HTTP request was made. */
            lastRequestTime?: components["schemas"]["Time"];
            /**
             * Format: int32
             * @description LastResponseStatusCode is the HTTP status code from the last request.
             */
            lastResponseStatusCode?: number;
            /** @description LastSuccessfulShas tracks the last SHA that achieved success for each branch. Used with reportOn "proposed" + polling to skip HTTP requests when all environments have already succeeded for their current SHAs. */
            lastSuccessfulShas?: components["schemas"]["WebRequestCommitStatusLastSuccessfulShaItem"][];
            /** @description PhasePerBranch holds the resolved phase for each applicable branch. Every applicable branch is always present after reconciliation. */
            phasePerBranch?: components["schemas"]["WebRequestCommitStatusPhasePerBranchItem"][];
            /** @description ResponseOutput: same semantics as WebRequestCommitStatusEnvironmentStatus.ResponseOutput, one shared map for context=promotionstrategy. */
            responseOutput?: components["schemas"]["JSON"];
            /** @description SuccessOutput: same semantics as WebRequestCommitStatusEnvironmentStatus.SuccessOutput, one shared map for context=promotionstrategy. */
            successOutput?: components["schemas"]["JSON"];
            /** @description TriggerOutput: same semantics as WebRequestCommitStatusEnvironmentStatus.TriggerOutput, one shared map for context=promotionstrategy. */
            triggerOutput?: components["schemas"]["JSON"];
        };
        /**
         * @description WebRequestCommitStatusSpec defines the desired state of WebRequestCommitStatus.
         *
         *     Documentation map: template variables and Sprig rules — HTTPRequestSpec; context (environments vs promotionstrategy) — ModeSpec; success expression after HTTP response — WhenWithOutputSpec; trigger when/output expressions — WhenWithOutputSpec; persisted trigger/response/success maps — WebRequestCommitStatusEnvironmentStatus.
         */
        WebRequestCommitStatusSpec: {
            /**
             * @description DescriptionTemplate is the human-readable commit status description in the SCM provider (GitHub, GitLab, etc.). Uses Go templates; variable list and Sprig rules match HTTPRequestSpec (spec.httpRequest). Rendered with the latest data after the most recent HTTP request and trigger evaluation (unlike spec.httpRequest URL/body/headers, which use pre-request data — see HTTPRequestSpec). How spec.mode.context restricts template data: see ModeSpec.
             *
             *     Additional template variables (not in HTTPRequestSpec):
             *       - {{ index .TriggerVariables "key" }}: result of trigger.when.variables.expression this reconcile (nil if not configured)
             *       - {{ index .SuccessVariables "key" }}: result of success.when.variables.expression this reconcile (nil if not configured)
             *
             *     Examples: "External approval for {{ .Branch }}", "{{ .Phase }} - waiting for external approval".
             *
             *     If not specified, defaults to empty string.
             */
            descriptionTemplate?: string;
            /**
             * @description HTTPRequest configures the outbound HTTP call. See HTTPRequestSpec.
             * @default {}
             */
            httpRequest: components["schemas"]["HTTPRequestSpec"];
            /**
             * @description Key is the unique identifier for this validation rule. It is used as the commit status key and in status messages. This key is matched against PromotionStrategy's proposedCommitStatuses or activeCommitStatuses to determine which environments this validation applies to. Must be lowercase alphanumeric with hyphens, 1–63 characters (pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$).
             * @default
             */
            key: string;
            /**
             * @description Mode selects polling vs trigger and request scope (context). See ModeSpec.
             * @default {}
             */
            mode: components["schemas"]["ModeSpec"];
            /**
             * @description PromotionStrategyRef references the PromotionStrategy this applies to. The controller will check commits from ALL environments in the referenced PromotionStrategy where this WebRequestCommitStatus.Spec.Key matches an entry in either:
             *       - PromotionStrategy.Spec.ProposedCommitStatuses (applies to all environments), OR
             *       - PromotionStrategy.Spec.ActiveCommitStatuses (applies to all environments), OR
             *       - Environment.ProposedCommitStatuses (applies to specific environment), OR
             *       - Environment.ActiveCommitStatuses (applies to specific environment)
             * @default {}
             */
            promotionStrategyRef: components["schemas"]["io_argoproj_promoter_v1alpha1_ObjectReference"];
            /**
             * @description ReportOn specifies which commit SHA to report the CommitStatus on. - "proposed": Reports on the proposed hydrated commit SHA (default) - "active": Reports on the active hydrated commit SHA
             *
             *     When "proposed": Polls until success, then stops polling for that SHA. Use "proposed" for checks that need to run just once before a change is promoted, like an approval step.
             *
             *     When "active": Polls forever, even after success (active state can change). Use "active" for checks that monitor the change after it's released, for example a metrics monitoring service.
             */
            reportOn?: string;
            /**
             * @description Success defines when the commit status phase is success. Evaluated every reconcile. See SuccessSpec.
             * @default {}
             */
            success: components["schemas"]["SuccessSpec"];
            /**
             * @description UrlTemplate is the commit status target URL in the SCM provider. Same Go templates, variables (including .TriggerVariables and .SuccessVariables), timing, and context rules as DescriptionTemplate. Typical uses: approval UI, dashboard, API, or runbook links. To include the reported SHA, walk PromotionStrategy.Status.Environments by Branch:
             *       "https://approvals.example.com/{{ range .PromotionStrategy.Status.Environments }}{{ if eq .Branch $.Branch }}{{ .Proposed.Hydrated.Sha }}{{ end }}{{ end }}"
             *     For branch-only links: "https://dashboard.example.com/{{ .Branch }}/status".
             *
             *     If not specified, defaults to empty string (no URL shown).
             */
            urlTemplate?: string;
        };
        /** @description WebRequestCommitStatusStatus defines the observed state of WebRequestCommitStatus. */
        WebRequestCommitStatusStatus: {
            /** @description Conditions represent the latest available observations of an object's state */
            conditions?: components["schemas"]["Condition"][];
            /** @description Environments holds the status of each environment when context is "environments". When context is "promotionstrategy", this slice is empty and PromotionStrategyContext is used instead. */
            environments?: components["schemas"]["WebRequestCommitStatusEnvironmentStatus"][];
            /** @description InstanceID mirrors metadata.labels[promoter.argoproj.io/instance-id] stamped on each reconcile attempt by this install's controller, including when Ready=False; omitted when the resource has no instance-id label (default install). */
            instanceID?: string;
            /**
             * Format: int64
             * @description ObservedGeneration is the .metadata.generation that this status was reconciled from. Because status is written via Server-Side Apply with ForceOwnership (which has no optimistic-concurrency check), this field is the canonical way to detect stale status writes: compare status.observedGeneration with metadata.generation.
             */
            observedGeneration?: number;
            /** @description PromotionStrategyContext holds the result of the one HTTP run when context is "promotionstrategy". At most one request is made per WebRequestCommitStatus; phase(s) are reported on each environment's CommitStatus. */
            promotionStrategyContext?: components["schemas"]["WebRequestCommitStatusPromotionStrategyContextStatus"];
        };
        /** @description WhenWithOutputSpec holds a when condition (boolean expression) and optional output (map expression). */
        WhenWithOutputSpec: {
            /**
             * @description Expression is a boolean expr expression that decides whether the HTTP request should be made. It is evaluated BEFORE each potential HTTP request. When it returns true the request is made; when false the controller keeps the previous phase and skips the request.
             *
             *     Available variables:
             *       - Branch (string): the environment branch currently being processed (empty for the shared HTTP request in promotionstrategy context)
             *       - Phase (string): previous reconcile's phase — per-environment in environments context; aggregate of all branches in promotionstrategy context (success only if all succeeded, failure if any failed, pending otherwise)
             *       - PromotionStrategy (PromotionStrategy): the full PromotionStrategy spec and status
             *       - WebRequestCommitStatus (WebRequestCommitStatus): the full WebRequestCommitStatus spec and status (snapshot from the previous reconcile)
             *       - TriggerOutput (map[string]any): custom data from the previous when.output.expression evaluation
             *       - ResponseOutput (map[string]any): response data from the previous HTTP request (if any)
             *       - SuccessOutput (map[string]any): custom data from the previous success.when.output.expression evaluation
             *       - Variables (map[string]any): when spec.variables is set, the map returned by variables.expression this reconcile; omitted otherwise
             *
             *     Note: PromotionStrategy.Status.Environments is an ordered array representing the promotion sequence. Use Branch + filter/find to look up environment-specific data:
             *       find(PromotionStrategy.Status.Environments, {.Branch == Branch}).Proposed.Hydrated.Sha
             *
             *     Examples:
             *       # Always trigger (equivalent to polling mode)
             *       - "true"
             *
             *       # Only trigger when SHA changes from what we last tracked
             *       - "find(PromotionStrategy.Status.Environments, {.Branch == Branch}).Proposed.Hydrated.Sha != (TriggerOutput['lastCheckedSha'] ?? '')"
             *
             *       # Only trigger when a particular commit status is success (e.g. argocd-health)
             *       - "let env = find(PromotionStrategy.Status.Environments, {.Branch == Branch}); any(env.Proposed.CommitStatuses, {.Key == 'argocd-health' && .Phase == 'success'})"
             *
             *       # Only retry if the previous response indicated we should
             *       - "ResponseOutput == nil || ResponseOutput.status == 'retry'"
             * @default
             */
            expression: string;
            /**
             * @description Output optionally holds an expression that produces a map of data to persist across reconcile cycles. The expression runs on every reconcile (whether or not the HTTP request is made). Its result is stored in status (per-environment under environments[].triggerOutput, or under promotionStrategyContext.triggerOutput when context is promotionstrategy) and is available in the next reconcile as TriggerOutput in when.expression, when.output.expression, and in templates. Use it to track state such as attempt counts, last-seen SHAs, or timestamps.
             *
             *     Delivery semantics caveat: persisted output is read from the controller's informer cache at the start of the next reconcile. Under cache-propagation lag, controller restarts, or status-write retries, the next reconcile may not see the most recently persisted output and may re-fire the HTTP request. Treat this as AT-LEAST-ONCE delivery — counters built on TriggerOutput are eventually-consistent (a stale read can cause a duplicate increment), so use them for backoff hints, not for hard "fail after N attempts" gates.
             *
             *     Variables are the same as for Expression (see above). The expression must return a map/object; every key is stored in TriggerOutput.
             *
             *     Examples:
             *       # Track SHA to detect changes (idempotent: replays produce the same trackedSha for the same input)
             *       - "{ trackedSha: find(PromotionStrategy.Status.Environments, {.Branch == Branch}).Proposed.Hydrated.Sha }"
             *
             *       # Increment attempt counter (eventually-consistent; may briefly under-count under cache lag)
             *       - "{ attemptCount: (TriggerOutput[\"attemptCount\"] ?? 0) + 1 }"
             */
            output?: components["schemas"]["OutputSpec"];
            /**
             * @description Variables optionally holds an expression that runs before Expression and Output.Expression. It receives the same variables as Expression (see Expression documentation below) and must return a map/object. The result is injected as top-level binding Variables (map) for Expression and Output.Expression only — use Variables.<key> in those expressions. The Variables binding is not set when spec.variables is omitted. It is not available to response.output.expression. The result is also available in Go templates for DescriptionTemplate and UrlTemplate:
             *       - trigger.when.variables result → {{ index .TriggerVariables "key" }}
             *       - success.when.variables result → {{ index .SuccessVariables "key" }}
             */
            variables?: components["schemas"]["OutputSpec"];
        };
        /** @description WindowStatus holds details about a specific window state (active or upcoming). */
        WindowStatus: {
            /** @description Allow is the cron expression of the allow window, if applicable. */
            allow?: string;
            /** @description Exclude is the cron expression of the exclusion window, if applicable. */
            exclude?: string;
            /** @description Transition is when this window state is expected to change (e.g. window closing for active, window opening for next). */
            transition?: components["schemas"]["Time"];
        };
        /** @description ObjectReference is a reference to an object by name. It is used to refer to objects in the same namespace. */
        io_argoproj_promoter_v1alpha1_ObjectReference: {
            /**
             * @description Name is the name of the object to refer to.
             * @default
             */
            name: string;
        };
    };
    responses: never;
    parameters: never;
    requestBodies: never;
    headers: never;
    pathItems: never;
};
export type $defs = Record<string, never>;
export type operations = Record<string, never>;
