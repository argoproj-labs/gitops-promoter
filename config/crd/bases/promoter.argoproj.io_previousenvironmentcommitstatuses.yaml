---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: previousenvironmentcommitstatuses.promoter.argoproj.io
spec:
  group: promoter.argoproj.io
  names:
    kind: PreviousEnvironmentCommitStatus
    listKind: PreviousEnvironmentCommitStatusList
    plural: previousenvironmentcommitstatuses
    singular: previousenvironmentcommitstatus
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.promotionStrategyRef.name
      name: PromotionStrategy
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          PreviousEnvironmentCommitStatus is the Schema for the previousenvironmentcommitstatuses API.
          This resource is automatically created and managed by the PromotionStrategy controller.
          It manages CommitStatus resources that gate promotions based on the health of the previous environment.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PreviousEnvironmentCommitStatus
            properties:
              environments:
                description: |-
                  Environments lists the environments to monitor for previous-environment checks.
                  This is derived from the PromotionStrategy's environments configuration.
                items:
                  description: PreviousEnvEnvironment defines an environment that
                    needs previous-environment checking.
                  properties:
                    activeCommitStatuses:
                      description: |-
                        ActiveCommitStatuses from the PromotionStrategy spec for this environment.
                        These are the commit statuses that must pass before promotion can proceed.
                      items:
                        description: CommitStatusSelector is used to select commit
                          statuses by their key.
                        properties:
                          key:
                            maxLength: 63
                            minLength: 1
                            pattern: ([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]
                            type: string
                        required:
                        - key
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - key
                      x-kubernetes-list-type: map
                    branch:
                      description: Branch is the name of the environment branch.
                      minLength: 1
                      type: string
                  required:
                  - branch
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - branch
                x-kubernetes-list-type: map
              promotionStrategyRef:
                description: |-
                  PromotionStrategyRef is a reference to the parent PromotionStrategy.
                  The controller gets RepositoryReference from the CTPs when creating CommitStatuses.
                properties:
                  name:
                    description: Name is the name of the object to refer to.
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                required:
                - name
                type: object
            required:
            - environments
            - promotionStrategyRef
            type: object
          status:
            description: status defines the observed state of PreviousEnvironmentCommitStatus
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              environments:
                description: Environments tracks the state of each environment's previous-environment
                  check.
                items:
                  description: PreviousEnvEnvironmentStatus defines the observed state
                    of a previous-environment check for an environment.
                  properties:
                    branch:
                      description: Branch is the name of the environment branch.
                      minLength: 1
                      type: string
                    commitStatusName:
                      description: CommitStatusName is the name of the CommitStatus
                        resource created for this environment.
                      type: string
                    phase:
                      description: Phase is the current phase of the previous-environment
                        check (pending/success).
                      enum:
                      - pending
                      - success
                      type: string
                    previousEnvironmentBranch:
                      description: PreviousEnvironmentBranch is the branch of the
                        previous environment being checked.
                      type: string
                    sha:
                      description: |-
                        Sha is the commit SHA that this status applies to.
                        Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
                      maxLength: 64
                      pattern: ^([a-f0-9]{40}|[a-f0-9]{64})?$
                      type: string
                  required:
                  - branch
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - branch
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
