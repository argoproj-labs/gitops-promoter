---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: timedcommitstatuses.promoter.argoproj.io
spec:
  group: promoter.argoproj.io
  names:
    kind: TimedCommitStatus
    listKind: TimedCommitStatusList
    plural: timedcommitstatuses
    singular: timedcommitstatus
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.promotionStrategyRef.name
      name: PromotionStrategy
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TimedCommitStatus is the Schema for the timedcommitstatuses API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of TimedCommitStatus
            properties:
              environments:
                items:
                  description: TimedCommitStatusEnvironments defines the branch/environment
                    and duration to wait before reporting the gate as success.
                  properties:
                    branch:
                      description: Branch is the name of the branch/environment you
                        want to gate for the configured duration.
                      type: string
                    duration:
                      description: |-
                        Duration is the time duration to wait before considering the commit status as success.
                        The duration should be in a format accepted by Go's time.ParseDuration function, e.g., "5m", "1h30m".
                      type: string
                  required:
                  - branch
                  - duration
                  type: object
                type: array
              promotionStrategyRef:
                description: PromotionStrategyRef is a reference to the promotion
                  strategy that this timed commit status applies to.
                properties:
                  name:
                    description: Name is the name of the object to refer to.
                    type: string
                required:
                - name
                type: object
            required:
            - environments
            - promotionStrategyRef
            type: object
          status:
            description: status defines the observed state of TimedCommitStatus
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              environments:
                description: Environments holds the status of each environment being
                  tracked.
                items:
                  description: TimedCommitStatusEnvironmentsStatus defines the observed
                    timing status for a specific environment.
                  properties:
                    atMostDurationRemaining:
                      description: |-
                        AtMostDurationRemaining is the maximum amount of time remaining until the gate is satisfied.
                        This is calculated at reconciliation time. When the gate is satisfied (phase=success), this will be 0.
                      type: string
                    branch:
                      description: Branch is the name of the branch/environment.
                      type: string
                    commitTime:
                      description: CommitTime is when the commit was deployed to the
                        active environment.
                      format: date-time
                      type: string
                    phase:
                      description: Phase represents the current phase of the timed
                        gate.
                      enum:
                      - pending
                      - success
                      type: string
                    requiredDuration:
                      description: |-
                        RequiredDuration is the duration that must elapse before promotion is allowed.
                        If this is updated in the spec with a pending time. The new duration will be calculated from the original CommitTime.
                        Allowing you to extend the duration of an already deployed commit, if the time has not yet elapsed.
                      type: string
                    sha:
                      description: Sha is the commit SHA being tracked for this environment.
                      type: string
                  required:
                  - atMostDurationRemaining
                  - branch
                  - commitTime
                  - phase
                  - requiredDuration
                  - sha
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - branch
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
