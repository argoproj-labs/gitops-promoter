---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: regexapplytests.
spec:
  group: ""
  names:
    kind: RegexApplyTest
    listKind: RegexApplyTestList
    plural: regexapplytests
    singular: regexapplytest
  scope: Namespaced
  versions:
  - name: ""
    schema:
      openAPIV3Schema:
        description: |-
          RegexApplyTest is the type for the RegexApplyTest custom resource.
          It mirrors the CRD in crd.yaml. OptionalSha uses omitempty so that
          empty string is omitted from JSON and thus never sent to the server.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RegexApplyTestSpec is the spec of RegexApplyTest.
            properties:
              optionalSha:
                description: |-
                  OptionalSha is optional. CRD pattern rejects "" but allows 40/64-char hex.
                  With omitempty, Go value "" is not serialized → field absent in patch →
                  server does not validate "".
                type: string
            type: object
        type: object
    served: true
    storage: true
