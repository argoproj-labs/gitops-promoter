---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: regexapplytestapplyconfigs.
spec:
  group: ""
  names:
    kind: RegexApplyTestApplyConfig
    listKind: RegexApplyTestApplyConfigList
    plural: regexapplytestapplyconfigs
    singular: regexapplytestapplyconfig
  scope: Namespaced
  versions:
  - name: ""
    schema:
      openAPIV3Schema:
        description: |-
          RegexApplyTestApplyConfig is a full apply-config style object (like
          generated applyconfiguration types) for SSA. Spec uses pointer fields
          so that WithOptionalSha("") produces "optionalSha": "" in JSON.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              RegexApplyTestSpecApplyConfig mirrors the applyconfiguration pattern used by
              client-go / controller-gen: pointer fields with omitempty. WithXxx("") sets
              the pointer to &"", so the field is present in JSON (omitempty only omits
              when the pointer is nil). This matches how promoter's applyconfiguration
              types (e.g. CommitShaStateApplyConfiguration.Sha *string) behave.
            properties:
              optionalSha:
                type: string
            type: object
        type: object
    served: true
    storage: true
