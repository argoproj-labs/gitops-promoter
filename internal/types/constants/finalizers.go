package constants

const (
	// PullRequestFinalizer is the finalizer used to ensure PullRequest resources are properly cleaned up.
	PullRequestFinalizer = "pullrequest.promoter.argoporoj.io/finalizer"
)
